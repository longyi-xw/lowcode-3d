# v0.3 sub-stage A — AI proxy + BYO-key + provider 配置 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** Rust 端 AI proxy（先 Anthropic，留 provider 抽象）+ OS keychain 存 API key + Settings Dialog「AI providers」配置 UI，让后续 sub-stage B 的 Skill 能调通 LLM；key 绝不进 renderer/bundle/导出/日志。

**架构：** Rust `ai.rs`：纯函数 `build_anthropic_body`/`parse_anthropic_response`（cargo test 红绿）+ async 命令（keychain via `keyring`、HTTP via `reqwest`，手动验证）。前端 `services/ai/proxy.ts` 封装 specta bindings 命令 + 读 settings provider/model。Settings Dialog（接预留 `useUIStore.settingsOpen`，仿 `ShortcutsHelpDialog`）挂 App 根，AI providers section 内放 Anthropic 卡。

**技术栈：** Rust（tauri 2 + serde + specta + reqwest + keyring）· TypeScript · Zustand · React + Radix Dialog · react-i18next · Vitest · cargo test。

**前置：** spec `docs/superpowers/specs/2026-06-05-v0.3a-ai-proxy-design.md`。分支 `feat/v0.3a-ai-proxy`（spec 已 commit）。基线 `pnpm test` 396 绿。

> **相对 spec 的实现说明**：spec §5.3 假设「Settings 视图」已存在，实际项目无独立 Settings 面板（`settingsOpen` 是预留态、唯一设置 UI 是 StartupView 内联语言/主题）。经用户确认按 `design/prototype/img_5.png` 原型放进 settings → 本 plan **新建 Settings Dialog 壳**（T7）。本期只实装 AI providers 页的 **Anthropic 卡**；OpenAI/Ollama 卡、Skill routing、其余 nav 页（General/Appearance/…）随后续填充。**遵循 spec 安全：只显示 connected 状态、不回显 key 任何片段**（偏离原型的 key 末位脱敏，更保守）。

---

## 文件结构

**新增：**

- `src-tauri/src/ai.rs` — `AiProvider`/`AiCompleteRequest`/`AiCompleteResponse`/`AiError` 类型 + 纯函数 `build_anthropic_body`/`parse_anthropic_response`（+ `#[cfg(test)]`）+ 5 个命令。
- `src/services/ai/proxy.ts`(+`.test.ts`) — 封装 bindings 命令 + 读 settings。
- `src/ui/settings/SettingsDialog.tsx`(+`.test.tsx`) — Settings Dialog 壳（nav + 内容路由）。
- `src/ui/settings/AiProvidersSection.tsx`(+`.test.tsx`) — AI providers 页（Anthropic 卡）。

**修改：**

- `src-tauri/Cargo.toml`（reqwest + keyring）
- `src-tauri/src/lib.rs`（`mod ai;` + `use` + `collect_commands![]` 加 5 命令）
- `src/bindings/tauri.ts`（`pnpm bindings` 生成，git-tracked）
- `src/services/settings/store.ts`(+`.test.ts`)（aiProvider/aiModel + persist v2 迁移）
- `src/App.tsx`（挂 `<SettingsDialog/>`）
- `src/ui/views/StartupView.tsx` + `src/ui/views/EditorView.tsx`（齿轮按钮 → `setSettingsOpen(true)`）
- `src/i18n/locales/{en-US,zh-CN}/settings.json`（`nav.*` + `ai.*`）
- `docs/roadmap.md`（T9，v0.3 标 in-progress + sub-stage A 勾选）

---

## 任务 T1：Cargo 依赖 + ai.rs 类型骨架

**文件：** 修改 `src-tauri/Cargo.toml`；创建 `src-tauri/src/ai.rs`

- [ ] **步骤 1：加依赖** — `src-tauri/Cargo.toml` 的 `[dependencies]` 末尾加：

```toml
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
keyring = "3"
```

> `default-features = false` + `rustls-tls` 避免 openssl（Homebrew Rust 无系统 openssl-dev 时也能编）。`keyring` 3.x：macOS 用 Security framework（无额外系统依赖）；**Linux 构建需 secret-service/dbus**——见 T9 风险。

- [ ] **步骤 2：创建 `ai.rs` 类型骨架**

```rust
use serde::{Deserialize, Serialize};
use serde_json::Value;
use specta::Type;

/// 本期仅 Anthropic;加 provider = 加 enum 分支 + match 分支。
/// lowercase 序列化 → "anthropic"，与前端 settings.aiProvider + keychain account 一致。
#[derive(Debug, Clone, Copy, Serialize, Deserialize, Type)]
#[serde(rename_all = "lowercase")]
pub enum AiProvider {
    Anthropic,
}

#[derive(Debug, Serialize, Deserialize, Type)]
pub struct AiCompleteRequest {
    pub provider: AiProvider,
    pub model: String,
    pub system: String,
    pub user: String,
    pub json_schema: Option<Value>,
}

#[derive(Debug, Serialize, Deserialize, Type)]
pub struct AiCompleteResponse {
    pub text: Option<String>,
    pub json: Option<Value>,
}

/// 与 FolderError 同款 specta 形状: { code, data? }。
#[derive(Debug, Serialize, Deserialize, Type)]
#[serde(tag = "code", content = "data", rename_all = "snake_case")]
pub enum AiError {
    NoKey,
    Network(String),
    ApiError { status: u16, message: String },
    Parse(String),
    Keychain(String),
}
```

> `Keychain(String)` 是相对 spec 4 变体的小增补——keychain 读写失败需独立错误，比硬塞进 Network 语义更清晰。

- [ ] **步骤 3：lib.rs 先声明模块**（让 ai.rs 进编译）— `src-tauri/src/lib.rs` 顶部 `mod project_state;` 后加 `mod ai;`。

- [ ] **步骤 4：编译确认**

运行：`cargo build --manifest-path src-tauri/Cargo.toml`
预期：编译通过（首次拉 reqwest/keyring 依赖，较慢）。`ai.rs` 暂有未使用类型告警可接受（T2/T3 用上）。

- [ ] **步骤 5：Commit**

```sh
git add src-tauri/Cargo.toml src-tauri/Cargo.lock src-tauri/src/ai.rs src-tauri/src/lib.rs
git commit -m "feat(ai): Cargo deps (reqwest/keyring) + ai.rs type skeleton

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T2：ai.rs 纯函数 build/parse + cargo test

**文件：** 修改 `src-tauri/src/ai.rs`

- [ ] **步骤 1：写失败测试**（`ai.rs` 末尾加 `#[cfg(test)] mod tests`）

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use serde_json::json;

    #[test]
    fn body_without_schema_has_messages_no_tools() {
        let b = build_anthropic_body("m", "sys", "hi", None);
        assert_eq!(b["model"], "m");
        assert_eq!(b["system"], "sys");
        assert_eq!(b["messages"][0]["role"], "user");
        assert_eq!(b["messages"][0]["content"], "hi");
        assert!(b.get("tools").is_none());
    }

    #[test]
    fn body_with_schema_forces_tool_use() {
        let schema = json!({ "type": "object" });
        let b = build_anthropic_body("m", "", "", Some(&schema));
        assert_eq!(b["tools"][0]["name"], "emit_result");
        assert_eq!(b["tools"][0]["input_schema"], schema);
        assert_eq!(b["tool_choice"]["type"], "tool");
        assert_eq!(b["tool_choice"]["name"], "emit_result");
    }

    #[test]
    fn parse_text_response() {
        let r = parse_anthropic_response(&json!({
            "content": [{ "type": "text", "text": "hello" }]
        }))
        .unwrap();
        assert_eq!(r.text.as_deref(), Some("hello"));
        assert!(r.json.is_none());
    }

    #[test]
    fn parse_tool_use_response() {
        let r = parse_anthropic_response(&json!({
            "content": [{ "type": "tool_use", "name": "emit_result", "input": { "x": 1 } }]
        }))
        .unwrap();
        assert_eq!(r.json, Some(json!({ "x": 1 })));
        assert!(r.text.is_none());
    }

    #[test]
    fn parse_missing_content_errors() {
        assert!(matches!(
            parse_anthropic_response(&json!({})).unwrap_err(),
            AiError::Parse(_)
        ));
    }
}
```

- [ ] **步骤 2：运行确认失败**

运行：`cargo test --manifest-path src-tauri/Cargo.toml ai::tests`
预期：编译失败（`build_anthropic_body`/`parse_anthropic_response` 未定义）。

- [ ] **步骤 3：实现纯函数**（`ai.rs`，类型定义之后、tests 之前）

```rust
const ANTHROPIC_VERSION: &str = "2023-06-01";
const ANTHROPIC_URL: &str = "https://api.anthropic.com/v1/messages";
const TOOL_NAME: &str = "emit_result";
const MAX_TOKENS: u32 = 1024;

/// 组 Anthropic Messages 请求体。有 schema → 加单个强制 tool（结构化输出）。
pub fn build_anthropic_body(
    model: &str,
    system: &str,
    user: &str,
    schema: Option<&Value>,
) -> Value {
    let mut body = serde_json::json!({
        "model": model,
        "max_tokens": MAX_TOKENS,
        "system": system,
        "messages": [{ "role": "user", "content": user }],
    });
    if let Some(schema) = schema {
        body["tools"] = serde_json::json!([{
            "name": TOOL_NAME,
            "description": "Return the result as structured data.",
            "input_schema": schema,
        }]);
        body["tool_choice"] = serde_json::json!({ "type": "tool", "name": TOOL_NAME });
    }
    body
}

/// 解析 Anthropic 响应:优先 tool_use block 的 input(结构化),否则拼 text block。
pub fn parse_anthropic_response(body: &Value) -> Result<AiCompleteResponse, AiError> {
    let content = body
        .get("content")
        .and_then(|c| c.as_array())
        .ok_or_else(|| AiError::Parse("missing content array".into()))?;

    for block in content {
        if block.get("type").and_then(|t| t.as_str()) == Some("tool_use") {
            let input = block
                .get("input")
                .cloned()
                .ok_or_else(|| AiError::Parse("tool_use without input".into()))?;
            return Ok(AiCompleteResponse { text: None, json: Some(input) });
        }
    }

    let mut text = String::new();
    for block in content {
        if block.get("type").and_then(|t| t.as_str()) == Some("text") {
            if let Some(t) = block.get("text").and_then(|t| t.as_str()) {
                text.push_str(t);
            }
        }
    }
    if text.is_empty() {
        return Err(AiError::Parse("no text or tool_use blocks".into()));
    }
    Ok(AiCompleteResponse { text: Some(text), json: None })
}
```

- [ ] **步骤 4：运行确认通过**

运行：`cargo test --manifest-path src-tauri/Cargo.toml ai::tests`
预期：5 tests pass。

- [ ] **步骤 5：Commit**

```sh
git add src-tauri/src/ai.rs
git commit -m "feat(ai): build_anthropic_body + parse_anthropic_response (pure, tested)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T3：ai.rs 命令（keychain + reqwest）+ lib.rs 注册

**文件：** 修改 `src-tauri/src/ai.rs`、`src-tauri/src/lib.rs`

- [ ] **步骤 1：ai.rs 加命令**（纯函数之后）

```rust
use keyring::{Entry, Error as KeyringError};

const KEYCHAIN_SERVICE: &str = "lowcode3d-ai";

fn provider_account(p: AiProvider) -> &'static str {
    match p {
        AiProvider::Anthropic => "anthropic",
    }
}

fn key_entry(p: AiProvider) -> Result<Entry, AiError> {
    Entry::new(KEYCHAIN_SERVICE, provider_account(p))
        .map_err(|e| AiError::Keychain(e.to_string()))
}

#[tauri::command]
#[specta::specta]
pub fn set_ai_key(provider: AiProvider, key: String) -> Result<(), AiError> {
    key_entry(provider)?
        .set_password(&key)
        .map_err(|e| AiError::Keychain(e.to_string()))
}

#[tauri::command]
#[specta::specta]
pub fn has_ai_key(provider: AiProvider) -> Result<bool, AiError> {
    match key_entry(provider)?.get_password() {
        Ok(_) => Ok(true),
        Err(KeyringError::NoEntry) => Ok(false),
        Err(e) => Err(AiError::Keychain(e.to_string())),
    }
}

#[tauri::command]
#[specta::specta]
pub fn clear_ai_key(provider: AiProvider) -> Result<(), AiError> {
    match key_entry(provider)?.delete_credential() {
        Ok(()) | Err(KeyringError::NoEntry) => Ok(()),
        Err(e) => Err(AiError::Keychain(e.to_string())),
    }
}

async fn anthropic_complete(
    key: &str,
    req: &AiCompleteRequest,
) -> Result<AiCompleteResponse, AiError> {
    let body =
        build_anthropic_body(&req.model, &req.system, &req.user, req.json_schema.as_ref());
    let resp = reqwest::Client::new()
        .post(ANTHROPIC_URL)
        .header("x-api-key", key)
        .header("anthropic-version", ANTHROPIC_VERSION)
        .json(&body)
        .send()
        .await
        .map_err(|e| AiError::Network(e.to_string()))?;
    let status = resp.status();
    let val: Value = resp
        .json()
        .await
        .map_err(|e| AiError::Parse(e.to_string()))?;
    if !status.is_success() {
        let message = val["error"]["message"]
            .as_str()
            .unwrap_or("unknown error")
            .to_string();
        return Err(AiError::ApiError { status: status.as_u16(), message });
    }
    parse_anthropic_response(&val)
}

#[tauri::command]
#[specta::specta]
pub async fn ai_complete(req: AiCompleteRequest) -> Result<AiCompleteResponse, AiError> {
    let key = match key_entry(req.provider)?.get_password() {
        Ok(k) => k,
        Err(KeyringError::NoEntry) => return Err(AiError::NoKey),
        Err(e) => return Err(AiError::Keychain(e.to_string())),
    };
    match req.provider {
        AiProvider::Anthropic => anthropic_complete(&key, &req).await,
    }
}

#[tauri::command]
#[specta::specta]
pub async fn test_ai_provider(
    provider: AiProvider,
    model: String,
) -> Result<(), AiError> {
    let req = AiCompleteRequest {
        provider,
        model,
        system: String::new(),
        user: "ping".into(),
        json_schema: None,
    };
    ai_complete(req).await.map(|_| ())
}
```

- [ ] **步骤 2：lib.rs 注册命令**

`src-tauri/src/lib.rs`：`use project_state::{...};` 之后加：

```rust
use ai::{ai_complete, clear_ai_key, has_ai_key, set_ai_key, test_ai_provider};
```

`collect_commands![]` 末尾（`write_export_files,` 后）加：

```rust
        set_ai_key,
        has_ai_key,
        clear_ai_key,
        ai_complete,
        test_ai_provider,
```

- [ ] **步骤 3：编译确认**

运行：`cargo build --manifest-path src-tauri/Cargo.toml`
预期：通过（命令胶水 + keychain + 真实 HTTP **不**单测——T9 人工 tauri dev 验证）。

- [ ] **步骤 4：Commit**

```sh
git add src-tauri/src/ai.rs src-tauri/src/lib.rs
git commit -m "feat(ai): set/has/clear_ai_key + ai_complete + test_ai_provider commands

keychain(keyring) 存取 + reqwest 调 Anthropic;key 不回 renderer。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T4：重新生成 bindings

**文件：** 修改 `src/bindings/tauri.ts`（生成）

- [ ] **步骤 1：生成**

运行：`pnpm bindings`
预期：无报错；`src/bindings/tauri.ts` 出现 `aiComplete`/`setAiKey`/`hasAiKey`/`clearAiKey`/`testAiProvider` 命令 + `AiProvider`/`AiCompleteRequest`/`AiCompleteResponse`/`AiError` 类型。

- [ ] **步骤 2：确认形状**

运行：`grep -nE "aiComplete|setAiKey|hasAiKey|AiError|AiCompleteRequest" src/bindings/tauri.ts`
预期：命令 + 类型存在。**记下 `AiCompleteRequest` 字段在 TS 里的实际命名**（参照 `ExportPayload` 保留 snake_case，预期 `json_schema`）——T6 proxy.ts 按此对齐。`typecheck`：`pnpm typecheck` 通过。

- [ ] **步骤 3：Commit**

```sh
git add src/bindings/tauri.ts
git commit -m "chore(bindings): regenerate for ai_* commands

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T5：settings store 加 aiProvider/aiModel + persist 迁移

**文件：** 修改 `src/services/settings/store.ts` + `store.test.ts`（若无则建）

- [ ] **步骤 1：写失败测试**（`src/services/settings/store.test.ts`）

```ts
import { describe, expect, it } from "vitest";

import { useSettingsStore } from "./store";

describe("useSettingsStore AI config", () => {
  it("defaults aiProvider=anthropic + a non-empty aiModel", () => {
    const s = useSettingsStore.getState();
    expect(s.aiProvider).toBe("anthropic");
    expect(typeof s.aiModel).toBe("string");
    expect(s.aiModel.length).toBeGreaterThan(0);
  });

  it("setAiModel updates the model", () => {
    useSettingsStore.getState().setAiModel("claude-x");
    expect(useSettingsStore.getState().aiModel).toBe("claude-x");
  });
});
```

- [ ] **步骤 2：运行确认失败**

运行：`pnpm vitest run src/services/settings/store.test.ts`
预期：FAIL（`aiProvider`/`setAiModel` undefined）。

- [ ] **步骤 3：实现**（`store.ts`）

类型 + 接口 + body + persist 版本。顶部加：

```ts
export type AiProvider = "anthropic"; // union 预留(后续 | "openai" | "ollama")
```

`SettingsState` 接口加：

```ts
  aiProvider: AiProvider;
  aiModel: string;
  setAiProvider: (provider: AiProvider) => void;
  setAiModel: (model: string) => void;
```

store body 加（默认值）：

```ts
      aiProvider: "anthropic",
      aiModel: "claude-3-5-sonnet-latest", // 用户可在 Settings 改;测试连接校验
      setAiProvider: (aiProvider) => set({ aiProvider }),
      setAiModel: (aiModel) => set({ aiModel }),
```

persist 配置 `version: 1` 改 `version: 2` + 加 migrate（老版本补默认）：

```ts
      version: 2,
      migrate: (persisted, from) => {
        const s = (persisted ?? {}) as Partial<SettingsState>;
        if (from < 2) {
          s.aiProvider = s.aiProvider ?? "anthropic";
          s.aiModel = s.aiModel ?? "claude-3-5-sonnet-latest";
        }
        return s as SettingsState;
      },
```

- [ ] **步骤 4：运行确认通过**

运行：`pnpm vitest run src/services/settings/store.test.ts`
预期：PASS（2）。

- [ ] **步骤 5：Commit**

```sh
git add src/services/settings/store.ts src/services/settings/store.test.ts
git commit -m "feat(settings): aiProvider/aiModel config (persist v2 migrate)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T6：services/ai/proxy.ts

**文件：** 创建 `src/services/ai/proxy.ts` + `proxy.test.ts`

- [ ] **步骤 1：写失败测试**（`proxy.test.ts`，mock bindings + settings）

```ts
import { beforeEach, describe, expect, it, vi } from "vitest";

vi.mock("@/bindings/tauri", () => ({
  commands: {
    aiComplete: vi.fn(),
    setAiKey: vi.fn(),
    hasAiKey: vi.fn(),
    clearAiKey: vi.fn(),
    testAiProvider: vi.fn(),
  },
}));

import { commands } from "@/bindings/tauri";
import { useSettingsStore } from "@/services/settings/store";

import { aiComplete, hasAiKey, setAiKey } from "./proxy";

describe("ai proxy", () => {
  beforeEach(() => {
    vi.clearAllMocks();
    useSettingsStore.setState({ aiProvider: "anthropic", aiModel: "claude-x" });
  });

  it("aiComplete reads provider/model from settings + returns data on ok", async () => {
    vi.mocked(commands.aiComplete).mockResolvedValue({
      status: "ok",
      data: { text: "hi", json: null },
    });
    const res = await aiComplete({ system: "s", user: "u" });
    expect(commands.aiComplete).toHaveBeenCalledWith(
      expect.objectContaining({
        provider: "anthropic",
        model: "claude-x",
        system: "s",
        user: "u",
      }),
    );
    expect(res.text).toBe("hi");
  });

  it("aiComplete throws the AiError on error status", async () => {
    vi.mocked(commands.aiComplete).mockResolvedValue({
      status: "error",
      error: { code: "no_key" },
    });
    await expect(aiComplete({ system: "", user: "u" })).rejects.toMatchObject({
      code: "no_key",
    });
  });

  it("setAiKey + hasAiKey delegate to commands for the current provider", async () => {
    vi.mocked(commands.setAiKey).mockResolvedValue({ status: "ok", data: null });
    await setAiKey("sk-test");
    expect(commands.setAiKey).toHaveBeenCalledWith("anthropic", "sk-test");

    vi.mocked(commands.hasAiKey).mockResolvedValue({ status: "ok", data: true });
    expect(await hasAiKey()).toBe(true);
  });
});
```

- [ ] **步骤 2：运行确认失败**

运行：`pnpm vitest run src/services/ai/proxy.test.ts`
预期：FAIL（模块缺失）。

- [ ] **步骤 3：实现 `proxy.ts`**

> `json_schema` 字段名对齐 T4 生成的 bindings（预期 snake_case）。`Result` 形状 `{status:"ok",data}|{status:"error",error}`（同 `commands.importGlbIntoProject` 用法）。

```ts
import { commands, type AiCompleteResponse, type AiError } from "@/bindings/tauri";
import { useSettingsStore } from "@/services/settings/store";

function provider() {
  return useSettingsStore.getState().aiProvider;
}

/** Throws the AiError (`{ code, data? }`) on failure; callers map to toast. */
export async function aiComplete(opts: {
  system: string;
  user: string;
  jsonSchema?: unknown;
}): Promise<AiCompleteResponse> {
  const { aiProvider, aiModel } = useSettingsStore.getState();
  const res = await commands.aiComplete({
    provider: aiProvider,
    model: aiModel,
    system: opts.system,
    user: opts.user,
    json_schema: (opts.jsonSchema ?? null) as never,
  });
  if (res.status === "error") throw res.error satisfies AiError;
  return res.data;
}

export async function setAiKey(key: string): Promise<void> {
  const res = await commands.setAiKey(provider(), key);
  if (res.status === "error") throw res.error;
}

export async function hasAiKey(): Promise<boolean> {
  const res = await commands.hasAiKey(provider());
  if (res.status === "error") throw res.error;
  return res.data;
}

export async function clearAiKey(): Promise<void> {
  const res = await commands.clearAiKey(provider());
  if (res.status === "error") throw res.error;
}

/** Resolves on success; throws AiError on failure (used by 测试连接 button). */
export async function testConnection(): Promise<void> {
  const { aiProvider, aiModel } = useSettingsStore.getState();
  const res = await commands.testAiProvider(aiProvider, aiModel);
  if (res.status === "error") throw res.error;
}
```

- [ ] **步骤 4：运行确认通过**

运行：`pnpm vitest run src/services/ai/proxy.test.ts && pnpm typecheck`
预期：PASS（3）+ typecheck 0。（若 `json_schema` 字段名与生成 bindings 不符，按 bindings 改键名。）

- [ ] **步骤 5：Commit**

```sh
git add src/services/ai/proxy.ts src/services/ai/proxy.test.ts
git commit -m "feat(ai): proxy.ts wraps ai_* commands + reads settings provider/model

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T7：Settings Dialog 壳 + App 挂载 + 齿轮触发 + i18n

**文件：** 创建 `src/ui/settings/SettingsDialog.tsx`(+`.test.tsx`)；修改 `src/App.tsx`、`src/ui/views/StartupView.tsx`、`src/ui/views/EditorView.tsx`、`src/i18n/locales/{en-US,zh-CN}/settings.json`

- [ ] **步骤 1：i18n `nav.*`**（settings.json en，在 `language` 同级加）

```json
  "nav": {
    "ai_providers": "AI providers"
  },
```

zh：`"ai_providers": "AI 服务商"`。

- [ ] **步骤 2：写失败测试**（`SettingsDialog.test.tsx`）

```tsx
import { render, screen } from "@testing-library/react";
import { beforeEach, describe, expect, it } from "vitest";

import { useUIStore } from "@/services/ui/store";

import { SettingsDialog } from "./SettingsDialog";

describe("SettingsDialog", () => {
  beforeEach(() => useUIStore.setState({ settingsOpen: false }));

  it("does not render content when settingsOpen is false", () => {
    render(<SettingsDialog />);
    expect(screen.queryByText("AI providers")).not.toBeInTheDocument();
  });

  it("shows the AI providers nav + section when open", () => {
    useUIStore.setState({ settingsOpen: true });
    render(<SettingsDialog />);
    expect(screen.getAllByText("AI providers").length).toBeGreaterThan(0);
  });
});
```

- [ ] **步骤 3：运行确认失败** → FAIL（模块缺失）。

- [ ] **步骤 4：实现 `SettingsDialog.tsx`**（仿 `ShortcutsHelpDialog`，接 `settingsOpen`；nav 数据驱动，本期单页）

```tsx
import { useState } from "react";
import { useTranslation } from "react-i18next";

import {
  Dialog,
  DialogContent,
  DialogHeader,
  DialogTitle,
} from "@/components/ui/dialog";
import { cn } from "@/lib/utils";
import { useUIStore } from "@/services/ui/store";

import { AiProvidersSection } from "./AiProvidersSection";

type SettingsPage = "ai_providers";

const NAV: { id: SettingsPage; labelKey: string }[] = [
  { id: "ai_providers", labelKey: "nav.ai_providers" },
];

export function SettingsDialog() {
  const { t } = useTranslation("settings");
  const open = useUIStore((s) => s.settingsOpen);
  const setOpen = useUIStore((s) => s.setSettingsOpen);
  const [page, setPage] = useState<SettingsPage>("ai_providers");

  return (
    <Dialog open={open} onOpenChange={setOpen}>
      <DialogContent className="max-w-[760px]">
        <DialogHeader>
          <DialogTitle>{t("title")}</DialogTitle>
        </DialogHeader>
        <div className="grid grid-cols-[180px_1fr] gap-4">
          <nav className="flex flex-col gap-1 border-r border-border pr-2">
            {NAV.map((item) => (
              <button
                key={item.id}
                type="button"
                onClick={() => setPage(item.id)}
                className={cn(
                  "rounded px-2 py-1 text-left text-sm",
                  page === item.id
                    ? "bg-primary text-primary-foreground"
                    : "text-muted-foreground hover:bg-muted",
                )}
              >
                {t(item.labelKey)}
              </button>
            ))}
          </nav>
          <div className="min-h-[320px]">
            {page === "ai_providers" && <AiProvidersSection />}
          </div>
        </div>
      </DialogContent>
    </Dialog>
  );
}
```

> 注：`AiProvidersSection`（T8）尚不存在——T7 步骤先建一个最小占位（`export function AiProvidersSection(){ return <p>AI providers</p>; }`）让 T7 测试过，T8 再替换为真实实现。或调整顺序先 T8。**本 plan 取：T7 先放占位，T8 实装**——故 T7 步骤 4 同时建占位文件 `src/ui/settings/AiProvidersSection.tsx`：

```tsx
// 占位,T8 替换
export function AiProvidersSection() {
  return <p className="text-sm text-muted-foreground">AI providers</p>;
}
```

- [ ] **步骤 5：App 挂载 + 齿轮触发**

`src/App.tsx`：import `SettingsDialog`，在 `<NewProjectDialog />` 后加 `<SettingsDialog />`。

`StartupView.tsx` + `EditorView.tsx`：各加一个齿轮按钮 → `useUIStore.getState().setSettingsOpen(true)`（EditorView 放 hierarchy header 的 close 按钮旁；StartupView 放语言/主题控件附近）。用 lucide `Settings` 图标：

```tsx
import { Settings } from "lucide-react";
// ...
<button
  type="button"
  onClick={() => setSettingsOpen(true)}
  title={t("settings:title")}
  className="...(仿现有图标按钮)"
>
  <Settings className="h-3.5 w-3.5" />
</button>;
```

（EditorView 从 `useUIStore` 取 `setSettingsOpen`；StartupView 同。）

- [ ] **步骤 6：运行确认通过 + typecheck**

运行：`pnpm vitest run src/ui/settings/SettingsDialog.test.tsx && pnpm typecheck`
预期：PASS（2）+ typecheck 0。

- [ ] **步骤 7：Commit**

```sh
git add src/ui/settings/SettingsDialog.tsx src/ui/settings/SettingsDialog.test.tsx \
        src/ui/settings/AiProvidersSection.tsx src/App.tsx \
        src/ui/views/StartupView.tsx src/ui/views/EditorView.tsx \
        src/i18n/locales/en-US/settings.json src/i18n/locales/zh-CN/settings.json
git commit -m "feat(settings): SettingsDialog shell (settingsOpen) + gear triggers + nav i18n

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T8：AI providers section（Anthropic 卡）

**文件：** 替换 `src/ui/settings/AiProvidersSection.tsx` + 创建 `.test.tsx`；改 settings.json（`ai.*`）

- [ ] **步骤 1：i18n `ai.*`**（settings.json en，`nav` 同级）

```json
  "ai": {
    "title": "AI providers",
    "byok_note": "Bring your own key. Calls are proxied through the Tauri Rust process — the renderer never sees the key.",
    "provider": { "anthropic": "Anthropic" },
    "model": "Model",
    "api_key": "API key",
    "key_placeholder": "sk-ant-…",
    "configured": "configured",
    "not_configured": "not configured",
    "save": "Save",
    "clear": "Clear",
    "test": "Test connection",
    "test_ok": "Connection OK",
    "test_fail": "Connection failed: {{message}}"
  },
```

zh 对应：`title:"AI 服务商"`、`byok_note:"自带 key。调用经 Tauri Rust 进程代理——渲染层永不接触 key。"`、`provider.anthropic:"Anthropic"`、`model:"模型"`、`api_key:"API 密钥"`、`key_placeholder:"sk-ant-…"`、`configured:"已配置"`、`not_configured:"未配置"`、`save:"保存"`、`clear:"清除"`、`test:"测试连接"`、`test_ok:"连接正常"`、`test_fail:"连接失败：{{message}}"`。

- [ ] **步骤 2：写失败测试**（`AiProvidersSection.test.tsx`，mock proxy）

```tsx
import { fireEvent, render, screen, waitFor } from "@testing-library/react";
import { beforeEach, describe, expect, it, vi } from "vitest";

vi.mock("@/services/ai/proxy", () => ({
  hasAiKey: vi.fn().mockResolvedValue(false),
  setAiKey: vi.fn().mockResolvedValue(undefined),
  clearAiKey: vi.fn().mockResolvedValue(undefined),
  testConnection: vi.fn().mockResolvedValue(undefined),
}));

import { hasAiKey, setAiKey } from "@/services/ai/proxy";

import { AiProvidersSection } from "./AiProvidersSection";

describe("AiProvidersSection", () => {
  beforeEach(() => vi.clearAllMocks());

  it("renders the Anthropic card + BYOK note", async () => {
    render(<AiProvidersSection />);
    expect(screen.getByText("Anthropic")).toBeInTheDocument();
    expect(screen.getByText(/renderer never sees the key/i)).toBeInTheDocument();
    await waitFor(() => expect(hasAiKey).toHaveBeenCalled());
  });

  it("saving a key calls setAiKey then re-checks hasAiKey", async () => {
    render(<AiProvidersSection />);
    fireEvent.change(screen.getByLabelText("API key"), {
      target: { value: "sk-ant-xyz" },
    });
    fireEvent.click(screen.getByText("Save"));
    await waitFor(() => expect(setAiKey).toHaveBeenCalledWith("sk-ant-xyz"));
  });
});
```

- [ ] **步骤 3：运行确认失败** → FAIL（占位无这些元素 / getByLabelText 找不到）。

- [ ] **步骤 4：实现 `AiProvidersSection.tsx`**

```tsx
import { useEffect, useState } from "react";
import { useTranslation } from "react-i18next";
import { toast } from "sonner";

import { cn } from "@/lib/utils";
import { clearAiKey, hasAiKey, setAiKey, testConnection } from "@/services/ai/proxy";
import { useSettingsStore } from "@/services/settings/store";

export function AiProvidersSection() {
  const { t } = useTranslation("settings");
  const aiModel = useSettingsStore((s) => s.aiModel);
  const setAiModel = useSettingsStore((s) => s.setAiModel);
  const [configured, setConfigured] = useState(false);
  const [keyInput, setKeyInput] = useState("");
  const [busy, setBusy] = useState(false);

  const refresh = () => {
    void hasAiKey()
      .then(setConfigured)
      .catch(() => setConfigured(false));
  };
  useEffect(refresh, []);

  const onSave = async () => {
    if (!keyInput) return;
    setBusy(true);
    try {
      await setAiKey(keyInput);
      setKeyInput("");
      refresh();
    } catch {
      toast.error(t("test_fail", { message: "keychain" }));
    } finally {
      setBusy(false);
    }
  };

  const onClear = async () => {
    setBusy(true);
    try {
      await clearAiKey();
      refresh();
    } finally {
      setBusy(false);
    }
  };

  const onTest = async () => {
    setBusy(true);
    try {
      await testConnection();
      toast.success(t("test_ok"));
    } catch (e) {
      const message =
        (e as { data?: { message?: string } })?.data?.message ??
        (e as { code?: string })?.code ??
        "error";
      toast.error(t("test_fail", { message }));
    } finally {
      setBusy(false);
    }
  };

  return (
    <div className="space-y-4 text-sm">
      <p className="text-muted-foreground">{t("ai.byok_note")}</p>

      <div className="rounded border border-border p-3">
        <div className="mb-2 flex items-center gap-2">
          <span className="font-medium">{t("ai.provider.anthropic")}</span>
          <span
            className={cn(
              "rounded px-1.5 py-0.5 text-[10px]",
              configured
                ? "bg-primary/20 text-primary"
                : "bg-muted text-muted-foreground",
            )}
          >
            {configured ? t("ai.configured") : t("ai.not_configured")}
          </span>
        </div>

        <label className="mb-2 flex items-center gap-2">
          <span className="w-16 text-muted-foreground">{t("ai.model")}</span>
          <input
            value={aiModel}
            onChange={(e) => setAiModel(e.target.value)}
            className="flex-1 rounded border border-border bg-background/50 px-2 py-1 font-mono text-xs outline-none focus:border-primary"
          />
        </label>

        <label className="mb-2 flex items-center gap-2">
          <span className="w-16 text-muted-foreground">{t("ai.api_key")}</span>
          <input
            type="password"
            aria-label={t("ai.api_key")}
            value={keyInput}
            placeholder={configured ? "••••••••" : t("ai.key_placeholder")}
            onChange={(e) => setKeyInput(e.target.value)}
            className="flex-1 rounded border border-border bg-background/50 px-2 py-1 font-mono text-xs outline-none focus:border-primary"
          />
        </label>

        <div className="flex gap-2">
          <button
            type="button"
            disabled={busy || !keyInput}
            onClick={() => void onSave()}
            className="rounded bg-primary px-2 py-1 text-xs text-primary-foreground disabled:opacity-50"
          >
            {t("ai.save")}
          </button>
          <button
            type="button"
            disabled={busy || !configured}
            onClick={() => void onTest()}
            className="rounded border border-border px-2 py-1 text-xs hover:bg-muted disabled:opacity-50"
          >
            {t("ai.test")}
          </button>
          <button
            type="button"
            disabled={busy || !configured}
            onClick={() => void onClear()}
            className="rounded border border-border px-2 py-1 text-xs text-muted-foreground hover:bg-muted disabled:opacity-50"
          >
            {t("ai.clear")}
          </button>
        </div>
      </div>
    </div>
  );
}
```

> key 输入框 `type="password"` 且**从不**用已存的 key 回填（仅占位 `••••••••` 表示已配置）——符合 spec「不回显」。

- [ ] **步骤 5：运行确认通过 + 全套**

运行：`pnpm vitest run src/ui/settings/AiProvidersSection.test.tsx && pnpm lint && pnpm typecheck && pnpm test`
预期：PASS + lint 0 error + typecheck 0 + 前端全绿。

- [ ] **步骤 6：Commit**

```sh
git add src/ui/settings/AiProvidersSection.tsx src/ui/settings/AiProvidersSection.test.tsx \
        src/i18n/locales/en-US/settings.json src/i18n/locales/zh-CN/settings.json
git commit -m "feat(settings): AI providers section — Anthropic card (key/model/test/clear)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T9：验证 + 视觉验证 + PR + roadmap + merge

- [ ] **步骤 1：全套验证**

运行：`cargo test --manifest-path src-tauri/Cargo.toml && pnpm lint && pnpm typecheck && pnpm test`
预期：Rust 5 tests + 前端全绿 + lint 0 error + typecheck 0。

- [ ] **步骤 2：视觉 + 端到端验证（人工 `pnpm tauri dev`，需用户 Anthropic key）**

- [ ] 齿轮按钮（启动页 + 编辑器）打开 Settings Dialog，左 nav 见「AI providers」
- [ ] 填 model + API key → Save → 状态变「已配置」；输入框不回显原 key（重开仍只显示占位）
- [ ] 「测试连接」用真实 key → toast「连接正常」；用错 key → toast「连接失败 + message」
- [ ] 关闭重开应用 → 仍「已配置」（keychain 持久）；「清除」→ 变「未配置」
- [ ] devtools 网络/控制台 + 导出产物里**搜不到** key 明文
- [ ] 中英文文案

- [ ] **步骤 3：push + PR**

```sh
git push -u origin feat/v0.3a-ai-proxy
/opt/homebrew/bin/gh pr create --base main --head feat/v0.3a-ai-proxy \
  --title "feat(v0.3a): AI proxy + BYO-key + provider config" \
  --body "$(cat <<'EOF'
## What
v0.3 第一个 sub-stage（基础设施）：Rust AI proxy（Anthropic + provider 抽象）+ OS keychain 存 key + Settings Dialog「AI providers」配置。
- Rust `ai.rs`：纯函数 build/parse（cargo test）+ 命令 set/has/clear_ai_key + ai_complete(text/结构化 via tool-use) + test_ai_provider。
- key 经 keychain，renderer 无取回路径（has_ai_key 仅布尔）。
- 前端 `services/ai/proxy.ts` + Settings Dialog 壳（接预留 settingsOpen，按 prototype img_5）+ Anthropic 卡。

## Why
- Spec: \`docs/superpowers/specs/2026-06-05-v0.3a-ai-proxy-design.md\`
- Plan: \`docs/superpowers/plans/2026-06-05-v0.3a-ai-proxy-plan.md\`
- sub-stage B（Skill 框架 + scene-edit 加灯 + NL 输入）依赖本期。

## How to test
- [x] cargo test（Rust 纯函数）+ lint/typecheck/test 本地绿
- [ ] CI 绿
- [ ] 人工 pnpm tauri dev（需 Anthropic key，plan T9 步骤 2）

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **步骤 4：CI 绿 + 视觉验证后回填 roadmap + merge**

`docs/roadmap.md`：v0.3 节从「Planned」改「In progress（拆 2 sub-stage）」+ Sub-stages 列 `[x] A AI proxy+BYO-key+provider 配置（#NN）` / `[ ] B Skill 框架+scene-edit+NL UI`；表格行 v0.3 status 🟡 + #NN。commit + push。CI 绿 + 视觉验证通过 → `gh pr merge NN --merge --delete-branch`。

> **CI 注意**：现有 CI 为前端 `Lint·Typecheck·Test`，不跑 `cargo test`，也不 build Rust。本 PR 改了 Rust，但前端 CI 仍能过（bindings 已生成提交）。Rust 编译/测试靠本地 + release.yml（tag 时）。**keyring 在 release.yml 的 Ubuntu job 可能需 `apt-get install -y libdbus-1-dev`**——若 v0.3 发版时 Ubuntu 构建失败，加该依赖（参照 v0.1 macOS universal 修法的排查路径）。

---

## 收尾验收

满足 spec §2：Settings 配 Anthropic key（keychain 不回显）+ 测试连接调通 + `ai_complete`（text + 结构化）可用 + key 不泄漏 + Rust/前端测试绿 + 人工验证。完成后进入 **sub-stage B**（Skill 接口 + 注册表 + 执行循环 + scene-edit 加灯 tool + 自然语言输入 UI + 填 docs/skill-guide.md）。
