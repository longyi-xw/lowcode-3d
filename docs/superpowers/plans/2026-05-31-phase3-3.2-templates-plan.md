# Phase 3 · 3.2 项目模板系统 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法跟踪进度。

**目标：** New（StartupView 按钮 + File>New 菜单）打开统一模板选择 Dialog，提供 starter / single-cube / empty 三个工厂模板（starter 默认），点卡→脏检查→载入编辑器；撤掉 StartupView 占位 templates section。

**架构：** 工厂注册表 `PROJECT_TEMPLATES`（id→`create()`）；`useUIStore.newProjectOpen` 控制 Dialog 开关；`newProject()` 只开 Dialog，`createProjectFromTemplate(id)` 做 `exclusive`+`confirmDiscard`+`loadProjectIntoEditor`；`NewProjectDialog` 挂 `App.tsx` 根；新增 `project` i18n namespace。

**技术栈：** Zustand + React 18 + Vitest + @testing-library/react + shadcn `<Dialog>`（Radix）+ react-i18next + zod（`SceneProjectSchema`）+ lucide-react 图标。

**前置：** spec `docs/superpowers/specs/2026-05-31-phase3-3.2-templates-design.md`（commit fbcf632）。分支 `feat/phase3-3.2-templates`（从 main）。Node 20 默认，`git`/`pnpm` 直接用，`gh` 用 `/opt/homebrew/bin/gh` 全路径。

**起手：**

```sh
pnpm install
pnpm test          # 基线 307 通过
```

---

## 文件结构

**新增：**

- `src/services/scene/templates.ts` — `TemplateId` / `ProjectTemplate` 类型 + `PROJECT_TEMPLATES` 注册表 + `createSingleCubeProject` / `createEmptyProject` 工厂
- `src/services/scene/templates.test.ts`
- `src/services/project/actions.test.ts` — `createProjectFromTemplate` 行为测试
- `src/ui/project/NewProjectDialog.tsx` + `.test.tsx`
- `src/i18n/locales/en-US/project.json` + `zh-CN/project.json`

**修改：**

- `src/services/scene/demo-project.ts` — `export const TARGET`；删过时注释
- `src/services/ui/store.ts` + `.test.ts` — `newProjectOpen` / `setNewProjectOpen`
- `src/services/project/actions.ts` — `newProject()` 改开 picker；新增 `createProjectFromTemplate`；`loadProjectIntoEditor` 形参放宽到 `SceneProject`；删 `createDemoProject` import
- `src/i18n/config.ts` — `NAMESPACES` 加 `"project"`
- `src/i18n/index.ts` — import + resources 并入 project ns
- `src/ui/views/StartupView.tsx` — 删 templates section + 无用 import
- `src/i18n/locales/{en-US,zh-CN}/startup.json` — 删孤立 `templates` 块
- `src/App.tsx` — 挂 `<NewProjectDialog />`
- `docs/roadmap.md` — PR 后回填 3.2 勾选

---

## 任务 T1：模板注册表 + 工厂

**文件：** 创建 `src/services/scene/templates.ts` + `.test.ts`；修改 `src/services/scene/demo-project.ts`

- [ ] **步骤 1：先导出 TARGET（demo-project.ts）**

`src/services/scene/demo-project.ts` 把 `const TARGET` 改为 `export const TARGET`，并把 JSDoc 里 "Used as the temporary \"New project\" payload until a real template picker lands." 这句删掉（改为 "Backs the \"Starter scene\" template (id `starter`) in the New-project picker."）。

- [ ] **步骤 2：写失败测试 `templates.test.ts`**

```ts
import { readFileSync } from "node:fs";
import { dirname, resolve } from "node:path";
import { fileURLToPath } from "node:url";
import { describe, expect, it } from "vitest";

import { SceneProjectSchema } from "@/core/scene/schemas";

import { PROJECT_TEMPLATES, createSingleCubeProject } from "./templates";

const __dirname = dirname(fileURLToPath(import.meta.url));
const examplesDir = resolve(__dirname, "../../../examples");

describe("PROJECT_TEMPLATES", () => {
  it("lists starter, single-cube, empty in order", () => {
    expect(PROJECT_TEMPLATES.map((t) => t.id)).toEqual([
      "starter",
      "single-cube",
      "empty",
    ]);
  });

  it("marks only starter as recommended", () => {
    expect(PROJECT_TEMPLATES.filter((t) => t.recommended).map((t) => t.id)).toEqual([
      "starter",
    ]);
  });

  it("every create() passes SceneProjectSchema", () => {
    for (const t of PROJECT_TEMPLATES) {
      expect(() => SceneProjectSchema.parse(t.create())).not.toThrow();
    }
  });

  it("create() yields a fresh project id each call", () => {
    for (const t of PROJECT_TEMPLATES) {
      expect(t.create().metadata.id).not.toBe(t.create().metadata.id);
    }
  });

  it("each template has templates.* i18n keys + an icon", () => {
    for (const t of PROJECT_TEMPLATES) {
      expect(t.nameKey).toMatch(/^templates\./);
      expect(t.descriptionKey).toMatch(/^templates\./);
      expect(t.icon).toBeTruthy();
    }
  });
});

describe("createSingleCubeProject", () => {
  it("matches examples/single-cube/project.json (ignoring volatile metadata)", () => {
    const raw = JSON.parse(
      readFileSync(resolve(examplesDir, "single-cube/project.json"), "utf-8"),
    );
    const normalize = (p: {
      metadata: Record<string, unknown> & { target_runtime: Record<string, unknown> };
    }) => ({
      ...p,
      metadata: {
        ...p.metadata,
        id: "X",
        name: "X",
        created_at: "X",
        updated_at: "X",
        target_runtime: { ...p.metadata.target_runtime, version: "X" },
      },
    });
    expect(normalize(createSingleCubeProject())).toEqual(normalize(raw));
  });
});
```

- [ ] **步骤 3：跑确认失败**

```sh
pnpm vitest run src/services/scene/templates.test.ts
```

预期：FAIL（找不到 `./templates`）。

- [ ] **步骤 4：实现 `templates.ts`**

```ts
import { Box, LayoutTemplate, Square } from "lucide-react";
import type { LucideIcon } from "lucide-react";

import { generateUUID } from "@/core/id/uuid";
import { createDefaultProject } from "@/core/scene/defaults";
import type { SceneNode, SceneProject } from "@/core/scene/types";

import { TARGET, createDemoProject } from "./demo-project";

export type TemplateId = "starter" | "single-cube" | "empty";

export interface ProjectTemplate {
  id: TemplateId;
  /** i18n key under the "project" namespace. */
  nameKey: string;
  descriptionKey: string;
  icon: LucideIcon;
  recommended?: boolean;
  /** Always returns a fresh project (new metadata.id + timestamps). */
  create: (name?: string) => SceneProject;
}

const IDENTITY = {
  position: [0, 0, 0] as [number, number, number],
  rotation: [0, 0, 0, 1] as [number, number, number, number],
  scale: [1, 1, 1] as [number, number, number],
};

/** Blank scene — thin wrapper over createDefaultProject with a fresh id. */
export function createEmptyProject(name = "Untitled project"): SceneProject {
  return createDefaultProject({ id: generateUUID(), name, target: TARGET });
}

/**
 * One cube (builtin primitive) + one directional light + one perspective
 * camera. Mirrors examples/single-cube/project.json structurally; the
 * drift-guard test keeps them in sync. Fresh id/timestamps + current TARGET.
 */
export function createSingleCubeProject(name = "Untitled project"): SceneProject {
  const base = createDefaultProject({ id: generateUUID(), name, target: TARGET });
  const nodes: Record<string, SceneNode> = {
    "cube-1": {
      id: "cube-1",
      name: "Cube",
      type: "mesh",
      transform: IDENTITY,
      parent_id: null,
      children_ids: [],
      visible: true,
      locked: false,
      data: { type: "mesh", asset_id: "asset-cube" },
      behaviors: [],
      user_data: {},
    },
    "key-light": {
      id: "key-light",
      name: "Key Light",
      type: "light",
      transform: { ...IDENTITY, position: [3, 5, 3] },
      parent_id: null,
      children_ids: [],
      visible: true,
      locked: false,
      data: {
        type: "light",
        light_kind: "directional",
        color: "#ffffff",
        intensity: 1.2,
        cast_shadow: true,
      },
      behaviors: [],
      user_data: {},
    },
    "main-camera": {
      id: "main-camera",
      name: "Main Camera",
      type: "camera",
      transform: { ...IDENTITY, position: [4, 3, 4] },
      parent_id: null,
      children_ids: [],
      visible: true,
      locked: false,
      data: {
        type: "camera",
        camera_kind: "perspective",
        fov: 50,
        near: 0.1,
        far: 1000,
      },
      behaviors: [],
      user_data: {},
    },
  };
  return {
    ...base,
    scene: { nodes, root_node_ids: ["cube-1", "key-light", "main-camera"] },
    assets: [
      {
        id: "asset-cube",
        content_hash: "sha256-cube-placeholder",
        kind: "geometry",
        relative_path: "assets/cube.glb",
        tags: ["primitive", "cube"],
        description: "1m unit cube used as scaffolding primitive",
        source: { kind: "builtin", library_id: "primitives" },
      },
    ],
    settings: { ...base.settings, background: { kind: "color", color: "#101418" } },
  };
}

export const PROJECT_TEMPLATES: ProjectTemplate[] = [
  {
    id: "starter",
    nameKey: "templates.starter.name",
    descriptionKey: "templates.starter.description",
    icon: LayoutTemplate,
    recommended: true,
    create: createDemoProject,
  },
  {
    id: "single-cube",
    nameKey: "templates.single_cube.name",
    descriptionKey: "templates.single_cube.description",
    icon: Box,
    create: createSingleCubeProject,
  },
  {
    id: "empty",
    nameKey: "templates.empty.name",
    descriptionKey: "templates.empty.description",
    icon: Square,
    create: createEmptyProject,
  },
];
```

- [ ] **步骤 5：跑确认通过**

```sh
pnpm vitest run src/services/scene/templates.test.ts
```

预期：PASS（6 个用例）。如 drift 测试挂，对照 `examples/single-cube/project.json` 逐字段核（version/id/name/时间戳已被 normalize 忽略）。

- [ ] **步骤 6：Commit**

```sh
git add src/services/scene/templates.ts src/services/scene/templates.test.ts \
        src/services/scene/demo-project.ts
git commit -m "feat(templates): PROJECT_TEMPLATES registry + single-cube/empty factories

工厂注册表映射 id→create()：starter=createDemoProject(recommended)、
single-cube=新增 createSingleCubeProject(忠实镜像 examples/single-cube
+ 防漂移测试)、empty=createEmptyProject(包 createDefaultProject)。
导出 demo-project TARGET 供三个工厂共用当前 runtime 版本。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T2：useUIStore.newProjectOpen

**文件：** 修改 `src/services/ui/store.ts` + `.test.ts`

- [ ] **步骤 1：写失败测试（store.test.ts 末尾追加）**

```ts
describe("useUIStore — newProjectOpen (3.2)", () => {
  beforeEach(() => useUIStore.setState({ newProjectOpen: false }));

  it("defaults to false", () => {
    expect(useUIStore.getState().newProjectOpen).toBe(false);
  });

  it("setNewProjectOpen flips it", () => {
    useUIStore.getState().setNewProjectOpen(true);
    expect(useUIStore.getState().newProjectOpen).toBe(true);
  });
});
```

- [ ] **步骤 2：跑确认失败**

```sh
pnpm vitest run src/services/ui/store.test.ts
```

预期：FAIL（`newProjectOpen` undefined / `setNewProjectOpen` not a function）。

- [ ] **步骤 3：实现（store.ts）**

`UIState` interface 内（紧跟 `setHelpOpen` 之后）加：

```ts
/** Whether the New-project template picker dialog is visible. */
newProjectOpen: boolean;
setNewProjectOpen: (open: boolean) => void;
```

`create<UIState>((set) => ({ ... }))` body 内（紧跟 `setHelpOpen` 之后）加：

```ts
newProjectOpen: false,
setNewProjectOpen: (newProjectOpen) => set({ newProjectOpen }),
```

- [ ] **步骤 4：跑确认通过**

```sh
pnpm vitest run src/services/ui/store.test.ts
```

预期：PASS。

- [ ] **步骤 5：Commit**

```sh
git add src/services/ui/store.ts src/services/ui/store.test.ts
git commit -m "feat(ui-store): newProjectOpen flag for the template picker

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T3：actions.ts 重构（newProject 开 picker + createProjectFromTemplate）

**文件：** 修改 `src/services/project/actions.ts`；创建 `src/services/project/actions.test.ts`

- [ ] **步骤 1：写失败测试 `actions.test.ts`**

```ts
import { beforeEach, describe, expect, it, vi } from "vitest";

vi.mock("@tauri-apps/plugin-dialog", () => ({
  ask: vi.fn(),
  open: vi.fn(),
  save: vi.fn(),
}));

import { useAppViewStore } from "@/services/app-view/store";
import { useSceneStore } from "@/services/scene/store";
import { useUIStore } from "@/services/ui/store";

import { createProjectFromTemplate, newProject } from "./actions";
import { useProjectStore } from "./store";

describe("newProject", () => {
  it("opens the template picker (non-destructive)", () => {
    useUIStore.setState({ newProjectOpen: false });
    useSceneStore.setState({ project: null });
    newProject();
    expect(useUIStore.getState().newProjectOpen).toBe(true);
    expect(useSceneStore.getState().project).toBeNull(); // nothing loaded yet
  });
});

describe("createProjectFromTemplate", () => {
  beforeEach(() => {
    useProjectStore.getState().markClean(); // not dirty → confirmDiscard auto-true
    useSceneStore.setState({ project: null });
    useUIStore.setState({ newProjectOpen: true });
  });

  it("loads the single-cube template and closes the dialog", async () => {
    await createProjectFromTemplate("single-cube");
    const project = useSceneStore.getState().project;
    expect(project).not.toBeNull();
    expect(Object.keys(project!.scene.nodes).sort()).toEqual([
      "cube-1",
      "key-light",
      "main-camera",
    ]);
    expect(useUIStore.getState().newProjectOpen).toBe(false);
    expect(useAppViewStore.getState().view).toBe("editor");
  });

  it("loads the empty template with no nodes", async () => {
    await createProjectFromTemplate("empty");
    expect(useSceneStore.getState().project!.scene.nodes).toEqual({});
    expect(useUIStore.getState().newProjectOpen).toBe(false);
  });

  it("ignores an unknown template id", async () => {
    await createProjectFromTemplate("nope" as never);
    expect(useSceneStore.getState().project).toBeNull();
  });
});
```

- [ ] **步骤 2：跑确认失败**

```sh
pnpm vitest run src/services/project/actions.test.ts
```

预期：FAIL（`createProjectFromTemplate` 未导出 / `newProject` 仍是旧行为载入 demo）。

- [ ] **步骤 3：改 `actions.ts`**

(a) 顶部 import 区：删 `import { createDemoProject } from "@/services/scene/demo-project";`，加：

```ts
import { PROJECT_TEMPLATES, type TemplateId } from "@/services/scene/templates";
```

(b) 替换 `newProject`（原 `export async function newProject(): Promise<void> { … }` 整块）为：

```ts
/** Opens the template picker. Non-destructive — actual load happens in
 *  createProjectFromTemplate once the user picks a template. */
export function newProject(): void {
  useUIStore.getState().setNewProjectOpen(true);
}

/** Loads a fresh project from the chosen template (guarded by the dirty
 *  check), then closes the picker. */
export async function createProjectFromTemplate(id: TemplateId): Promise<void> {
  await exclusive("newProject", async () => {
    const tpl = PROJECT_TEMPLATES.find((t) => t.id === id);
    if (!tpl) return;
    if (!(await confirmDiscard("Start a new project and discard current changes?"))) {
      return;
    }
    loadProjectIntoEditor(tpl.create(), null);
    useUIStore.getState().setNewProjectOpen(false);
  });
}
```

(c) `loadProjectIntoEditor` 形参类型 `project: ReturnType<typeof createDemoProject>` → `project: SceneProject`（`SceneProject` 已在顶部 import）。

- [ ] **步骤 4：跑确认通过**

```sh
pnpm vitest run src/services/project/actions.test.ts
```

预期：PASS（4 用例）。

- [ ] **步骤 5：Commit**

```sh
git add src/services/project/actions.ts src/services/project/actions.test.ts
git commit -m "refactor(actions): newProject opens picker; createProjectFromTemplate loads

newProject() 现在只开模板 picker(非破坏)；createProjectFromTemplate(id)
承接破坏性动作：exclusive + confirmDiscard(脏检查) + loadProjectIntoEditor
+ 关闭 picker。loadProjectIntoEditor 形参收敛到 SceneProject。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T4：NewProjectDialog + project i18n namespace

**文件：** 创建 `src/i18n/locales/{en-US,zh-CN}/project.json`、`src/ui/project/NewProjectDialog.tsx` + `.test.tsx`；修改 `src/i18n/config.ts`、`src/i18n/index.ts`

- [ ] **步骤 1：建两个 locale 文件**

`src/i18n/locales/en-US/project.json`：

```json
{
  "new_dialog": {
    "title": "New project",
    "description": "Choose a starting point for your scene."
  },
  "templates": {
    "starter": {
      "name": "Starter scene",
      "description": "Grid, a cube in a group, key + ambient lights. Good for jumping in."
    },
    "single_cube": {
      "name": "Single cube",
      "description": "One cube, a directional light, and a camera."
    },
    "empty": {
      "name": "Empty project",
      "description": "A blank scene. Build from scratch."
    }
  },
  "recommended_badge": "Recommended"
}
```

`src/i18n/locales/zh-CN/project.json`：

```json
{
  "new_dialog": {
    "title": "新建项目",
    "description": "为你的场景选择一个起点。"
  },
  "templates": {
    "starter": {
      "name": "起步场景",
      "description": "网格、组内一个立方体、主光 + 环境光。适合直接上手。"
    },
    "single_cube": {
      "name": "单个立方体",
      "description": "一个立方体、一束方向光、一个相机。"
    },
    "empty": {
      "name": "空项目",
      "description": "空白场景，从零开始搭。"
    }
  },
  "recommended_badge": "推荐"
}
```

- [ ] **步骤 2：注册 namespace**

`src/i18n/config.ts` 的 `NAMESPACES` 数组加 `"project"`（放 `"errors"` 之后）。

`src/i18n/index.ts`：import 区加

```ts
import enProject from "./locales/en-US/project.json";
import zhProject from "./locales/zh-CN/project.json";
```

`resources["en-US"]` 加 `project: enProject,`；`resources["zh-CN"]` 加 `project: zhProject,`。

- [ ] **步骤 3：写失败测试 `NewProjectDialog.test.tsx`**

```tsx
import { fireEvent, render, screen } from "@testing-library/react";
import { beforeEach, describe, expect, it, vi } from "vitest";

vi.mock("@/services/project/actions", () => ({
  createProjectFromTemplate: vi.fn(),
}));

import { createProjectFromTemplate } from "@/services/project/actions";
import { useUIStore } from "@/services/ui/store";

import { NewProjectDialog } from "./NewProjectDialog";

describe("NewProjectDialog", () => {
  beforeEach(() => {
    vi.clearAllMocks();
    useUIStore.setState({ newProjectOpen: false });
  });

  it("renders nothing when closed", () => {
    render(<NewProjectDialog />);
    expect(screen.queryByText(/New project|新建项目/)).toBeNull();
  });

  it("renders all three template cards + recommended badge when open", () => {
    useUIStore.setState({ newProjectOpen: true });
    render(<NewProjectDialog />);
    expect(screen.getByText(/Starter scene|起步场景/)).toBeInTheDocument();
    expect(screen.getByText(/Single cube|单个立方体/)).toBeInTheDocument();
    expect(screen.getByText(/Empty project|空项目/)).toBeInTheDocument();
    expect(screen.getByText(/Recommended|推荐/)).toBeInTheDocument();
  });

  it("clicking a card calls createProjectFromTemplate with its id", () => {
    useUIStore.setState({ newProjectOpen: true });
    render(<NewProjectDialog />);
    fireEvent.click(screen.getByRole("button", { name: /Single cube|单个立方体/ }));
    expect(createProjectFromTemplate).toHaveBeenCalledWith("single-cube");
  });
});
```

- [ ] **步骤 4：跑确认失败**

```sh
pnpm vitest run src/ui/project/NewProjectDialog.test.tsx
```

预期：FAIL（找不到组件）。

- [ ] **步骤 5：实现 `NewProjectDialog.tsx`**

```tsx
import { useTranslation } from "react-i18next";

import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogHeader,
  DialogTitle,
} from "@/components/ui/dialog";
import { createProjectFromTemplate } from "@/services/project/actions";
import { PROJECT_TEMPLATES } from "@/services/scene/templates";
import { useUIStore } from "@/services/ui/store";

export function NewProjectDialog() {
  const { t } = useTranslation("project");
  const open = useUIStore((s) => s.newProjectOpen);
  const setOpen = useUIStore((s) => s.setNewProjectOpen);

  return (
    <Dialog open={open} onOpenChange={setOpen}>
      <DialogContent className="max-w-[560px]">
        <DialogHeader>
          <DialogTitle>{t("new_dialog.title")}</DialogTitle>
          <DialogDescription>{t("new_dialog.description")}</DialogDescription>
        </DialogHeader>
        <ul className="grid gap-2">
          {PROJECT_TEMPLATES.map((tpl) => {
            const Icon = tpl.icon;
            return (
              <li key={tpl.id}>
                <button
                  type="button"
                  onClick={() => void createProjectFromTemplate(tpl.id)}
                  className="flex w-full items-center gap-3 rounded-md border border-border p-3 text-left transition hover:bg-muted"
                >
                  <Icon className="h-5 w-5 shrink-0 text-muted-foreground" />
                  <span className="flex-1">
                    <span className="flex items-center gap-2 text-sm font-medium text-foreground">
                      {t(tpl.nameKey)}
                      {tpl.recommended && (
                        <span className="rounded bg-primary/10 px-1.5 py-0.5 font-mono text-[9px] uppercase tracking-wider text-primary">
                          {t("recommended_badge")}
                        </span>
                      )}
                    </span>
                    <span className="mt-0.5 block text-xs text-muted-foreground">
                      {t(tpl.descriptionKey)}
                    </span>
                  </span>
                </button>
              </li>
            );
          })}
        </ul>
      </DialogContent>
    </Dialog>
  );
}
```

- [ ] **步骤 6：跑确认通过**

```sh
pnpm vitest run src/ui/project/NewProjectDialog.test.tsx
```

预期：PASS（3 用例）。

- [ ] **步骤 7：Commit**

```sh
git add src/ui/project/NewProjectDialog.tsx src/ui/project/NewProjectDialog.test.tsx \
        src/i18n/config.ts src/i18n/index.ts \
        src/i18n/locales/en-US/project.json src/i18n/locales/zh-CN/project.json
git commit -m "feat(ui): NewProjectDialog + project i18n namespace

shadcn Dialog 读 useUIStore.newProjectOpen，渲染 PROJECT_TEMPLATES 三张卡
(icon+名+描述，starter 带 Recommended 角标)，点卡→createProjectFromTemplate。
新增 project i18n namespace（en/zh）。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T5：StartupView 删占位 section + App.tsx 挂 Dialog

**文件：** 修改 `src/ui/views/StartupView.tsx`、`src/App.tsx`、`src/i18n/locales/{en-US,zh-CN}/startup.json`；创建 `src/ui/views/StartupView.test.tsx`

- [ ] **步骤 1：写失败测试 `StartupView.test.tsx`**

```tsx
import { render, screen } from "@testing-library/react";
import { describe, expect, it } from "vitest";

import { StartupView } from "./StartupView";

describe("StartupView", () => {
  it("shows New project and no runtime-template placeholder", () => {
    render(<StartupView />);
    expect(screen.getByText(/New project|新建项目/)).toBeInTheDocument();
    expect(screen.queryByText(/Three\.js \+ Vite|R3F \+ Next/)).toBeNull();
  });
});
```

- [ ] **步骤 2：跑确认失败**

```sh
pnpm vitest run src/ui/views/StartupView.test.tsx
```

预期：FAIL（当前仍渲染 three_vite / r3f_next 标签 → `queryByText` 命中非 null）。

- [ ] **步骤 3：改 `StartupView.tsx`**

删掉第二个 `<section>`（`templates.heading` + 两个 `RailAction`：three_vite / r3f_next）整块（现状约 124–137 行）。第一个 section（New/Open/Clone/Import）与其余不动。

顶部 import 里删掉 `LayoutTemplate` 和 `GitBranch`（仅被删掉的 section 使用；`FileBox`/`FilePlus`/`FolderOpen`/`Link2`/主题图标仍在用）。`createDemoProject` import 保留（`open()` helper 的 clone/import 分支仍用）。

- [ ] **步骤 4：删孤立 i18n 键**

`src/i18n/locales/en-US/startup.json` 与 `zh-CN/startup.json`：删整个 `"templates": { "heading": …, "three_vite": …, "r3f_next": …, "soon_badge": … }` 块。其余键不动。

- [ ] **步骤 5：App.tsx 挂 Dialog**

`src/App.tsx` import 区加：

```tsx
import { NewProjectDialog } from "@/ui/project/NewProjectDialog";
```

在 `return (<> … </>)` 内、四个 view 之后加一行（与 view 平级）：

```tsx
<NewProjectDialog />
```

- [ ] **步骤 6：跑确认通过 + 全套回归**

```sh
pnpm vitest run src/ui/views/StartupView.test.tsx
pnpm lint && pnpm typecheck && pnpm test
```

预期：StartupView 测试 PASS；全套绿（含新增 ~15 用例）。

- [ ] **步骤 7：Commit**

```sh
git add src/ui/views/StartupView.tsx src/App.tsx \
        src/i18n/locales/en-US/startup.json src/i18n/locales/zh-CN/startup.json \
        src/ui/views/StartupView.test.tsx
git commit -m "feat(ui): wire NewProjectDialog at App root; drop placeholder templates section

App.tsx 挂 <NewProjectDialog />（覆盖 startup+editor）。StartupView 删掉
three_vite/r3f_next 占位 templates section + 无用 import + 孤立 i18n 键。
New 按钮(及 File>New)现在打开模板 picker。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T6：验证 + 视觉验证 + PR + roadmap 回填

**文件：** 修改 `docs/roadmap.md`（PR 后）

- [ ] **步骤 1：全套验证**

```sh
pnpm lint
pnpm typecheck
pnpm test
pnpm format:check   # 已知会报未跟踪的 CLAUDE.md / tests/ —— 范围外噪声，忽略；新代码须自身干净
```

预期：lint/typecheck/test 全绿。

- [ ] **步骤 2：视觉验证（人工 `pnpm tauri dev`，按 spec §11）**

- [ ] StartupView 不再有 three_vite/r3f templates section
- [ ] New project → 弹模板 Dialog，3 卡，starter 带「推荐」角标
- [ ] 点 starter → 编辑器场景 = grid + Models 组 + cube + 主光 + 环境光
- [ ] 再 New → single-cube → cube + 方向光 + Main Camera 节点，无 grid
- [ ] 再 New → empty → 空场景，可正常加节点
- [ ] 编辑器内改动(dirty) → File>New(⌘N) → Dialog → 点模板 → 原生 discard 确认 → 确认载入 / 取消停留
- [ ] 中英文切换 Dialog 文案正确
- [ ] 连续两次同模板新建 = 两个独立项目

- [ ] **步骤 3：push + 开 PR**

```sh
git push -u origin feat/phase3-3.2-templates
/opt/homebrew/bin/gh pr create --base main --head feat/phase3-3.2-templates \
  --title "feat(phase3-3.2): project template picker (New-project dialog)" \
  --body "$(cat <<'EOF'
## What

Phase 3 · 3.2 项目模板系统。New（StartupView 按钮 + File>New ⌘N）打开统一模板选择 Dialog，3 个工厂模板：

- **Starter scene**（默认/推荐）= 原 demo（grid + Models 组 + cube + key/ambient 光）
- **Single cube** = cube + directional light + camera（忠实镜像 examples/single-cube + 防漂移测试）
- **Empty project** = 空场景（createDefaultProject）

点卡 → 脏检查（dirty 弹原生 confirm）→ loadProjectIntoEditor → 关闭 Dialog。每次新建都是 fresh project id。撤掉 StartupView 的 three_vite/r3f 占位 templates section。

新增：`PROJECT_TEMPLATES` 工厂注册表、`useUIStore.newProjectOpen`、`createProjectFromTemplate` action、`NewProjectDialog`(挂 App 根)、`project` i18n namespace。

## Why

- Spec: \`docs/superpowers/specs/2026-05-31-phase3-3.2-templates-design.md\`
- Plan: \`docs/superpowers/plans/2026-05-31-phase3-3.2-templates-plan.md\`
- Roadmap: \`docs/roadmap.md\` v0.1 Phase 3 · 3.2

## How to test

- [x] \`pnpm lint\` / \`pnpm typecheck\` / \`pnpm test\`（本地绿，新增 ~15 测试）
- [ ] CI 全绿
- [ ] 人工 \`pnpm tauri dev\` 视觉验证（plan T6 步骤 2）

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **步骤 4：CI 绿后回填 roadmap + merge**

`docs/roadmap.md` line 63 `- [ ] 3.2 项目模板系统（…）` → `- [x] … （[#NN](https://github.com/longyi-xw/lowcode-3d/pull/NN)）`，commit `docs(roadmap): mark 3.2 templates shipped (#NN)` 进分支 push（同 3.1 套路，可顺手修 line 65 的 3.4 漏勾）。CI 绿 + 视觉验证通过后 `gh pr merge NN --merge --delete-branch`。

---

## 收尾验收

满足 spec §1 成功标准：

1. ✅ 两入口共用模板 Dialog
2. ✅ 3 模板 starter(默认)/single-cube/empty
3. ✅ 点卡 → 脏检查 → 载入 → 关 Dialog
4. ✅ fresh project id 每次
5. ✅ 删占位 templates section
6. ✅ lint/typecheck/test 绿 + 视觉验证

下一步：merge → 选下一 sub-stage（3.3 错误处理 / 3.5 发布）。
