# v0.2 材质编辑 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 让属性面板能编辑 mesh 节点的 6 个 PBR 材质参数（baseColor / metalness / roughness / emissive / emissive_intensity / opacity），编辑可撤销/重做，导出代码含正确材质字段。

**架构：** 新增 `material.ts`（`DEFAULT_MESH_MATERIAL` + `resolveMaterial` 纯函数）作为面板/runtime/codegen 三处共用的"默认⊕override→有效值"单一来源；`SetMaterialOverrideCommand` + `setMeshMaterial` store mutator 走既有命令/撤销栈；`MaterialSection` 面板 + `SliderRow` 控件挂进 `NodeProperties`；`emitMesh` 读 override emit 材质。

**技术栈：** TypeScript + zod + Three.js + Zustand + React + react-i18next + Vitest + @testing-library/react。

**前置：** spec `docs/superpowers/specs/2026-06-03-v0.2-material-editing-design.md`。分支 `feat/v0.2-material-editing`（spec + roadmap backlog 已 commit）。基线 `pnpm test` 373 绿。

---

## 文件结构

**新增：**

- `src/core/scene/material.ts` — `DEFAULT_MESH_MATERIAL`、`ResolvedMaterial`、`MaterialOverride` 类型、`resolveMaterial(override?)`。单一职责：材质默认值 + 解析。
- `src/core/scene/material.test.ts`
- `src/core/command/commands/set-material-override.ts` — 材质 override 命令。
- `src/core/command/commands/set-material-override.test.ts`
- `src/ui/editor/SliderRow.tsx` — range + 数值框组合控件。
- `src/ui/editor/SliderRow.test.tsx`
- `src/ui/editor/MaterialSection.tsx` — 材质编辑面板区。
- `src/ui/editor/MaterialSection.test.tsx`

**修改：**

- `src/core/command/types.ts` — `SceneEditorStore.setMeshMaterial`
- `src/core/command/commands/_test-utils.ts` — Call 联合 + FakeEditor.setMeshMaterial
- `src/core/command/commands/set-node-transform.test.ts` + `src/services/command-history/store.test.ts` — 手写 mock 补 setMeshMaterial
- `src/services/scene/store.ts` — `setMeshMaterial` impl + `getSceneEditorStore` 映射
- `src/runtime/three/node-builders/mesh.ts`（+ `mesh.test.ts`）— 用 `resolveMaterial` 全量设材质
- `src/runtime/three/export/scene-codegen.ts`（+ `scene-codegen.test.ts`）— `emitMesh` 用 `resolveMaterial`
- `src/i18n/locales/{en-US,zh-CN}/editor.json` — `properties.material.*`
- `src/ui/views/EditorView.tsx` — `NodeProperties` 内挂 `MaterialSection`
- `docs/roadmap.md`（T9）— v0.2 材质 sub-stage 勾选

---

## 任务 T1：material.ts — 默认材质 + resolveMaterial 纯函数

**文件：** 创建 `src/core/scene/material.ts` + `material.test.ts`

- [ ] **步骤 1：写失败测试 `material.test.ts`**

```ts
import { describe, expect, it } from "vitest";

import { DEFAULT_MESH_MATERIAL, resolveMaterial } from "./material";

describe("resolveMaterial", () => {
  it("returns the default material when override is undefined", () => {
    expect(resolveMaterial(undefined)).toEqual(DEFAULT_MESH_MATERIAL);
  });

  it("returns the default material for an override with only a slot", () => {
    expect(resolveMaterial({ slot: 0 })).toEqual(DEFAULT_MESH_MATERIAL);
  });

  it("overrides only the fields present, keeping defaults for the rest", () => {
    const resolved = resolveMaterial({
      slot: 0,
      color: "#ff0000",
      metalness: 0.8,
    });
    expect(resolved.color).toBe("#ff0000");
    expect(resolved.metalness).toBe(0.8);
    // untouched → defaults
    expect(resolved.roughness).toBe(DEFAULT_MESH_MATERIAL.roughness);
    expect(resolved.emissive).toBe(DEFAULT_MESH_MATERIAL.emissive);
    expect(resolved.emissive_intensity).toBe(DEFAULT_MESH_MATERIAL.emissive_intensity);
    expect(resolved.opacity).toBe(DEFAULT_MESH_MATERIAL.opacity);
  });

  it("keeps an explicit 0 (does not fall back to default)", () => {
    // opacity 0 is meaningful — must not be treated as missing
    expect(resolveMaterial({ slot: 0, opacity: 0 }).opacity).toBe(0);
  });
});
```

- [ ] **步骤 2：跑确认失败**

运行：`pnpm vitest run src/core/scene/material.test.ts`
预期：FAIL — `Failed to resolve import "./material"`。

- [ ] **步骤 3：实现 `material.ts`**

```ts
import type { NodeData } from "./types";

export type MaterialOverride = NonNullable<
  Extract<NodeData, { type: "mesh" }>["material_overrides"]
>[number];

export interface ResolvedMaterial {
  color: string; // hex #rrggbb
  metalness: number;
  roughness: number;
  emissive: string; // hex #rrggbb
  emissive_intensity: number;
  opacity: number;
}

/** Single source of truth for a mesh's default material — previously hard-coded
 *  in mesh.ts (builder) and emitMesh (codegen). */
export const DEFAULT_MESH_MATERIAL: ResolvedMaterial = {
  color: "#cccccc",
  metalness: 0,
  roughness: 0.7,
  emissive: "#000000",
  emissive_intensity: 1,
  opacity: 1,
};

/** Default ⊕ override → fully-resolved material. Used by the panel (display),
 *  the runtime builder (render) and codegen (export) so all three agree.
 *  Per-field `??` (not spread) because optional override fields are
 *  `undefined` and would otherwise clobber the defaults. */
export function resolveMaterial(override?: MaterialOverride): ResolvedMaterial {
  return {
    color: override?.color ?? DEFAULT_MESH_MATERIAL.color,
    metalness: override?.metalness ?? DEFAULT_MESH_MATERIAL.metalness,
    roughness: override?.roughness ?? DEFAULT_MESH_MATERIAL.roughness,
    emissive: override?.emissive ?? DEFAULT_MESH_MATERIAL.emissive,
    emissive_intensity:
      override?.emissive_intensity ?? DEFAULT_MESH_MATERIAL.emissive_intensity,
    opacity: override?.opacity ?? DEFAULT_MESH_MATERIAL.opacity,
  };
}
```

> 注：`?? ` 对 `opacity: 0` 正确（`0 ?? d` = `0`）。`MaterialOverride` 在此导出，T2/T3/T5/T8 复用。

- [ ] **步骤 4：跑确认通过**

运行：`pnpm vitest run src/core/scene/material.test.ts`
预期：PASS（4）。

- [ ] **步骤 5：Commit**

```sh
git add src/core/scene/material.ts src/core/scene/material.test.ts
git commit -m "feat(scene): material.ts — DEFAULT_MESH_MATERIAL + resolveMaterial

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T2：store setMeshMaterial mutator（接口 + 适配 + mock + impl）

**文件：** 修改 `src/core/command/types.ts`、`src/services/scene/store.ts`(+`store.test.ts`)、`src/core/command/commands/_test-utils.ts`、`src/core/command/commands/set-node-transform.test.ts`、`src/services/command-history/store.test.ts`

> ⚠ 加 `SceneEditorStore` 方法必须同步 5 处，否则 typecheck 红（AddNodeCommand 经验）：①接口 ②getSceneEditorStore 映射 ③_test-utils FakeEditor ④两处手写 mock。

- [ ] **步骤 1：写失败测试（store.test.ts 追加）**

在 `src/services/scene/store.test.ts` 末尾加（沿用其 seed 风格——确认文件顶部已有 `useSceneStore` import 与建 project 的 helper；若 helper 名不同照抄现有用例的建 project 方式）：

```ts
describe("useSceneStore.setMeshMaterial", () => {
  function seedMesh() {
    useSceneStore.setState({
      project: {
        spec_version: "0.1.0",
        metadata: {
          id: "p",
          name: "t",
          created_at: "",
          updated_at: "",
          target_runtime: {
            kind: "three.js",
            version: "0.184.0",
            module_format: "esm",
          },
        },
        scene: {
          root_node_ids: ["m"],
          nodes: {
            m: {
              id: "m",
              name: "M",
              type: "mesh",
              transform: {
                position: [0, 0, 0],
                rotation: [0, 0, 0, 1],
                scale: [1, 1, 1],
              },
              parent_id: null,
              children_ids: [],
              visible: true,
              locked: false,
              data: { type: "mesh", geometry: { kind: "box" } },
              behaviors: [],
              user_data: {},
            },
          },
        },
        assets: [],
        settings: {
          units: "meters",
          up_axis: "y",
          background: { kind: "color", color: "#000" },
        },
      },
    });
  }

  it("writes material_overrides[0] on a mesh node", () => {
    seedMesh();
    useSceneStore.getState().setMeshMaterial("m", { slot: 0, color: "#ff0000" });
    const node = useSceneStore.getState().getNode("m");
    expect(node?.data).toMatchObject({
      type: "mesh",
      material_overrides: [{ slot: 0, color: "#ff0000" }],
    });
  });

  it("clears material_overrides when override is undefined", () => {
    seedMesh();
    useSceneStore.getState().setMeshMaterial("m", { slot: 0, color: "#ff0000" });
    useSceneStore.getState().setMeshMaterial("m", undefined);
    expect(
      (useSceneStore.getState().getNode("m")?.data as { material_overrides?: unknown })
        .material_overrides,
    ).toBeUndefined();
  });
});
```

- [ ] **步骤 2：跑确认失败**

运行：`pnpm vitest run src/services/scene/store.test.ts`
预期：FAIL — `setMeshMaterial is not a function`。

- [ ] **步骤 3：接口 — `src/core/command/types.ts`**

顶部已 `import type { ... } from "../scene/types"`。加 `MaterialOverride` 导入与方法：

```ts
import type { MaterialOverride } from "../scene/material";
```

在 `addNode(node: SceneNode): void;` 同区块加：

```ts
  setMeshMaterial(nodeId: string, override: MaterialOverride | undefined): void;
```

- [ ] **步骤 4：useSceneStore impl + getSceneEditorStore 映射 — `src/services/scene/store.ts`**

`SceneState` 接口（`addNode` 附近）加：

```ts
  setMeshMaterial: (nodeId: string, override: MaterialOverride | undefined) => void;
```

文件顶部加 `import type { MaterialOverride } from "@/core/scene/material";`。

store body 内（`setBehaviorParameters` 之后）加 impl：

```ts
  setMeshMaterial: (nodeId, override) =>
    set((s) => {
      if (!s.project) return s;
      const node = s.project.scene.nodes[nodeId];
      if (!node || node.data.type !== "mesh") return s;
      const nextNode: SceneNode = {
        ...node,
        data: {
          ...node.data,
          material_overrides: override ? [override] : undefined,
        },
      };
      return mutateNode(s, nodeId, nextNode);
    }),
```

`getSceneEditorStore` 的 return 对象内（`addNode` 行后）加：

```ts
    setMeshMaterial: (nodeId, override) =>
      useSceneStore.getState().setMeshMaterial(nodeId, override),
```

- [ ] **步骤 5：\_test-utils + 两处手写 mock**

`src/core/command/commands/_test-utils.ts`：顶部已 import scene types，加 `import type { MaterialOverride } from "@/core/scene/material";`。Call 联合加一支：

```ts
  | { op: "setMeshMaterial"; nodeId: string; override: MaterialOverride | undefined }
```

`makeFakeEditor` return 内（`addNode` 后）加：

```ts
    setMeshMaterial: (nodeId, override) =>
      calls.push({ op: "setMeshMaterial", nodeId, override }),
```

`src/core/command/commands/set-node-transform.test.ts` 与 `src/services/command-history/store.test.ts` 的手写 `SceneEditorStore` mock（`addNode: () => { throw ... }` 旁）各加：

```ts
    setMeshMaterial: () => {
      throw new Error("setMeshMaterial not implemented in this fake");
    },
```

- [ ] **步骤 6：跑确认通过 + 全量 typecheck**

运行：`pnpm vitest run src/services/scene/store.test.ts && pnpm typecheck`
预期：store.test PASS；typecheck exit 0。

- [ ] **步骤 7：Commit**

```sh
git add src/core/command/types.ts src/core/command/commands/_test-utils.ts \
        src/core/command/commands/set-node-transform.test.ts \
        src/services/command-history/store.test.ts \
        src/services/scene/store.ts src/services/scene/store.test.ts
git commit -m "feat(scene): setMeshMaterial mutator (writes mesh material_overrides[0])

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T3：SetMaterialOverrideCommand

**文件：** 创建 `src/core/command/commands/set-material-override.ts` + `.test.ts`

- [ ] **步骤 1：写失败测试 `set-material-override.test.ts`**

```ts
import { describe, expect, it } from "vitest";

import { makeFakeEditor } from "./_test-utils";
import {
  SET_MATERIAL_OVERRIDE,
  SetMaterialOverrideCommand,
} from "./set-material-override";

const red = { slot: 0, color: "#ff0000" };
const green = { slot: 0, color: "#00ff00" };

describe("SetMaterialOverrideCommand", () => {
  it("apply() sets the override", () => {
    const editor = makeFakeEditor();
    new SetMaterialOverrideCommand({
      node_id: "m",
      override: red,
      prev_override: undefined,
    }).apply(editor);
    expect(editor.calls).toEqual([
      { op: "setMeshMaterial", nodeId: "m", override: red },
    ]);
  });

  it("revert() restores prev_override (undefined clears)", () => {
    const editor = makeFakeEditor();
    new SetMaterialOverrideCommand({
      node_id: "m",
      override: red,
      prev_override: undefined,
    }).revert(editor);
    expect(editor.calls).toEqual([
      { op: "setMeshMaterial", nodeId: "m", override: undefined },
    ]);
  });

  it("merges consecutive edits on the same node within the window", () => {
    const a = new SetMaterialOverrideCommand({
      node_id: "m",
      override: red,
      prev_override: undefined,
      timestamp: 1000,
    });
    const b = new SetMaterialOverrideCommand({
      node_id: "m",
      override: green,
      prev_override: red,
      timestamp: 1200,
    });
    expect(a.canMergeWith(b)).toBe(true);
    const merged = a.mergeWith(b);
    expect(merged.payload.override).toEqual(green); // latest
    expect(merged.payload.prev_override).toBeUndefined(); // earliest
    expect(merged.id).toBe(a.id);
  });

  it("does not merge across nodes or outside the window", () => {
    const a = new SetMaterialOverrideCommand({
      node_id: "m",
      override: red,
      prev_override: undefined,
      timestamp: 1000,
    });
    expect(
      a.canMergeWith(
        new SetMaterialOverrideCommand({
          node_id: "other",
          override: green,
          prev_override: undefined,
          timestamp: 1100,
        }),
      ),
    ).toBe(false);
    expect(
      a.canMergeWith(
        new SetMaterialOverrideCommand({
          node_id: "m",
          override: green,
          prev_override: red,
          timestamp: 9000,
        }),
      ),
    ).toBe(false);
  });

  it("uses a stable type", () => {
    expect(
      new SetMaterialOverrideCommand({
        node_id: "m",
        override: red,
        prev_override: undefined,
      }).type,
    ).toBe(SET_MATERIAL_OVERRIDE);
  });
});
```

- [ ] **步骤 2：跑确认失败**

运行：`pnpm vitest run src/core/command/commands/set-material-override.test.ts`
预期：FAIL — 模块缺失。

- [ ] **步骤 3：实现 `set-material-override.ts`**（镜像 `set-node-transform.ts`）

```ts
import type { MaterialOverride } from "@/core/scene/material";

import { generateUUID } from "../../id/uuid";
import type { Command, SceneEditorStore } from "../types";

export const SET_MATERIAL_OVERRIDE = "node.material.set" as const;

/** Merge window for consecutive material edits (e.g. dragging a slider at
 *  60fps) — same rationale + value as SetNodeTransform. */
export const MERGE_WINDOW_MS = 500;

export interface SetMaterialOverridePayload extends Record<string, unknown> {
  node_id: string;
  override: MaterialOverride | undefined;
  prev_override: MaterialOverride | undefined;
}

export interface SetMaterialOverrideInput {
  node_id: string;
  override: MaterialOverride | undefined;
  prev_override: MaterialOverride | undefined;
  id?: string;
  timestamp?: number;
}

export class SetMaterialOverrideCommand implements Command {
  readonly id: string;
  readonly type = SET_MATERIAL_OVERRIDE;
  readonly timestamp: number;
  readonly payload: SetMaterialOverridePayload;

  constructor(input: SetMaterialOverrideInput) {
    this.id = input.id ?? generateUUID();
    this.timestamp = input.timestamp ?? Date.now();
    this.payload = {
      node_id: input.node_id,
      override: input.override,
      prev_override: input.prev_override,
    };
  }

  apply(store: SceneEditorStore): void {
    store.setMeshMaterial(this.payload.node_id, this.payload.override);
  }

  revert(store: SceneEditorStore): void {
    store.setMeshMaterial(this.payload.node_id, this.payload.prev_override);
  }

  canMergeWith(other: Command): boolean {
    if (other.type !== SET_MATERIAL_OVERRIDE) return false;
    const o = other.payload as SetMaterialOverridePayload;
    if (o.node_id !== this.payload.node_id) return false;
    return Math.abs(other.timestamp - this.timestamp) < MERGE_WINDOW_MS;
  }

  mergeWith(other: Command): SetMaterialOverrideCommand {
    if (!this.canMergeWith(other)) {
      throw new Error(
        `cannot merge ${this.type} with ${other.type}: node or window mismatch`,
      );
    }
    const o = other.payload as SetMaterialOverridePayload;
    // earliest prev (revert to gesture start) + latest override (current).
    return new SetMaterialOverrideCommand({
      id: this.id,
      node_id: this.payload.node_id,
      override: o.override,
      prev_override: this.payload.prev_override,
      timestamp: other.timestamp,
    });
  }
}
```

- [ ] **步骤 4：跑确认通过**

运行：`pnpm vitest run src/core/command/commands/set-material-override.test.ts`
预期：PASS（5）。

- [ ] **步骤 5：Commit**

```sh
git add src/core/command/commands/set-material-override.ts \
        src/core/command/commands/set-material-override.test.ts
git commit -m "feat(commands): SetMaterialOverrideCommand (apply/revert/merge)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T4：mesh.ts 用 resolveMaterial 全量设材质

**文件：** 修改 `src/runtime/three/node-builders/mesh.ts` + `mesh.test.ts`

> 现状 build 创建写死默认材质后调 `applyOverrides(material, override[0])`（逐字段 if-set）。改为：build/update 都用 `resolveMaterial(override[0])` **全量**设 6 个字段——这样清空 override 时材质会正确重置回默认（旧的 if-set 不会重置缺失字段），且默认值与面板/codegen 同源。

- [ ] **步骤 1：扩 `mesh.test.ts`（追加 material 用例）**

`mesh.test.ts` 的 `meshNode` 当前签名 `meshNode(geometry?)`。新增一个带 material 的工厂或扩展。追加：

```ts
import { DEFAULT_MESH_MATERIAL } from "@/core/scene/material";

function meshWithMaterial(override?: {
  slot: number;
  color?: string;
  opacity?: number;
}): SceneNode {
  return {
    id: "m",
    name: "M",
    type: "mesh",
    transform: { position: [0, 0, 0], rotation: [0, 0, 0, 1], scale: [1, 1, 1] },
    parent_id: null,
    children_ids: [],
    visible: true,
    locked: false,
    data: {
      type: "mesh",
      geometry: { kind: "box" },
      material_overrides: override ? [override] : undefined,
    },
    behaviors: [],
    user_data: {},
  };
}

describe("mesh builder material", () => {
  it("applies the default material when there is no override", () => {
    const mesh = build(meshWithMaterial()) as THREE.Mesh;
    const mat = mesh.material as THREE.MeshStandardMaterial;
    expect(`#${mat.color.getHexString()}`).toBe(DEFAULT_MESH_MATERIAL.color);
    expect(mat.metalness).toBe(DEFAULT_MESH_MATERIAL.metalness);
    expect(mat.roughness).toBe(DEFAULT_MESH_MATERIAL.roughness);
  });

  it("applies override fields and sets transparent when opacity < 1", () => {
    const mesh = build(
      meshWithMaterial({ slot: 0, color: "#ff0000", opacity: 0.5 }),
    ) as THREE.Mesh;
    const mat = mesh.material as THREE.MeshStandardMaterial;
    expect(`#${mat.color.getHexString()}`).toBe("#ff0000");
    expect(mat.opacity).toBe(0.5);
    expect(mat.transparent).toBe(true);
  });

  it("resets material back to default when override is removed (update)", () => {
    const mesh = build(meshWithMaterial({ slot: 0, color: "#ff0000" })) as THREE.Mesh;
    update(mesh, meshWithMaterial()); // no override now
    const mat = mesh.material as THREE.MeshStandardMaterial;
    expect(`#${mat.color.getHexString()}`).toBe(DEFAULT_MESH_MATERIAL.color);
  });
});
```

确认 `mesh.test.ts` 顶部已 `import { build } from "./mesh";` — 追加 `update`：`import { build, update } from "./mesh";`。

- [ ] **步骤 2：跑确认失败**

运行：`pnpm vitest run src/runtime/three/node-builders/mesh.test.ts`
预期：FAIL（默认色断言：现状写死 `0xcccccc` 其实等于 `#cccccc` 可能已过；但 `transparent`/reset 用例会 FAIL，因现状无 opacity/transparent 处理且 update 不重置）。

- [ ] **步骤 3：改 `mesh.ts`**

顶部加 `import { resolveMaterial } from "@/core/scene/material";`。

`build` 内创建材质处改为：

```ts
const material = new THREE.MeshStandardMaterial();
applyResolvedMaterial(material, resolveMaterial(data.material_overrides?.[0]));
```

`update` 内材质处改为（替换原 `applyOverrides` 调用）：

```ts
if (object.material instanceof THREE.MeshStandardMaterial) {
  applyResolvedMaterial(object.material, resolveMaterial(data.material_overrides?.[0]));
}
```

把原 `applyOverrides` 函数替换为全量设值的 `applyResolvedMaterial`：

```ts
import type { ResolvedMaterial } from "@/core/scene/material";

function applyResolvedMaterial(
  material: THREE.MeshStandardMaterial,
  m: ResolvedMaterial,
): void {
  material.color.set(m.color);
  material.metalness = m.metalness;
  material.roughness = m.roughness;
  material.emissive.set(m.emissive);
  material.emissiveIntensity = m.emissive_intensity;
  material.opacity = m.opacity;
  material.transparent = m.opacity < 1;
}
```

删除旧的 `applyOverrides` 与不再用的 `MaterialOverride` 局部类型（若仅它用）。`geometryFor` / `requireMeshData` / `geometryKind` 不动。

- [ ] **步骤 4：跑确认通过 + 全量**

运行：`pnpm vitest run src/runtime/three/node-builders/mesh.test.ts && pnpm typecheck`
预期：mesh.test PASS；typecheck 0。

- [ ] **步骤 5：Commit**

```sh
git add src/runtime/three/node-builders/mesh.ts src/runtime/three/node-builders/mesh.test.ts
git commit -m "feat(runtime): mesh builder uses resolveMaterial (full set, resets on clear)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T5：codegen emitMesh 读 override emit 材质

**文件：** 修改 `src/runtime/three/export/scene-codegen.ts` + `scene-codegen.test.ts`

- [ ] **步骤 1：写失败测试（`scene-codegen.test.ts` 的 “mesh geometry” describe 旁加 “mesh material”）**

复用现有 `meshGeomNode` 风格，新增带 material 的 mesh：

```ts
describe("scene-codegen mesh material", () => {
  function meshMatNode(override?: {
    slot: number;
    color?: string;
    metalness?: number;
    opacity?: number;
  }): SceneNode {
    return {
      id: "m1",
      name: "M1",
      type: "mesh",
      transform: IDENTITY,
      parent_id: null,
      children_ids: [],
      visible: true,
      locked: false,
      data: {
        type: "mesh",
        geometry: { kind: "box" },
        material_overrides: override ? [override] : undefined,
      },
      behaviors: [],
      user_data: {},
    };
  }

  it("emits override color + metalness", () => {
    const p = withNodes(
      emptyProject(),
      [meshMatNode({ slot: 0, color: "#ff0000", metalness: 0.8 })],
      ["m1"],
    );
    const src = generateSceneModule({ project: p }).sceneModuleSource;
    expect(src).toMatch(/color: "#ff0000"/);
    expect(src).toMatch(/metalness: 0\.8/);
  });

  it("emits transparent: true when opacity < 1", () => {
    const p = withNodes(
      emptyProject(),
      [meshMatNode({ slot: 0, opacity: 0.5 })],
      ["m1"],
    );
    const src = generateSceneModule({ project: p }).sceneModuleSource;
    expect(src).toMatch(/opacity: 0\.5/);
    expect(src).toMatch(/transparent: true/);
  });

  it("emits the default material (opaque) when there is no override", () => {
    const p = withNodes(emptyProject(), [meshMatNode()], ["m1"]);
    const src = generateSceneModule({ project: p }).sceneModuleSource;
    expect(src).toMatch(/color: "#cccccc"/);
    expect(src).toMatch(/transparent: false/);
  });
});
```

> 现有 “no TypeScript-only syntax” 测试（`/:\s*THREE\./` 等）保持不变——新 emit 用引号 hex 串与字面量，不引入 `: THREE.`。

- [ ] **步骤 2：跑确认失败**

运行：`pnpm vitest run src/runtime/three/export/scene-codegen.test.ts -t "mesh material"`
预期：FAIL（现状写死 `color: 0xcccccc`，无 override/transparent）。

- [ ] **步骤 3：改 `emitMesh`**

`scene-codegen.ts` 顶部加 `import { resolveMaterial } from "@/core/scene/material";`。`emitMesh` 内 `_mat` 行（现 `MeshStandardMaterial({ color: 0xcccccc, ... })`）改为：

```ts
const m = resolveMaterial(node.data.material_overrides?.[0]);
push(
  ctx,
  `const ${varName}_mat = new THREE.MeshStandardMaterial({ ` +
    `color: ${JSON.stringify(m.color)}, metalness: ${m.metalness}, ` +
    `roughness: ${m.roughness}, emissive: ${JSON.stringify(m.emissive)}, ` +
    `emissiveIntensity: ${m.emissive_intensity}, opacity: ${m.opacity}, ` +
    `transparent: ${m.opacity < 1} });`,
);
```

（`node.data.type === "mesh"` 守卫已在函数开头；`node.data` 此处已窄化为 mesh。）

- [ ] **步骤 4：跑确认通过 + 全量**

运行：`pnpm vitest run src/runtime/three/export/scene-codegen.test.ts && pnpm typecheck`
预期：全 PASS（含 no-TS-syntax 回归）；typecheck 0。

- [ ] **步骤 5：Commit**

```sh
git add src/runtime/three/export/scene-codegen.ts src/runtime/three/export/scene-codegen.test.ts
git commit -m "feat(export): emitMesh emits material from resolveMaterial (color/metalness/roughness/emissive/opacity/transparent)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T6：SliderRow 控件

**文件：** 创建 `src/ui/editor/SliderRow.tsx` + `SliderRow.test.tsx`

- [ ] **步骤 1：写失败测试 `SliderRow.test.tsx`**

```tsx
import { fireEvent, render, screen } from "@testing-library/react";
import { describe, expect, it, vi } from "vitest";

import { SliderRow } from "./SliderRow";

describe("SliderRow", () => {
  it("renders the label and current value", () => {
    render(
      <SliderRow
        label="Metalness"
        value={0.3}
        min={0}
        max={1}
        step={0.01}
        onChange={() => {}}
      />,
    );
    expect(screen.getByText("Metalness")).toBeInTheDocument();
    expect(screen.getByRole("slider")).toHaveValue("0.3");
  });

  it("fires onChange when the range moves", () => {
    const onChange = vi.fn();
    render(
      <SliderRow
        label="Roughness"
        value={0.5}
        min={0}
        max={1}
        step={0.01}
        onChange={onChange}
      />,
    );
    fireEvent.change(screen.getByRole("slider"), { target: { value: "0.8" } });
    expect(onChange).toHaveBeenCalledWith(0.8);
  });

  it("does not fire onChange when disabled", () => {
    const onChange = vi.fn();
    render(
      <SliderRow
        label="Opacity"
        value={1}
        min={0}
        max={1}
        step={0.01}
        disabled
        onChange={onChange}
      />,
    );
    const slider = screen.getByRole("slider");
    expect(slider).toBeDisabled();
  });
});
```

- [ ] **步骤 2：跑确认失败**

运行：`pnpm vitest run src/ui/editor/SliderRow.test.tsx`
预期：FAIL — 模块缺失。

- [ ] **步骤 3：实现 `SliderRow.tsx`**

```tsx
import { cn } from "@/lib/utils";

interface Props {
  label: string;
  value: number;
  min: number;
  max: number;
  step: number;
  disabled?: boolean;
  onChange: (next: number) => void;
}

/** Label + native range + number box, kept in sync. Emits onChange(number)
 *  on every move; the caller's Command coalesces a drag into one undo. */
export function SliderRow({
  label,
  value,
  min,
  max,
  step,
  disabled = false,
  onChange,
}: Props) {
  const emit = (raw: string) => {
    const n = Number.parseFloat(raw);
    if (Number.isFinite(n)) onChange(n);
  };
  return (
    <div className="grid grid-cols-[80px_1fr_56px] items-center gap-2">
      <span className="text-muted-foreground">{label}</span>
      <input
        type="range"
        aria-label={label}
        value={value}
        min={min}
        max={max}
        step={step}
        disabled={disabled}
        onChange={(e) => emit(e.target.value)}
        className={cn("w-full", disabled && "opacity-50")}
      />
      <input
        type="number"
        aria-label={`${label} value`}
        value={value}
        min={min}
        max={max}
        step={step}
        disabled={disabled}
        onChange={(e) => emit(e.target.value)}
        className={cn(
          "w-full rounded border border-border bg-background/50 px-1 py-0.5 text-right font-mono text-[11px] outline-none focus:border-primary",
          disabled && "cursor-not-allowed opacity-50",
        )}
      />
    </div>
  );
}
```

- [ ] **步骤 4：跑确认通过**

运行：`pnpm vitest run src/ui/editor/SliderRow.test.tsx`
预期：PASS（3）。

- [ ] **步骤 5：Commit**

```sh
git add src/ui/editor/SliderRow.tsx src/ui/editor/SliderRow.test.tsx
git commit -m "feat(ui): SliderRow (range + number box) control

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T7：i18n properties.material.\*（en + zh）

**文件：** 修改 `src/i18n/locales/en-US/editor.json`、`src/i18n/locales/zh-CN/editor.json`

> typed-i18n 的 key 类型从 en-US 的 JSON 推断（见 `src/i18n/types.ts`）；加 key 改 en + zh 两个 JSON 即可，无需改 types.ts。

- [ ] **步骤 1：en-US/editor.json 的 `properties` 段加 `material`**

把（line 8-12）：

```json
  "properties": {
    "title": "Properties",
    "empty": "Select a node to inspect",
    "prefab_section": "prefab asset"
  },
```

改为：

```json
  "properties": {
    "title": "Properties",
    "empty": "Select a node to inspect",
    "prefab_section": "prefab asset",
    "material": {
      "section": "material",
      "base_color": "Base Color",
      "metalness": "Metalness",
      "roughness": "Roughness",
      "emissive": "Emissive",
      "emissive_intensity": "Emissive Int.",
      "opacity": "Opacity"
    }
  },
```

- [ ] **步骤 2：zh-CN/editor.json 同步**

```json
  "properties": {
    "title": "属性",
    "empty": "选中节点以查看属性",
    "prefab_section": "预制体资源",
    "material": {
      "section": "材质",
      "base_color": "基础色",
      "metalness": "金属度",
      "roughness": "粗糙度",
      "emissive": "自发光",
      "emissive_intensity": "自发光强度",
      "opacity": "不透明度"
    }
  },
```

- [ ] **步骤 3：typecheck（确认 key 类型可用）**

运行：`pnpm typecheck`
预期：exit 0。

- [ ] **步骤 4：Commit**

```sh
git add src/i18n/locales/en-US/editor.json src/i18n/locales/zh-CN/editor.json
git commit -m "feat(i18n): editor properties.material.* (en/zh)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T8：MaterialSection 面板 + EditorView 挂载

**文件：** 创建 `src/ui/editor/MaterialSection.tsx` + `MaterialSection.test.tsx`；修改 `src/ui/views/EditorView.tsx`

- [ ] **步骤 1：写失败测试 `MaterialSection.test.tsx`**

```tsx
import { fireEvent, render, screen } from "@testing-library/react";
import { beforeEach, describe, expect, it, vi } from "vitest";

import { createDefaultProject } from "@/core/scene/defaults";
import type { SceneNode } from "@/core/scene/types";
import { useCommandHistoryStore } from "@/services/command-history/store";
import { useSceneStore } from "@/services/scene/store";

import { MaterialSection } from "./MaterialSection";

function meshNode(): SceneNode {
  return {
    id: "m",
    name: "M",
    type: "mesh",
    transform: { position: [0, 0, 0], rotation: [0, 0, 0, 1], scale: [1, 1, 1] },
    parent_id: null,
    children_ids: [],
    visible: true,
    locked: false,
    data: { type: "mesh", geometry: { kind: "box" } },
    behaviors: [],
    user_data: {},
  };
}

function seed(node: SceneNode) {
  const project = createDefaultProject({
    id: "p",
    name: "t",
    target: { kind: "three.js", version: "0.184.0", module_format: "esm" },
    now: new Date("2026-01-01T00:00:00Z"),
  });
  project.scene.root_node_ids = [node.id];
  project.scene.nodes[node.id] = node;
  useSceneStore.setState({ project });
}

describe("MaterialSection", () => {
  beforeEach(() => {
    useSceneStore.setState({ project: null });
    useCommandHistoryStore.getState().clear();
  });

  it("renders the material controls for a mesh node", () => {
    const node = meshNode();
    seed(node);
    render(<MaterialSection node={node} />);
    expect(screen.getByText("Base Color")).toBeInTheDocument();
    expect(screen.getByLabelText("Metalness")).toBeInTheDocument();
  });

  it("dispatches SetMaterialOverrideCommand when a slider changes", () => {
    const node = meshNode();
    seed(node);
    const exec = vi.spyOn(useCommandHistoryStore.getState(), "execute");
    render(<MaterialSection node={node} />);
    fireEvent.change(screen.getByLabelText("Metalness"), {
      target: { value: "0.9" },
    });
    expect(exec).toHaveBeenCalledWith(
      expect.objectContaining({ type: "node.material.set" }),
      expect.anything(),
    );
  });
});
```

- [ ] **步骤 2：跑确认失败**

运行：`pnpm vitest run src/ui/editor/MaterialSection.test.tsx`
预期：FAIL — 模块缺失。

- [ ] **步骤 3：实现 `MaterialSection.tsx`**

```tsx
import { useTranslation } from "react-i18next";

import { SetMaterialOverrideCommand } from "@/core/command/commands/set-material-override";
import { type MaterialOverride, resolveMaterial } from "@/core/scene/material";
import type { SceneNode } from "@/core/scene/types";
import { executeCommand } from "@/services/command-history";

import { SliderRow } from "./SliderRow";

export function MaterialSection({ node }: { node: SceneNode }) {
  const { t } = useTranslation("editor");
  if (node.data.type !== "mesh") return null;

  const cur = node.data.material_overrides?.[0];
  const m = resolveMaterial(cur);

  // Build the full override literal per field with explicit field names —
  // a computed `{ ...cur, [field]: value }` would widen the key to
  // `string | number` and not satisfy MaterialOverride. `...cur` is a no-op
  // when cur is undefined (first edit).
  const commit = (override: MaterialOverride) =>
    executeCommand(
      new SetMaterialOverrideCommand({
        node_id: node.id,
        override,
        prev_override: cur,
      }),
    );

  return (
    <div className="mt-2 space-y-2 border-t border-border pt-3 font-mono text-[11px]">
      <p className="mb-1 uppercase tracking-wider text-muted-foreground">
        {t("properties.material.section")}
      </p>

      <ColorField
        label={t("properties.material.base_color")}
        value={m.color}
        onChange={(v) => commit({ slot: 0, ...cur, color: v })}
      />
      <SliderRow
        label={t("properties.material.metalness")}
        value={m.metalness}
        min={0}
        max={1}
        step={0.01}
        onChange={(v) => commit({ slot: 0, ...cur, metalness: v })}
      />
      <SliderRow
        label={t("properties.material.roughness")}
        value={m.roughness}
        min={0}
        max={1}
        step={0.01}
        onChange={(v) => commit({ slot: 0, ...cur, roughness: v })}
      />
      <ColorField
        label={t("properties.material.emissive")}
        value={m.emissive}
        onChange={(v) => commit({ slot: 0, ...cur, emissive: v })}
      />
      <SliderRow
        label={t("properties.material.emissive_intensity")}
        value={m.emissive_intensity}
        min={0}
        max={4}
        step={0.1}
        onChange={(v) => commit({ slot: 0, ...cur, emissive_intensity: v })}
      />
      <SliderRow
        label={t("properties.material.opacity")}
        value={m.opacity}
        min={0}
        max={1}
        step={0.01}
        onChange={(v) => commit({ slot: 0, ...cur, opacity: v })}
      />
    </div>
  );
}

function ColorField({
  label,
  value,
  onChange,
}: {
  label: string;
  value: string;
  onChange: (hex: string) => void;
}) {
  return (
    <div className="grid grid-cols-[80px_1fr] items-center gap-2">
      <span className="text-muted-foreground">{label}</span>
      <input
        type="color"
        aria-label={label}
        value={value}
        onChange={(e) => onChange(e.target.value)}
        className="h-6 w-12 rounded border border-border bg-transparent"
      />
    </div>
  );
}
```

> `disabled`：`MaterialSection` 不单独接 disabled——`NodeProperties` 外层已用 `<fieldset disabled={playState==="play"}>` 包裹，locked 由其上下文覆盖（与 transform 行一致）。若视觉验证发现 locked 态未灰，再给 SliderRow/ColorField 传 `disabled={isEffectivelyLocked(node)}`。

- [ ] **步骤 4：EditorView 挂载**

`src/ui/views/EditorView.tsx` 顶部加 `import { MaterialSection } from "@/ui/editor/MaterialSection";`。`NodeProperties` 的 `<dl>` 内，`{node.data.type === "prefab_instance" && (<PrefabInfo .../>)}` 之后加：

```tsx
{
  node.data.type === "mesh" && <MaterialSection node={node} />;
}
```

- [ ] **步骤 5：跑确认通过 + 全量**

运行：`pnpm vitest run src/ui/editor/MaterialSection.test.tsx && pnpm lint && pnpm typecheck && pnpm test`
预期：MaterialSection PASS；lint 0 error；typecheck 0；test 全绿。

- [ ] **步骤 6：Commit**

```sh
git add src/ui/editor/MaterialSection.tsx src/ui/editor/MaterialSection.test.tsx src/ui/views/EditorView.tsx
git commit -m "feat(ui): MaterialSection panel (6 PBR params) wired into NodeProperties

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T9：验证 + 视觉验证 + PR + roadmap + merge

- [ ] **步骤 1：全套验证** `pnpm lint && pnpm typecheck && pnpm test`（全绿，0 error）。

- [ ] **步骤 2：视觉验证（人工 `pnpm tauri dev`，spec §2/§8 清单）**

- [ ] 选中 mesh（如双击库里的 Box），属性面板出现「material」区
- [ ] 改 Base Color / Emissive（color picker）→ 视口实时变
- [ ] 拖 Metalness / Roughness / Opacity / Emissive Int. 滑块 → 视口实时变；Opacity<1 半透明
- [ ] 一次拖动 = 一条 undo：Cmd+Z 一次回到拖动前；Cmd+Shift+Z 恢复
- [ ] 选中非 mesh（light/camera/group）→ 无 material 区
- [ ] play 模式 / locked 节点 → 控件 disabled
- [ ] Export Vite + Standalone → 运行页面材质（颜色/金属/粗糙/透明）正确
- [ ] 中英文文案

- [ ] **步骤 3：push + PR**

```sh
git push -u origin feat/v0.2-material-editing
/opt/homebrew/bin/gh pr create --base main --head feat/v0.2-material-editing \
  --title "feat(v0.2): material editing (PBR params panel + export)" \
  --body "$(cat <<'EOF'
## What

v0.2 第二个 sub-stage：mesh 节点 PBR 材质编辑（v0.2 收尾）。

- `material.ts`：`DEFAULT_MESH_MATERIAL` + `resolveMaterial`（面板/runtime/codegen 单一来源）。
- 属性面板 `MaterialSection`（仅 mesh）+ `SliderRow`：编辑 baseColor / metalness / roughness / emissive / emissive_intensity / opacity。
- `SetMaterialOverrideCommand` + `setMeshMaterial` mutator：可撤销，拖动合并为单条 undo。
- `emitMesh` 用 `resolveMaterial` emit 材质字段（含 `transparent: opacity<1`）。

## Why

- Spec: \`docs/superpowers/specs/2026-06-03-v0.2-material-editing-design.md\`
- Plan: \`docs/superpowers/plans/2026-06-03-v0.2-material-editing-plan.md\`
- 延后项见 roadmap Backlog：材质贴图 pipeline(normalMap)、资源拖拽落点、多源上传、多材质槽。

## How to test

- [x] lint / typecheck / test 本地绿
- [ ] CI 绿
- [ ] 人工 pnpm tauri dev（plan T9 步骤 2）

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **步骤 4：CI 绿 + 视觉验证后回填 roadmap + merge**

`docs/roadmap.md` v0.2 节：材质编辑 sub-stage `[x]` + #NN；success criteria 后两条勾选；表格行（line 26）改 `✅`（v0.2 两 sub-stage 齐）。commit + push。CI 绿 + 视觉验证通过 → `gh pr merge NN --merge --delete-branch`。

---

## 收尾验收

满足 spec §2：面板可编 6 参数 + undo/redo（拖动合并）+ 导出含材质字段（两目标）+ lint/typecheck/test 绿 + 视觉验证。v0.2 两个 sub-stage（资源库 #29 + 材质编辑）齐活，**v0.2 收尾**。延后项（材质贴图/拖拽落点/多源/多槽）已在 roadmap Backlog 记录。
