# B4a Babylon 材质覆盖 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 让 Babylon mesh 应用 PBR 材质覆盖（color/metalness/roughness/emissive/emissive_intensity/opacity，slot 0）create+update 都生效，与 Three 跨引擎对等，并加 `RuntimeNodeInfo.material` + conformance 材质对等。

**架构：** 复用引擎中立 `resolveMaterial`（`@/core/scene/material`），新 `babylon/material.ts` 写 Babylon `applyPbrMaterial`（→`PBRMaterial`），`BabylonAdapter.createMesh`/`syncNode("update")` 调用之；两引擎 `describeNode` 读真实材质填 `RuntimeNodeInfo.material`；conformance 参数化套件断言两引擎报等价材质。

**技术栈：** TypeScript / `@babylonjs/core` 9.11（`PBRMaterial`/`Color3`）/ three（`MeshStandardMaterial`）/ Vitest（NullEngine headless）。

**规格：** `docs/superpowers/specs/2026-06-18-v1.0b4a-babylon-material-design.md`

**测试约束：** 全部 headless 可测（NullEngine + Three 无 GL 的材质对象）；无需 smoke 即可验证字段正确。visual smoke（任务 7）只验观感（color/emissive/opacity 在 Babylon 实体可见）。

---

## 文件结构

| 文件                                           | 职责                                                      | 操作 |
| ---------------------------------------------- | --------------------------------------------------------- | ---- |
| `src/runtime/adapter.ts`                       | `MaterialInfo` + `RuntimeNodeInfo.material?`              | 修改 |
| `src/runtime/babylon/material.ts`              | `applyPbrMaterial` + `createPbrMaterial`                  | 创建 |
| `src/runtime/babylon/material.test.ts`         | 上者单测                                                  | 创建 |
| `src/runtime/babylon/adapter.ts`               | createMesh 应用材质 + update 重应用 + describeNode 读材质 | 修改 |
| `src/runtime/babylon/adapter.test.ts`          | 材质 create/update/clear/describeNode 断言                | 修改 |
| `src/runtime/three/adapter.ts`                 | describeNode 读材质                                       | 修改 |
| `src/runtime/three/adapter.test.ts`            | describeNode 报材质断言                                   | 修改 |
| `src/runtime/conformance/conformance-suite.ts` | 材质对等断言（参数化两引擎）                              | 修改 |
| `docs/roadmap.md`                              | B4a 行                                                    | 修改 |

---

## 任务 1：RuntimeNodeInfo 加 material

**文件：**

- 修改：`src/runtime/adapter.ts`（`RuntimeNodeInfo` interface，约 :23-33）

类型新增，无运行时行为，验证 = typecheck + 既有测试。

- [ ] **步骤 1：加 MaterialInfo + material 字段**

在 `src/runtime/adapter.ts` 的 `RuntimeNodeInfo` interface 之前加 `MaterialInfo`，并在 interface 内加 `material?`：

```ts
/** Engine-neutral snapshot of a mesh's resolved material (default ⊕ override),
 *  read from the actual engine material. Colours are #rrggbb hex (lowercase).
 *  Only present on mesh nodes that carry a material. */
export interface MaterialInfo {
  color: string;
  metalness: number;
  roughness: number;
  emissive: string;
  emissive_intensity: number;
  opacity: number;
}
```

在 `RuntimeNodeInfo` 内（`geometryKind?` 附近）加：

```ts
  material?: MaterialInfo;
```

- [ ] **步骤 2：验证**

运行：`pnpm typecheck && pnpm test`
预期：typecheck PASS、既有测试全绿（纯加可选字段，不影响现有）。

- [ ] **步骤 3：Commit**

```bash
git add src/runtime/adapter.ts
git commit -m "feat(runtime): add MaterialInfo to RuntimeNodeInfo (cross-engine material read)"
```

---

## 任务 2：babylon/material.ts — TDD

**文件：**

- 创建：`src/runtime/babylon/material.ts`
- 测试：`src/runtime/babylon/material.test.ts`

- [ ] **步骤 1：编写失败的测试**

```ts
// src/runtime/babylon/material.test.ts
import { describe, expect, it } from "vitest";
import { NullEngine, Scene, PBRMaterial } from "@babylonjs/core";
import { applyPbrMaterial, createPbrMaterial } from "./material";
import { DEFAULT_MESH_MATERIAL, resolveMaterial } from "@/core/scene/material";

function scene() {
  return new Scene(new NullEngine());
}

describe("applyPbrMaterial", () => {
  it("maps a resolved override onto PBRMaterial fields", () => {
    const s = scene();
    const mat = createPbrMaterial("m", s);
    applyPbrMaterial(
      mat,
      resolveMaterial({
        slot: 0,
        color: "#3366cc",
        metalness: 0.4,
        roughness: 0.2,
        emissive: "#110022",
        emissive_intensity: 2,
        opacity: 0.5,
      }),
    );
    expect(mat.albedoColor.toHexString().toLowerCase()).toBe("#3366cc");
    expect(mat.metallic).toBeCloseTo(0.4);
    expect(mat.roughness).toBeCloseTo(0.2);
    expect(mat.emissiveColor.toHexString().toLowerCase()).toBe("#110022");
    expect(mat.emissiveIntensity).toBeCloseTo(2);
    expect(mat.alpha).toBeCloseTo(0.5);
    expect(mat.transparencyMode).toBe(PBRMaterial.MATERIAL_ALPHABLEND);
    s.getEngine().dispose();
  });

  it("opacity=1 → OPAQUE; full-set resets to default when override cleared", () => {
    const s = scene();
    const mat = createPbrMaterial("m", s);
    applyPbrMaterial(mat, resolveMaterial({ slot: 0, opacity: 0.3 }));
    expect(mat.transparencyMode).toBe(PBRMaterial.MATERIAL_ALPHABLEND);
    // clear override → resolveMaterial(undefined) = defaults
    applyPbrMaterial(mat, resolveMaterial(undefined));
    expect(mat.alpha).toBeCloseTo(1);
    expect(mat.transparencyMode).toBe(PBRMaterial.MATERIAL_OPAQUE);
    expect(mat.albedoColor.toHexString().toLowerCase()).toBe(
      DEFAULT_MESH_MATERIAL.color,
    );
    s.getEngine().dispose();
  });
});
```

- [ ] **步骤 2：运行验证失败**

运行：`pnpm test src/runtime/babylon/material.test.ts`
预期：FAIL，模块不存在。

- [ ] **步骤 3：实现 material.ts**

```ts
// src/runtime/babylon/material.ts
import { Color3, PBRMaterial, type Scene } from "@babylonjs/core";

import type { ResolvedMaterial } from "@/core/scene/material";

/** Full-set a Babylon PBRMaterial from a resolved (default ⊕ override)
 *  descriptor. Every field is set unconditionally so clearing an override
 *  resets to default (mirrors three/node-builders/mesh.applyResolvedMaterial). */
export function applyPbrMaterial(mat: PBRMaterial, m: ResolvedMaterial): void {
  mat.albedoColor = Color3.FromHexString(m.color);
  mat.metallic = m.metalness;
  mat.roughness = m.roughness;
  mat.emissiveColor = Color3.FromHexString(m.emissive);
  mat.emissiveIntensity = m.emissive_intensity;
  mat.alpha = m.opacity;
  mat.transparencyMode =
    m.opacity < 1 ? PBRMaterial.MATERIAL_ALPHABLEND : PBRMaterial.MATERIAL_OPAQUE;
}

export function createPbrMaterial(name: string, scene: Scene): PBRMaterial {
  return new PBRMaterial(`${name}-mat`, scene);
}
```

- [ ] **步骤 4：运行验证通过**

运行：`pnpm test src/runtime/babylon/material.test.ts`
预期：PASS。

- [ ] **步骤 5：Commit**

```bash
git add src/runtime/babylon/material.ts src/runtime/babylon/material.test.ts
git commit -m "feat(babylon): applyPbrMaterial maps resolved material to PBRMaterial"
```

---

## 任务 3：BabylonAdapter 应用材质（create + update）

**文件：**

- 修改：`src/runtime/babylon/adapter.ts`（`createMesh` :335、`create()` 的 mesh 分支、`syncNode("update")` :116-128，import）
- 修改：`src/runtime/babylon/adapter.test.ts`

- [ ] **步骤 1：编写失败的测试**

在 `src/runtime/babylon/adapter.test.ts` 加（沿用现有 NullEngine adapter fixture；若现有 helper 不同，按其范式建 mesh 节点）：

```ts
import { Mesh, PBRMaterial } from "@babylonjs/core";

// 在现有 describe 内：
it("applies material overrides to the mesh on create", () => {
  const adapter = makeAdapter(); // 现有 fixture：NullEngine BabylonAdapter
  adapter.syncNode(
    meshNode("m", {
      material_overrides: [{ slot: 0, color: "#3366cc", metalness: 0.4 }],
    }),
    "add",
  );
  const mesh = adapter.getRuntimeObject("m") as Mesh;
  expect(mesh.material).toBeInstanceOf(PBRMaterial);
  expect((mesh.material as PBRMaterial).albedoColor.toHexString().toLowerCase()).toBe(
    "#3366cc",
  );
  expect((mesh.material as PBRMaterial).metallic).toBeCloseTo(0.4);
});

it("re-applies material on update and resets when override cleared", () => {
  const adapter = makeAdapter();
  adapter.syncNode(
    meshNode("m", { material_overrides: [{ slot: 0, color: "#ff0000" }] }),
    "add",
  );
  adapter.syncNode(
    meshNode("m", { material_overrides: [{ slot: 0, color: "#00ff00" }] }),
    "update",
  );
  const mesh = adapter.getRuntimeObject("m") as Mesh;
  expect((mesh.material as PBRMaterial).albedoColor.toHexString().toLowerCase()).toBe(
    "#00ff00",
  );
  // clear → default #cccccc
  adapter.syncNode(meshNode("m", {}), "update");
  expect((mesh.material as PBRMaterial).albedoColor.toHexString().toLowerCase()).toBe(
    "#cccccc",
  );
});
```

> `meshNode(id, dataExtra)` 建一个 `type:"mesh"` 节点，`data: { type: "mesh", geometry: { kind: "box" }, ...dataExtra }`。若现有 adapter.test.ts 已有 mesh 节点 helper，复用并把 material_overrides 传进去；没有就照现有 boxNode 范式加一个。

运行 `pnpm test src/runtime/babylon/adapter.test.ts` → 预期 FAIL（material 是 null / 不是 PBRMaterial）。

- [ ] **步骤 2：改 adapter.ts**

顶部 import 增补（核对现有 import，按需并入）：

```ts
import { Mesh, PBRMaterial } from "@babylonjs/core";
import { resolveMaterial, type MaterialOverride } from "@/core/scene/material";
import { applyPbrMaterial, createPbrMaterial } from "./material";
```

`createMesh`（:335）改为建几何后应用材质（加 `materialOverride?` 参数）：

```ts
function createMesh(
  name: string,
  kind: NonNullable<Extract<NodeData, { type: "mesh" }>["geometry"]>["kind"],
  scene: Scene,
  materialOverride?: MaterialOverride,
): Mesh {
  const mesh = buildMeshGeometry(name, kind, scene);
  const mat = createPbrMaterial(name, scene);
  applyPbrMaterial(mat, resolveMaterial(materialOverride));
  mesh.material = mat;
  return mesh;
}

function buildMeshGeometry(
  name: string,
  kind: NonNullable<Extract<NodeData, { type: "mesh" }>["geometry"]>["kind"],
  scene: Scene,
): Mesh {
  switch (kind) {
    case "sphere":
      return MeshBuilder.CreateSphere(name, { diameter: 1 }, scene);
    case "plane":
      return MeshBuilder.CreatePlane(name, { size: 1 }, scene);
    case "cylinder":
      return MeshBuilder.CreateCylinder(name, { height: 1, diameter: 1 }, scene);
    case "box":
    default:
      return MeshBuilder.CreateBox(name, { size: 1 }, scene);
  }
}
```

（即把原 `createMesh` 的 switch 抽成 `buildMeshGeometry`，`createMesh` 包一层加材质。`MeshBuilder.Create*` 返回 `Mesh`，类型对。）

`create()` 的 mesh 分支（约 :156-161）传入 override：

```ts
      case "mesh": {
        const kind = data.geometry?.kind ?? "box";
        return {
          object: createMesh(node.name, kind, this.scene, data.material_overrides?.[0]),
          meta: { nodeId: node.id, kind: "mesh", geometryKind: kind },
        };
      }
```

`syncNode("update")`（:116-128）在 `applyBabylonTransform(existing, node); existing.setEnabled(node.visible);` 之后加材质重应用：

```ts
if (
  node.data.type === "mesh" &&
  existing instanceof Mesh &&
  existing.material instanceof PBRMaterial
) {
  applyPbrMaterial(
    existing.material,
    resolveMaterial(node.data.material_overrides?.[0]),
  );
}
```

- [ ] **步骤 3：运行验证通过**

运行：`pnpm test src/runtime/babylon/adapter.test.ts && pnpm typecheck`
预期：PASS。

- [ ] **步骤 4：Commit**

```bash
git add src/runtime/babylon/adapter.ts src/runtime/babylon/adapter.test.ts
git commit -m "feat(babylon): apply PBR material overrides on mesh create + update"
```

---

## 任务 4：两引擎 describeNode 读材质

**文件：**

- 修改：`src/runtime/babylon/adapter.ts`（describeNode :183-208）+ `adapter.test.ts`
- 修改：`src/runtime/three/adapter.ts`（describeNode :272+）+ `adapter.test.ts`

- [ ] **步骤 1：编写失败的测试**

Babylon（`babylon/adapter.test.ts`）：

```ts
it("describeNode reports the mesh material", () => {
  const adapter = makeAdapter();
  adapter.syncNode(
    meshNode("m", {
      material_overrides: [{ slot: 0, color: "#3366cc", roughness: 0.2 }],
    }),
    "add",
  );
  const mat = adapter.describeNode("m")?.material;
  expect(mat?.color).toBe("#3366cc");
  expect(mat?.roughness).toBeCloseTo(0.2);
  expect(mat?.opacity).toBeCloseTo(1);
});
```

Three（`three/adapter.test.ts`，沿用其现有 fixture + mesh 节点 helper）：

```ts
it("describeNode reports the mesh material", () => {
  const adapter = makeThreeAdapter(); // 现有 fixture
  adapter.syncNode(
    meshNode("m", {
      material_overrides: [{ slot: 0, color: "#3366cc", roughness: 0.2 }],
    }),
    "add",
  );
  const mat = adapter.describeNode("m")?.material;
  expect(mat?.color).toBe("#3366cc");
  expect(mat?.roughness).toBeCloseTo(0.2);
});
```

运行两个 → 预期 FAIL（material 为 undefined）。

- [ ] **步骤 2：BabylonAdapter.describeNode 加材质**

在 `describeNode` 的 `return info` 之前（geometryKind 之后）加：

```ts
if (obj instanceof Mesh && obj.material instanceof PBRMaterial) {
  const mat = obj.material;
  info.material = {
    color: mat.albedoColor.toHexString().toLowerCase(),
    metalness: mat.metallic ?? 0,
    roughness: mat.roughness ?? 0,
    emissive: mat.emissiveColor.toHexString().toLowerCase(),
    emissive_intensity: mat.emissiveIntensity,
    opacity: mat.alpha,
  };
}
```

（`Mesh`/`PBRMaterial` 已在任务 3 import。`metallic`/`roughness` 是 `Nullable<number>`，applyPbrMaterial 总会设，`?? 0` 兜底类型。）

- [ ] **步骤 3：ThreeAdapter.describeNode 加材质**

在 `three/adapter.ts` describeNode 的 `return info` 之前加：

```ts
if (obj instanceof THREE.Mesh && obj.material instanceof THREE.MeshStandardMaterial) {
  const mat = obj.material;
  info.material = {
    color: `#${mat.color.getHexString()}`,
    metalness: mat.metalness,
    roughness: mat.roughness,
    emissive: `#${mat.emissive.getHexString()}`,
    emissive_intensity: mat.emissiveIntensity,
    opacity: mat.opacity,
  };
}
```

（`THREE` 已 import；`getHexString()` 返回小写无 `#`，前缀 `#` 与 Babylon `.toLowerCase()` 归一一致。）

- [ ] **步骤 4：运行验证通过**

运行：`pnpm test src/runtime/babylon/adapter.test.ts src/runtime/three/adapter.test.ts && pnpm typecheck`
预期：PASS。

- [ ] **步骤 5：Commit**

```bash
git add src/runtime/babylon/adapter.ts src/runtime/three/adapter.ts src/runtime/babylon/adapter.test.ts src/runtime/three/adapter.test.ts
git commit -m "feat(adapter): describeNode reports mesh material (both engines)"
```

---

## 任务 5：conformance 材质对等

**文件：**

- 修改：`src/runtime/conformance/conformance-suite.ts`

`describeAdapterConformance` 的断言对两引擎参数化跑同一批；加一条材质用例 → 两引擎都过 = 跨引擎对等。颜色用 ±2/通道容差（防 Three sRGB↔linear 8bit 往返 ±1）。

- [ ] **步骤 1：加 hex 容差 helper + 材质对等 it**

在 `conformance-suite.ts` 文件内（`describeAdapterConformance` 函数外，或顶部）加 helper：

```ts
function expectHexClose(actual: string | undefined, expected: string): void {
  expect(actual).toBeDefined();
  const ch = (h: string) => [1, 3, 5].map((i) => parseInt(h.slice(i, i + 2), 16));
  const a = ch(actual!);
  const e = ch(expected);
  for (let i = 0; i < 3; i++) expect(Math.abs(a[i]! - e[i]!)).toBeLessThanOrEqual(2);
}
```

在 `describe(...)` 内（如 "maps mesh geometry kinds" 之后）加：

```ts
it("describeNode reports equivalent material across engines", () => {
  const adapter = make();
  adapter.syncNode(
    {
      ...baseMeshNode("m"), // 现有套件里建 mesh 节点的方式
      data: {
        type: "mesh",
        geometry: { kind: "box" },
        material_overrides: [
          {
            slot: 0,
            color: "#3366cc",
            metalness: 0.4,
            roughness: 0.2,
            emissive: "#110022",
            emissive_intensity: 2,
            opacity: 0.5,
          },
        ],
      },
    },
    "add",
  );
  const mat = adapter.describeNode("m")?.material;
  expect(mat).toBeDefined();
  expectHexClose(mat?.color, "#3366cc");
  expect(mat?.metalness).toBeCloseTo(0.4);
  expect(mat?.roughness).toBeCloseTo(0.2);
  expectHexClose(mat?.emissive, "#110022");
  expect(mat?.emissive_intensity).toBeCloseTo(2);
  expect(mat?.opacity).toBeCloseTo(0.5);
});
```

> 用套件里现有的「建 mesh 节点」方式（看 "maps mesh geometry kinds" 用例怎么构造节点，复用同样的 base + 覆盖 data）。`make()` 是套件里已有的 adapter 工厂别名（见 :58-61）。

- [ ] **步骤 2：验证**

运行：`pnpm test src/runtime/conformance/ && pnpm typecheck`
预期：材质对等用例对 Three + Babylon 两引擎都过。

- [ ] **步骤 3：Commit**

```bash
git add src/runtime/conformance/conformance-suite.ts
git commit -m "test(conformance): material parity across engines"
```

---

## 任务 6：更新 roadmap

**文件：**

- 修改：`docs/roadmap.md`（B4 行）

- [ ] **步骤 1：把 B4 行拆出 B4a**

找到 B4 行（`- [ ] **B4 · 视口能力剩余**...`），在其下加 B4a 勾选子条目（保留 B4 父行其余项）：

```markdown
    - [x] **B4a · Babylon 材质覆盖**：`BabylonAdapter` create+update 应用 PBR 材质覆盖（`PBRMaterial`，复用引擎中立 `resolveMaterial`，新 `babylon/material.ts`）；`RuntimeNodeInfo.material` + 两引擎 `describeNode` 读真实材质 + conformance 材质对等。观感/色彩空间/IBL/光强对齐留 B4d。
```

（B4 父行保持 `- [ ]`，其余 socket markers / drop / focus / play / 底色 仍未做。）

- [ ] **步骤 2：Commit**

```bash
git add docs/roadmap.md
git commit -m "docs(roadmap): B4a Babylon material override complete"
```

---

## 任务 7：全量验证 + visual smoke

- [ ] **步骤 1：自动化全绿**

运行：`pnpm typecheck && pnpm lint && pnpm test`
预期：全 PASS。

- [ ] **步骤 2：visual smoke（`pnpm tauri dev`，Babylon 模式）**

1. 选中 mesh，属性面板改 **baseColor** → Babylon 实体颜色变（不再灰白）
2. 改 **emissive / emissive_intensity** → 自发光变
3. 改 **opacity** < 1 → 半透明
4. 改 **metalness/roughness** → 数值生效（金属观感偏暗是已知，B4d 对齐）
5. 撤销/重做材质改动 → Babylon 跟随
6. Three 模式材质零回归；console 零错

- [ ] **步骤 3：偏差则修复重跑**；全过后进入收尾。

---

## 自检记录

- **规格覆盖**：§3 material.ts→任务 2；§4 createMesh/update→任务 3；§5 RuntimeNodeInfo.material→任务 1、describeNode→任务 4、conformance→任务 5；§6 测试分布任务 2-5 + 任务 7 smoke；§8 交付物全覆盖。
- **占位符**：material.ts/applyPbrMaterial/describeNode 给完整代码；adapter.test/conformance 的 mesh 节点构造让实现者复用套件现有 helper（注明「看现有 X 怎么建节点」）而非编造——因各 test 文件的 fixture 命名需以实际为准（`makeAdapter`/`meshNode`/`baseMeshNode` 是占位名，实现者对齐现有）。
- **类型一致**：`MaterialInfo`（任务 1 定义）在任务 4 describeNode、任务 5 conformance 一致；`ResolvedMaterial`/`MaterialOverride`/`resolveMaterial`（core/scene/material 既有）在任务 2/3 一致；`applyPbrMaterial`/`createPbrMaterial`（任务 2）在任务 3 用；`#rrggbb 小写`归一在任务 4 两引擎 + 任务 5 容差一致。
- **顺序依赖**：1（类型）→4/5；2（material.ts）→3；3（apply）→4（describeNode 读已应用的材质）→5（conformance）。任务编号即执行序。
