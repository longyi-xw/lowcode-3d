# B4c Babylon play + focus + drop 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 把 play 行为预览 + F focus + 资源拖放落点接到 BabylonViewport，翻 `engineCapabilities` 三 flag，让 Babylon 模式与 Three 全对齐可编辑。

**架构：** 加 BabylonRenderHost 引擎专有面（`setFrameCallback`/`focusCamera`，镜像 ThreeRenderHost）+ `IRuntimeAdapter.raycastGroundPoint` 契约（Babylon 实现 + conformance 对等）+ Babylon focus 目标计算（镜像 `computeFocusTarget`）+ BabylonViewport 接 play/focus/drop（镜像 ThreeViewport），复用引擎中立 helper（`installBehaviors`/`drop-helpers`/`AddNodeCommand`）。

**技术栈：** TypeScript / `@babylonjs/core` 9.11（`ArcRotateCamera`/`createPickingRay`/`Ray.intersectsPlane`/`Plane`/`getDeltaTime`）/ React / Vitest（NullEngine headless）。

**规格：** `docs/superpowers/specs/2026-06-22-v1.0b4c-babylon-play-focus-drop-design.md`

**测试约束：** 引擎面（focusCamera/raycastGroundPoint/engineCapabilities/focus 计算/conformance）headless 可测；play/focus/drop 整链 = visual smoke（任务 8）。

---

## 文件结构

| 文件                                           | 职责                                         | 操作      |
| ---------------------------------------------- | -------------------------------------------- | --------- |
| `src/runtime/render-host.ts`                   | `engineCapabilities` 翻全 true（保留 seam）  | 修改      |
| `src/runtime/adapter.ts`                       | `IRuntimeAdapter.raycastGroundPoint` 契约    | 修改      |
| `src/runtime/babylon/adapter.ts`               | `raycastGroundPoint` 实现                    | 修改      |
| `src/runtime/babylon/render-host.ts`           | `setFrameCallback` + `focusCamera` + dispose | 修改      |
| `src/runtime/babylon/focus-helpers.ts`         | Babylon focus 目标计算                       | 创建      |
| `src/runtime/conformance/conformance-suite.ts` | `raycastGroundPoint` 对等                    | 修改      |
| `src/ui/viewport/BabylonViewport.tsx`          | play/focus/drop 接线 + hostRef               | 修改      |
| 对应 `.test.ts`                                | 单测                                         | 修改/创建 |
| `docs/roadmap.md`                              | B4c 行                                       | 修改      |

---

## 任务 1：engineCapabilities 翻全 true

**文件：**

- 修改：`src/runtime/render-host.ts`（`engineCapabilities`）
- 修改：`src/runtime/render-host.test.ts`

- [ ] **步骤 1：改测试**

把 `render-host.test.ts` 的 `engineCapabilities` describe 改为断言四项两引擎 true：

```ts
describe("engineCapabilities", () => {
  it("enables all editing capabilities on both engines (B4c)", () => {
    for (const e of ["three.js", "babylon.js"] as const) {
      const c = engineCapabilities(e);
      expect(c.gizmo).toBe(true);
      expect(c.play).toBe(true);
      expect(c.focus).toBe(true);
      expect(c.assetDrop).toBe(true);
    }
  });
});
```

运行 `pnpm test src/runtime/render-host.test.ts` → 预期 FAIL（babylon 的 play/focus/assetDrop 现为 false）。

- [ ] **步骤 2：改实现**

`src/runtime/render-host.ts` 的 `engineCapabilities`：

```ts
export function engineCapabilities(engine: ViewportEngine): EngineCapabilities {
  void engine; // both engines reach full editing parity at B4c; kept as the
  // capability-gate seam for future v1.x engines (r3f / Unity) that may not.
  return { gizmo: true, play: true, focus: true, assetDrop: true };
}
```

- [ ] **步骤 3：验证**

运行：`pnpm test src/runtime/render-host.test.ts && pnpm typecheck`
预期：PASS。

- [ ] **步骤 4：Commit**

```bash
git add src/runtime/render-host.ts src/runtime/render-host.test.ts
git commit -m "feat(render-host): enable all editing capabilities on both engines"
```

---

## 任务 2：IRuntimeAdapter.raycastGroundPoint + Babylon 实现

**文件：**

- 修改：`src/runtime/adapter.ts`（`IRuntimeAdapter` 加方法）
- 修改：`src/runtime/babylon/adapter.ts`（实现 + import）
- 修改：`src/runtime/babylon/adapter.test.ts`

ThreeAdapter 已有 `raycastGroundPoint`（签名一致），加到接口后只需 Babylon 补实现。

- [ ] **步骤 1：编写失败的测试**

`src/runtime/babylon/adapter.test.ts` 加（需定尺寸 NullEngine + 默认相机；BabylonAdapter 构造已建 default editor camera at [4,3,4]→origin，但视口尺寸要让 createPickingRay 有效——参考现有 pick 测试怎么定尺寸，若现有 babylon adapter pick 测试在别处/conformance，可在本测试里用 `new BabylonAdapter()` 后设引擎渲染尺寸）：

```ts
import { Matrix } from "@babylonjs/core"; // 若已 import 跳过

it("raycastGroundPoint(center) hits the y=0 ground near origin", () => {
  const a = new BabylonAdapter();
  // 默认 editor camera 看向原点；渲染尺寸默认 NullEngine 512×256（见 render width/height）。
  const w = a.scene.getEngine().getRenderWidth();
  const h = a.scene.getEngine().getRenderHeight();
  const hit = a.raycastGroundPoint(w / 2, h / 2);
  expect(hit).not.toBeNull();
  expect(hit![1]).toBeCloseTo(0, 4); // on the ground plane
});

it("raycastGroundPoint returns null when the ray misses the ground (points up)", () => {
  const a = new BabylonAdapter();
  // top edge of the view points above the horizon (camera above looking down).
  const hit = a.raycastGroundPoint(0, 0);
  // either null (sky) — assert it doesn't throw and is null OR a finite hit;
  // the contract: parallel/away → null. With [4,3,4]→origin the top-left is sky.
  expect(hit).toBeNull();
});
```

> 若 `(0,0)` 实际命中地面（取决于 fov framing），换一个明确朝天的点或调整断言；以「朝天 → null」语义为准，报告实际数值。运行 `pnpm test src/runtime/babylon/adapter.test.ts` → 预期 FAIL（方法不存在）。

- [ ] **步骤 2：加接口方法**

`src/runtime/adapter.ts` 的 `IRuntimeAdapter`（`pickAt` 之后）加：

```ts
  /** Raycast the screen point against the y=0 ground plane; returns the world
   *  hit [x,y,z], or null when the ray is parallel to / points away from the
   *  plane. Used for drag-drop placement. */
  raycastGroundPoint(screen_x: number, screen_y: number): [number, number, number] | null;
```

- [ ] **步骤 3：实现 BabylonAdapter.raycastGroundPoint**

顶部 import 补 `Matrix`, `Plane`（`@babylonjs/core`；`Vector3` 已有）。在 `pickAt` 附近加：

```ts
  raycastGroundPoint(
    screen_x: number,
    screen_y: number,
  ): [number, number, number] | null {
    const camera = this.scene.activeCamera;
    if (!camera) return null;
    const ray = this.scene.createPickingRay(
      screen_x,
      screen_y,
      Matrix.IdentityReadOnly,
      camera,
    );
    const t = ray.intersectsPlane(
      Plane.FromPositionAndNormal(Vector3.Zero(), Vector3.Up()),
    );
    if (t === null || t < 0) return null;
    const p = ray.origin.add(ray.direction.scale(t));
    return [p.x, p.y, p.z];
  }
```

- [ ] **步骤 4：验证**

运行：`pnpm test src/runtime/babylon/adapter.test.ts && pnpm typecheck`
预期：PASS（typecheck 也确认 IRuntimeAdapter 新方法两引擎都实现了——ThreeAdapter 已有、Babylon 新加）。

- [ ] **步骤 5：Commit**

```bash
git add src/runtime/adapter.ts src/runtime/babylon/adapter.ts src/runtime/babylon/adapter.test.ts
git commit -m "feat(adapter): raycastGroundPoint contract + Babylon implementation"
```

---

## 任务 3：BabylonRenderHost setFrameCallback + focusCamera

**文件：**

- 修改：`src/runtime/babylon/render-host.ts`
- 修改：`src/runtime/babylon/render-host.test.ts`

- [ ] **步骤 1：编写失败的测试**

`src/runtime/babylon/render-host.test.ts` 加（用现有 `makeHost()` NullEngine fixture）：

```ts
import { Vector3 } from "@babylonjs/core";

it("focusCamera sets the ArcRotate target + radius", () => {
  const { host } = makeHost();
  host.mount(document.createElement("canvas"));
  host.focusCamera(new Vector3(1, 2, 3), 7);
  const cam = host.adapter.scene.activeCamera as ArcRotateCamera;
  expect(cam.target.x).toBeCloseTo(1);
  expect(cam.target.y).toBeCloseTo(2);
  expect(cam.target.z).toBeCloseTo(3);
  expect(cam.radius).toBeCloseTo(7);
  host.dispose();
});

it("setFrameCallback fires each rendered frame with seconds delta", () => {
  const { host } = makeHost();
  host.mount(document.createElement("canvas"));
  let calls = 0;
  let lastDt = -1;
  host.setFrameCallback((dt) => {
    calls++;
    lastDt = dt;
  });
  host.start();
  host.adapter.scene.render(); // force a frame
  host.stop();
  expect(calls).toBeGreaterThan(0);
  expect(lastDt).toBeGreaterThanOrEqual(0); // seconds (ms/1000)
  host.dispose();
});
```

> `ArcRotateCamera` 已在该测试文件 import（B1 测试用过）。`makeHost` 返回 `{host, engine}`。运行 → 预期 FAIL（方法不存在）。

- [ ] **步骤 2：加字段 + 方法 + start tick + dispose**

`render-host.ts` 字段区（highlight 附近）加：

```ts
  private frameCb: ((dt: number) => void) | null = null;
```

`start()` 改为帧 tick：

```ts
  start(): void {
    const engine = this.babylonEngine;
    const scene = this.adapterInstance?.scene;
    if (!engine || !scene) return;
    engine.runRenderLoop(() => {
      this.frameCb?.(engine.getDeltaTime() / 1000); // ms → seconds (tickBehaviors expects seconds)
      scene.render();
    });
  }
```

加方法（gizmo 方法附近）：

```ts
  /** Engine-specific surface — per-frame hook so the viewport can tick play
   *  behaviors without the host knowing about play state (mirrors
   *  ThreeRenderHost.setFrameCallback). */
  setFrameCallback(cb: ((dt: number) => void) | null): void {
    this.frameCb = cb;
  }

  /** Engine-specific surface — F-focus moves the ArcRotate camera to center
   *  `target` at `distance` (mirrors ThreeRenderHost.focusCamera). */
  focusCamera(target: Vector3, distance: number): void {
    const cam = this.camera;
    if (!cam) return;
    cam.setTarget(target);
    cam.radius = distance;
  }
```

`dispose()` 加 `this.frameCb = null;`（在清 gizmo 回调附近）。

- [ ] **步骤 3：验证**

运行：`pnpm test src/runtime/babylon/render-host.test.ts && pnpm typecheck`
预期：PASS。

- [ ] **步骤 4：Commit**

```bash
git add src/runtime/babylon/render-host.ts src/runtime/babylon/render-host.test.ts
git commit -m "feat(babylon): render host setFrameCallback + focusCamera"
```

---

## 任务 4：babylon/focus-helpers.ts（Babylon focus 目标计算）— TDD

**文件：**

- 创建：`src/runtime/babylon/focus-helpers.ts`
- 测试：`src/runtime/babylon/focus-helpers.test.ts`

镜像 `src/ui/viewport/focus-helpers.computeFocusTarget`（Three 版：obj null→origin/distance 5；否则 bbox center + distance=max(maxSize\*2, 0.2)），用 Babylon `getHierarchyBoundingVectors`。

- [ ] **步骤 1：编写失败的测试**

```ts
// src/runtime/babylon/focus-helpers.test.ts
import { describe, expect, it } from "vitest";
import { NullEngine, Scene, MeshBuilder, Vector3 } from "@babylonjs/core";
import { computeBabylonFocusTarget } from "./focus-helpers";

function scene() {
  return new Scene(new NullEngine());
}

describe("computeBabylonFocusTarget", () => {
  it("null node → origin at default distance 5", () => {
    const r = computeBabylonFocusTarget(null);
    expect(r.target.equals(Vector3.Zero())).toBe(true);
    expect(r.distance).toBeCloseTo(5);
  });

  it("centers on a node's bbox; distance = max(size)*2 clamped to 0.2", () => {
    const s = scene();
    const box = MeshBuilder.CreateBox("b", { size: 2 }, s);
    box.position.set(10, 0, 0);
    box.computeWorldMatrix(true);
    const r = computeBabylonFocusTarget(box);
    expect(r.target.x).toBeCloseTo(10);
    expect(r.distance).toBeCloseTo(4); // size 2 → max 2 → *2 = 4
    s.getEngine().dispose();
  });
});
```

运行 → 预期 FAIL（模块不存在）。

- [ ] **步骤 2：实现 focus-helpers.ts**

```ts
// src/runtime/babylon/focus-helpers.ts
import { Vector3, type Node as BabylonNode } from "@babylonjs/core";

const MIN_DISTANCE = 0.2;
const DEFAULT_DISTANCE = 5;

/** Babylon counterpart of viewport/focus-helpers.computeFocusTarget: null →
 *  origin at default distance; else center on the node's world bounding box
 *  with distance = max(size)*2 clamped to MIN_DISTANCE. */
export function computeBabylonFocusTarget(node: BabylonNode | null): {
  target: Vector3;
  distance: number;
} {
  if (!node) return { target: Vector3.Zero(), distance: DEFAULT_DISTANCE };
  const tn = node as BabylonNode & {
    getHierarchyBoundingVectors?: () => { min: Vector3; max: Vector3 };
  };
  if (typeof tn.getHierarchyBoundingVectors !== "function") {
    return { target: Vector3.Zero(), distance: DEFAULT_DISTANCE };
  }
  const { min, max } = tn.getHierarchyBoundingVectors();
  const center = min.add(max).scale(0.5);
  const size = max.subtract(min);
  const maxSize = Math.max(size.x, size.y, size.z, 0);
  return { target: center, distance: Math.max(maxSize * 2, MIN_DISTANCE) };
}
```

- [ ] **步骤 3：验证**

运行：`pnpm test src/runtime/babylon/focus-helpers.test.ts && pnpm typecheck`
预期：PASS。

- [ ] **步骤 4：Commit**

```bash
git add src/runtime/babylon/focus-helpers.ts src/runtime/babylon/focus-helpers.test.ts
git commit -m "feat(babylon): computeBabylonFocusTarget (bbox center + distance)"
```

---

## 任务 5：conformance raycastGroundPoint 对等

**文件：**

- 修改：`src/runtime/conformance/conformance-suite.ts`

`raycastGroundPoint` 是 `IRuntimeAdapter` 方法（任务 2），加进 `makePickAdapter` describe 块（定 800×600 视口 + 默认相机 [4,3,4]→origin）。两引擎参数化跑。相机看向原点 → 屏幕中心 ray 命中 y=0 于原点 [0,0,0]。

- [ ] **步骤 1：加对等用例**

先**读 `conformance-suite.ts` 的 `makePickAdapter` describe 块**（约 :387+）确认 `makePick()` 工厂 + 视口尺寸（Three setViewportSize / Babylon sized NullEngine）+ 屏幕坐标怎么传（pick 用 400/300 中心、某 sky corner）。在 pick 用例之后加：

```ts
        it("raycastGroundPoint(center) hits the ground at the look-at origin", () => {
          const a = makePick();
          const hit = a.raycastGroundPoint(400, 300); // 800×600 center
          expect(hit).not.toBeNull();
          expect(hit![0]).toBeCloseTo(0, 1);
          expect(hit![1]).toBeCloseTo(0, 1);
          expect(hit![2]).toBeCloseTo(0, 1);
        });

        it("raycastGroundPoint(sky) returns null", () => {
          const a = makePick();
          // same sky-pointing screen point the pick test uses for "corner sky".
          const hit = a.raycastGroundPoint(/* sky corner x */, /* y */);
          expect(hit).toBeNull();
        });
```

> sky 点用 pick 测试里「corner sky returns null」那条用的同坐标（读出来填上）。center 命中原点的理由：默认相机 lookAt 原点（在 y=0 平面上），中心 ray = 视线方向 → 命中原点。两引擎相机 framing 一致（B2 已对齐），故两引擎都得 [≈0,0,0]。

- [ ] **步骤 2：验证（两引擎都过）**

运行：`pnpm test src/runtime/conformance/ && pnpm typecheck`
预期：raycastGroundPoint 对等用例对 Three + Babylon 都过。若某引擎 center 不是 [0,0,0] 或 sky 不是 null，停下报告实际值（别放宽容差掩盖）。

- [ ] **步骤 3：Commit**

```bash
git add src/runtime/conformance/conformance-suite.ts
git commit -m "test(conformance): raycastGroundPoint parity across engines"
```

---

## 任务 6：BabylonViewport 接 play + focus + drop

**文件：**

- 修改：`src/ui/viewport/BabylonViewport.tsx`

镜像 ThreeViewport 的 play（:238-282）/ focus effect / drop（:206-235）。需加 `hostRef`（focus effect 跨 effect 访问 host）。

- [ ] **步骤 1：加 import + hostRef**

import 补：

```ts
import { AddNodeCommand } from "@/core/command/commands/add-node";
import { dropPositionFor } from "@/lib/drop-helpers";
import { findLibraryItem } from "@/services/library/catalog";
import { captureTransform } from "@/runtime/babylon/transform-util";
import { computeBabylonFocusTarget } from "@/runtime/babylon/focus-helpers";
import type { Node as BabylonNode } from "@babylonjs/core";
```

组件内加 ref + 订阅 focus 状态：

```ts
const hostRef = useRef<BabylonRenderHost | null>(null);
const pendingFocusNodeId = useUIStore((s) => s.pendingFocusNodeId);
const consumeFocusRequest = useUIStore((s) => s.consumeFocusRequest);
```

mount effect 内 `host.mount(canvas)` 后设 `hostRef.current = host;`，cleanup 里 `hostRef.current = null;`。

- [ ] **步骤 2：drop 接线（mount effect 内，host.start 之前/之后均可，listener 在 window）**

镜像 ThreeViewport :206-235：

```ts
const onAssetDrop = (event: PointerEvent) => {
  const dragId = useUIStore.getState().assetDragItemId;
  if (!dragId) return;
  const rect = canvas.getBoundingClientRect();
  const x = event.clientX - rect.left;
  const y = event.clientY - rect.top;
  const inside = x >= 0 && y >= 0 && x <= rect.width && y <= rect.height;
  if (inside) {
    const uploads = useSceneStore.getState().project?.assets ?? [];
    const item = findLibraryItem(dragId, uploads);
    if (item) {
      const node = item.makeNode();
      const hit = adapter.raycastGroundPoint(x, y);
      if (hit) node.transform.position = dropPositionFor(node.transform.position, hit);
      executeCommand(new AddNodeCommand({ node }));
      useUIStore.getState().setSelectedNodeId(node.id);
    }
  }
  useUIStore.getState().endAssetDrag();
};
window.addEventListener("pointerup", onAssetDrop);
```

cleanup 加 `window.removeEventListener("pointerup", onAssetDrop);`。

- [ ] **步骤 3：play 接线（mount effect 内）**

镜像 ThreeViewport :238-282（用 babylon captureTransform + Babylon node transform 恢复）：

```ts
const transformSnapshots = new Map<string, ReturnType<typeof captureTransform>>();
let playing = false;

const enterPlay = () => {
  const project = useSceneStore.getState().project;
  if (!project) return;
  transformSnapshots.clear();
  for (const node of Object.values(project.scene.nodes)) {
    const obj = adapter.getRuntimeObject(node.id) as BabylonNode | undefined;
    if (obj) transformSnapshots.set(node.id, captureTransform(obj));
    if (node.behaviors.length > 0) adapter.installBehaviors(node.id, node.behaviors);
  }
  host.setGizmoTarget(null, false);
  host.setSelection(null);
  playing = true;
};

const exitPlay = () => {
  playing = false;
  const project = useSceneStore.getState().project;
  if (project) {
    for (const node of Object.values(project.scene.nodes))
      adapter.uninstallBehaviors(node.id);
  }
  for (const [nodeId, t] of transformSnapshots) {
    const obj = adapter.getRuntimeObject(nodeId) as
      | (BabylonNode & {
          position?: Vector3;
          rotationQuaternion?: Quaternion | null;
          scaling?: Vector3;
        })
      | undefined;
    if (!obj) continue;
    obj.position?.set(t.position[0], t.position[1], t.position[2]);
    if (obj.rotationQuaternion)
      obj.rotationQuaternion.set(
        t.rotation[0],
        t.rotation[1],
        t.rotation[2],
        t.rotation[3],
      );
    obj.scaling?.set(t.scale[0], t.scale[1], t.scale[2]);
  }
  transformSnapshots.clear();
  host.setSelection(useUIStore.getState().selectedNodeId);
  syncGizmoTarget(useUIStore.getState().selectedNodeId);
};

host.setFrameCallback((dt) => {
  if (playing) adapter.tickBehaviors(dt);
});
```

（import `Vector3`, `Quaternion` from `@babylonjs/core` 供恢复类型标注。`captureTransform` 返回 `Transform`，可直接用 `Transform` 类型替代 `ReturnType<...>`——从 `@/core/scene/types` import `Transform`。）

在现有 `unsubscribeUI`（:130-138）的回调里加 playState 分支：

```ts
if (state.playState !== prev.playState) {
  if (state.playState === "play") enterPlay();
  else exitPlay();
}
```

- [ ] **步骤 4：focus effect（组件内、mount effect 外，第二个 useEffect）**

镜像 ThreeViewport 的 focus effect：

```ts
useEffect(() => {
  if (pendingFocusNodeId === undefined) return;
  const host = hostRef.current;
  if (!host) {
    consumeFocusRequest();
    return;
  }
  const obj =
    pendingFocusNodeId === null
      ? null
      : ((host.adapter.getRuntimeObject(pendingFocusNodeId) as
          | BabylonNode
          | undefined) ?? null);
  const { target, distance } = computeBabylonFocusTarget(obj);
  host.focusCamera(target, distance);
  consumeFocusRequest();
}, [pendingFocusNodeId, consumeFocusRequest]);
```

- [ ] **步骤 5：验证**

运行：`pnpm typecheck && pnpm lint && pnpm test`
预期：全绿。注意 eslint react-hooks（勿在 effect body setState——本接线用 store.getState() 命令式，不引入 setState-in-effect；focus effect 的 consumeFocusRequest 是 store action 调用，与 ThreeViewport 一致）。既有 BabylonViewport 测试若断言旧结构，最小适配并说明。

- [ ] **步骤 6：Commit**

```bash
git add src/ui/viewport/BabylonViewport.tsx
git commit -m "feat(viewport): wire Babylon play + focus + drop"
```

---

## 任务 7：更新 roadmap

**文件：**

- 修改：`docs/roadmap.md`（B4c 行）

- [ ] **步骤 1：勾选 B4c**

把 `- [ ] **B4c · Babylon 视口交互剩余**...` 改为 `- [x]` 并补：

```markdown
    - [x] **B4c · Babylon 视口交互剩余**：play 行为预览 + F focus + 资源拖放落点接 BabylonViewport（镜像 ThreeViewport）；BabylonRenderHost 加 `setFrameCallback`/`focusCamera`，`raycastGroundPoint` 升 `IRuntimeAdapter`（+ conformance 对等），Babylon focus 目标计算；`engineCapabilities` 翻全 true（保留 seam）。
```

- [ ] **步骤 2：Commit**

```bash
git add docs/roadmap.md
git commit -m "docs(roadmap): B4c Babylon play+focus+drop complete"
```

---

## 任务 8：全量验证 + visual smoke

- [ ] **步骤 1：自动化全绿**

运行：`pnpm typecheck && pnpm lint && pnpm test`
预期：全 PASS。

- [ ] **步骤 2：visual smoke（`pnpm tauri dev`，Babylon 模式）**

1. 给节点加 auto-rotate/bob 行为 → **Play** → Babylon 实体转/动；**Stop** → transform 复位、选中恢复
2. 选中节点按 **F** → 相机居中到该节点
3. 从资源库**拖**几何到视口 → 落在 y=0 地面命中点
4. Play 按钮 / F / 资源拖入 在 Babylon **不再灰/无效**
5. Three 模式 play/focus/drop 零回归；console 零错

- [ ] **步骤 3：偏差则修复重跑**；全过后进入收尾。

---

## 自检记录

- **规格覆盖**：§3 host 面→任务 3；§4 raycastGroundPoint 契约+实现+conformance→任务 2+5；§5 engineCapabilities→任务 1；§6 viewport play/focus/drop→任务 6 + focus 计算→任务 4；§7 测试分布任务 1-6 + 任务 8 smoke；§9 交付物全覆盖。
- **占位符**：新代码（engineCapabilities/raycastGroundPoint/setFrameCallback/focusCamera/focus-helpers）给完整代码；conformance sky 坐标 + BabylonViewport 镜像段让实现者对齐现有（pick 测试 sky 坐标、ThreeViewport 行号），非编造——各 fixture/行号以实际为准。
- **类型一致**：`raycastGroundPoint` 签名（adapter.ts 契约）在任务 2 Babylon 实现、任务 5 conformance、任务 6 drop 调用一致；`setFrameCallback`/`focusCamera`（任务 3）在任务 6 用；`computeBabylonFocusTarget`（任务 4）在任务 6 用；`captureTransform`（babylon/transform-util，B3b）在任务 6 用；`Transform`（core/scene/types）快照类型。
- **顺序依赖**：2(raycast)→5(conformance)+6(drop)；3(host 面)→6；4(focus 计算)→6。任务编号即执行序。
