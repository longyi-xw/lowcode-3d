# v1.0 B4d — Babylon 渲染观感对齐 + 双引擎 IBL 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 消除 Babylon 与 Three 的渲染观感分歧（底色 sRGB、灯光属性未应用），给两引擎一致补中性 IBL，并收尾 B4b 遗留的 Babylon rotate/scale 拖拽 socket-marker 实时跟随。

**架构：** 五块独立改动各落对应 host/adapter：① Babylon `applyLightData` 应用节点 intensity+color（关闭 pre-existing gap）；② Babylon 整帧 image-processing 做 sRGB（对齐 Three `OutputPass`）；③ Babylon `.env` → `scene.environmentTexture`；④ Three `RoomEnvironment`+PMREM → `scene.environment`；⑤ Babylon rotate/scale gizmo 接 `onDragObservable` → `rebuildSocketMarkers`。

**技术栈：** Three.js 0.184（`RoomEnvironment`/`PMREMGenerator`）/ Babylon `@babylonjs/core` 9.11（`ImageProcessingConfiguration`+`ImageProcessingPostProcess`、`CubeTexture.CreateFromPrefilteredData`、`Light.diffuse/intensity`、`GizmoManager`）；Vitest+NullEngine（接缝/灯光/marker 单测）；`pnpm tauri dev` 视觉 smoke（保真）。规格 `docs/superpowers/specs/2026-06-23-v1.0b4d-rendering-fidelity-design.md`。

**已 de-risk（planning-phase node 实验已验，文件已删）：** `imageProcessingConfiguration.applyByPostProcess` 可设 ✓；`.env` `CubeTexture` 在 NullEngine 不抛、`environmentTexture` 非空 ✓；`gizmos.rotationGizmo`/`scaleGizmo` 都有 `onDragObservable` ✓。`.env` 资产已 curl 到 `src/assets/env/neutral.env`（273KB，未 ignore，待 Task 3 commit）。

**测试策略（非对称）：** Babylon host/adapter 走 NullEngine 单测（灯光/sRGB 接缝/IBL 接缝/marker）；Three IBL 无 headless（`PMREMGenerator` 需 WebGL）→ smoke only；视觉保真 → 用户 smoke。

---

## 文件结构

- 改 `src/runtime/babylon/adapter.ts` — `applyLightData`（intensity+color）+ `BABYLON_LIGHT_SCALE` + createLight/update 两处调用。
- 改 `src/runtime/babylon/render-host.ts` — sRGB image-processing post-process + `.env` IBL + rotate/scale marker 跟随 + dispose 清理。
- 改 `src/runtime/three/render-host.ts` — `RoomEnvironment`+PMREM → `scene.environment` + dispose。
- 新 `src/assets/env/neutral.env` — 中性 studio IBL（已 curl）。
- 改 `src/runtime/babylon/adapter.test.ts` / `render-host.test.ts` — 灯光/sRGB/IBL/marker 接缝单测。
- 改 `docs/roadmap.md` — 勾 B4d。

---

### 任务 1：Babylon 应用灯光 intensity + color（关闭 gap）

**文件：** 修改 `src/runtime/babylon/adapter.ts`；测试 `src/runtime/babylon/adapter.test.ts`

- [ ] **步骤 1：编写失败的测试**

`src/runtime/babylon/adapter.test.ts` 末尾（最后一个 `});` 之前）加：

```ts
it("applies node intensity and color to a Babylon light (B4d)", () => {
  const a = new BabylonAdapter();
  a.syncNode(
    {
      id: "L1",
      name: "L1",
      type: "light",
      data: {
        type: "light",
        light_kind: "directional",
        color: "#ff0000",
        intensity: 1.5,
      },
      transform: {
        position: [0, 0, 0],
        rotation: [0, 0, 0, 1],
        scale: [1, 1, 1],
      },
      parent_id: null,
      children_ids: [],
      visible: true,
      locked: false,
      behaviors: [],
      user_data: {},
    } as never,
    "add",
  );
  const light = a.getRuntimeObject("L1") as unknown as {
    intensity: number;
    diffuse: { r: number; g: number; b: number };
  };
  expect(light.intensity).toBeCloseTo(1.5); // 1.5 * BABYLON_LIGHT_SCALE(1.0)
  expect(light.diffuse.r).toBeCloseTo(1);
  expect(light.diffuse.g).toBeCloseTo(0);
  expect(light.diffuse.b).toBeCloseTo(0);
});
```

- [ ] **步骤 2：运行验证失败**

运行：`pnpm vitest run src/runtime/babylon/adapter.test.ts`
预期：FAIL（`intensity` 为默认 1（非 1.5）/ `diffuse` 为默认白）。

- [ ] **步骤 3：实现**

`src/runtime/babylon/adapter.ts`：

1. import 区把 `@babylonjs/core` 补 `Color3, Light`（与现有 `DirectionalLight` 等并列）。
2. 在 `createLight` 函数**之前**加常量 + helper：

```ts
/** Babylon-local brightness factor applied on top of the node's intensity to
 *  match Three's lighting. Initial 1.0; smoke-tuned after sRGB + IBL land
 *  (B4d). Babylon PBR vs Three physical-light units may differ at equal
 *  intensity — this is the single knob to reconcile them. */
const BABYLON_LIGHT_SCALE = 1.0;

/** Apply engine-neutral light node data (intensity + color) onto a Babylon
 *  Light, mirroring Three's node-builders/light.ts (color + intensity on
 *  create and update). Babylon's createLight only default-constructs, so
 *  without this the node's intensity/color are ignored (pre-B4d gap). */
function applyLightData(
  light: Light,
  data: Extract<NodeData, { type: "light" }>,
): void {
  light.intensity = data.intensity * BABYLON_LIGHT_SCALE;
  light.diffuse = Color3.FromHexString(data.color);
}
```

3. 重构 `createLight` 为先建后应用再返回：

```ts
function createLight(
  name: string,
  data: Extract<NodeData, { type: "light" }>,
  scene: Scene,
): BabylonNode {
  let light: Light;
  switch (data.light_kind) {
    case "directional":
      light = new DirectionalLight(name, new Vector3(0, -1, 0), scene);
      break;
    case "point":
      light = new PointLight(name, Vector3.Zero(), scene);
      break;
    case "spot":
      light = new SpotLight(
        name,
        Vector3.Zero(),
        new Vector3(0, -1, 0),
        Math.PI / 4,
        1,
        scene,
      );
      break;
    case "ambient":
      light = new HemisphericLight(name, new Vector3(0, 1, 0), scene);
      break;
  }
  applyLightData(light, data);
  return light;
}
```

4. 在 `syncNode` 的 update 分支（`applyBabylonTransform(existing, node);` 之后）加：

```ts
if (node.data.type === "light") applyLightData(existing as Light, node.data);
```

- [ ] **步骤 4：运行验证通过**

运行：`pnpm vitest run src/runtime/babylon/adapter.test.ts`
预期：PASS。

- [ ] **步骤 5：验证 + Commit**

运行 `pnpm typecheck && pnpm lint`（净）。

```bash
git add src/runtime/babylon/adapter.ts src/runtime/babylon/adapter.test.ts
git commit -m "feat(babylon): apply light intensity + color from node data (B4d)"
```

---

### 任务 2：Babylon sRGB 整帧 image-processing

**文件：** 修改 `src/runtime/babylon/render-host.ts`；测试 `src/runtime/babylon/render-host.test.ts`

- [ ] **步骤 1：编写失败的测试**

`src/runtime/babylon/render-host.test.ts`，在 `describe("engine-specific surface (v1.0 B4c)", ...)` 之前新增：

```ts
describe("rendering fidelity (v1.0 B4d)", () => {
  it("mount enables whole-frame sRGB image processing", () => {
    const { host } = makeHost();
    host.mount(document.createElement("canvas"));
    const ip = host.adapter.scene.imageProcessingConfiguration;
    expect(ip.isEnabled).toBe(true);
    expect(ip.applyByPostProcess).toBe(true);
    host.dispose();
  });
});
```

- [ ] **步骤 2：运行验证失败**

运行：`pnpm vitest run src/runtime/babylon/render-host.test.ts`
预期：FAIL（`applyByPostProcess` 默认 false）。

- [ ] **步骤 3：实现**

`src/runtime/babylon/render-host.ts`：

1. import 区把 `@babylonjs/core` 补 `ImageProcessingPostProcess`。
2. 加私有字段（在 `private socketMarkers` 附近）：`private imageProcessing: ImageProcessingPostProcess | null = null;`
3. mount 内，`this.camera = camera;` 之后插入：

```ts
// sRGB output (B4d) — match Three's OutputPass, which sRGB-encodes the whole
// frame (incl. the dark clearColor). applyByPostProcess=true moves image
// processing to a fullscreen post-process so the background gets the gamma
// curve too AND materials defer to it (no double-encode). NoToneMapping
// (Babylon default toneMappingEnabled=false) matches Three's default.
const ip = scene.imageProcessingConfiguration;
ip.isEnabled = true;
ip.applyByPostProcess = true;
this.imageProcessing = new ImageProcessingPostProcess("imageProcessing", 1.0, camera);
```

4. dispose 内（`this.gizmoManager?.dispose();` 之前）加：`this.imageProcessing?.dispose(); this.imageProcessing = null;`

- [ ] **步骤 4：运行验证通过**

运行：`pnpm vitest run src/runtime/babylon/render-host.test.ts`
预期：PASS。

- [ ] **步骤 5：验证 + Commit**

运行 `pnpm typecheck && pnpm lint`（净）。

```bash
git add src/runtime/babylon/render-host.ts src/runtime/babylon/render-host.test.ts
git commit -m "feat(babylon): whole-frame sRGB image processing (match Three OutputPass, B4d)"
```

---

### 任务 3：Babylon `.env` IBL

**文件：** 修改 `src/runtime/babylon/render-host.ts`；新 `src/assets/env/neutral.env`（已 curl，本任务 commit）；测试 `src/runtime/babylon/render-host.test.ts`

- [ ] **步骤 1：编写失败的测试**

`render-host.test.ts` 的 `describe("rendering fidelity (v1.0 B4d)", ...)` 内加：

```ts
it("mount sets a neutral IBL environment texture", () => {
  const { host } = makeHost();
  host.mount(document.createElement("canvas"));
  expect(host.adapter.scene.environmentTexture).not.toBeNull();
  host.dispose();
});
```

- [ ] **步骤 2：运行验证失败**

运行：`pnpm vitest run src/runtime/babylon/render-host.test.ts`
预期：FAIL（`environmentTexture` 为 null）。

- [ ] **步骤 3：实现**

`src/runtime/babylon/render-host.ts`：

1. import 区把 `@babylonjs/core` 补 `CubeTexture`，并加资产 URL 导入（顶部 import 区）：

```ts
import neutralEnvUrl from "@/assets/env/neutral.env?url";
```

2. 加私有字段：`private envTexture: CubeTexture | null = null;`
3. mount 内（sRGB 块之后）插入：

```ts
// Neutral IBL (B4d) — both engines were env-less, so PBR metals read dark.
// A small neutral studio .env gives metals something to reflect; Three uses
// RoomEnvironment for the parallel effect. Not pixel-identical across
// engines (different convolution) — both just neutral.
this.envTexture = CubeTexture.CreateFromPrefilteredData(neutralEnvUrl, scene);
scene.environmentTexture = this.envTexture;
```

4. dispose 内（`this.imageProcessing?.dispose();` 之后）加：`this.envTexture?.dispose(); this.envTexture = null;`

- [ ] **步骤 4：运行验证通过**

运行：`pnpm vitest run src/runtime/babylon/render-host.test.ts`
预期：PASS。

- [ ] **步骤 5：构建确认（资产打包）**

运行：`pnpm build`
预期：成功，无 `.env?url` 解析错误（确认 Vite 把 `src/assets/env/neutral.env` 当 URL 资产打包）。
**若失败**（Vite 拒绝 `.env` 扩展）：以 DONE_WITH_CONCERNS 报告，备选 = 资产移到 `public/env/neutral.env` 并用 `CubeTexture.CreateFromPrefilteredData("env/neutral.env", scene)`（去掉 `?url` 导入）。

- [ ] **步骤 6：验证 + Commit**

运行 `pnpm typecheck && pnpm lint && pnpm test`（净/全绿）。

```bash
git add src/assets/env/neutral.env src/runtime/babylon/render-host.ts src/runtime/babylon/render-host.test.ts
git commit -m "feat(babylon): neutral .env IBL environment (B4d)"
```

---

### 任务 4：Three RoomEnvironment IBL（smoke-only）

**文件：** 修改 `src/runtime/three/render-host.ts`（无 headless 单测——`PMREMGenerator` 需 WebGL）

- [ ] **步骤 1：实现**

`src/runtime/three/render-host.ts`：

1. import 区加：

```ts
import { RoomEnvironment } from "three/addons/environments/RoomEnvironment.js";
```

2. mount 内，`this.renderer = renderer;` 之后、composer 链之前插入：

```ts
// Neutral IBL (B4d) — RoomEnvironment is a built-in procedural neutral room;
// PMREM convolves it into an environment map so PBR/standard metals have
// something to reflect (parallels Babylon's .env IBL). Asset-free. The PMREM
// generator + its temporary scene are one-shot — dispose right after.
const pmrem = new THREE.PMREMGenerator(renderer);
adapter.scene.environment = pmrem.fromScene(new RoomEnvironment(), 0.04).texture;
pmrem.dispose();
```

- [ ] **步骤 2：验证**

运行：`pnpm typecheck && pnpm lint && pnpm test`
预期：typecheck/lint 净、全绿（无新单测——Three host 无 headless；视觉留 smoke）。

- [ ] **步骤 3：Commit**

```bash
git add src/runtime/three/render-host.ts
git commit -m "feat(three): RoomEnvironment IBL for PBR reflections (B4d)"
```

---

### 任务 5：marker rotate/scale 跟随（B4b 收尾）

**文件：** 修改 `src/runtime/babylon/render-host.ts`；测试 `src/runtime/babylon/render-host.test.ts`

- [ ] **步骤 1：编写失败的测试**

`render-host.test.ts` 的 `describe("socket markers (v1.0 B4b)", ...)` 内加（复用其 `mountedWith`/`boxNodeWithSockets`）：

```ts
it("rebuilds markers during a rotate-gizmo drag (parity with Three objectChange)", () => {
  const host = mountedWith(
    boxNodeWithSockets("d", [0, 0, 0], [{ position: [1, 0, 0] }]),
  );
  host.setGizmoMode("rotate");
  host.setGizmoTarget("d", false);
  host.syncSocketMarkers();
  const rg = host.gizmoManagerForTest!.gizmos.rotationGizmo!;
  // observers wired by wireActiveGizmo; firing onDrag must rebuild markers.
  const before = host.socketMarkersForTest!.getChildMeshes().length;
  rg.onDragObservable.notifyObservers({} as never);
  expect(host.socketMarkersForTest!.getChildMeshes()).toHaveLength(before);
  expect(rg.onDragObservable.observers.length).toBeGreaterThan(0);
  host.dispose();
});
```

- [ ] **步骤 2：运行验证失败**

运行：`pnpm vitest run src/runtime/babylon/render-host.test.ts`
预期：FAIL（rotation gizmo `onDragObservable` 无 observer / `observers.length === 0`）。

- [ ] **步骤 3：实现**

`src/runtime/babylon/render-host.ts` 的 `wireActiveGizmo`：当前 rotate/scale 分支（`g.onDragStartObservable.add(...)` + `g.onDragEndObservable.add(...)`）补一行，使非 translate gizmo 拖拽期也重建 marker：

```ts
g.onDragObservable.add(() => this.rebuildSocketMarkers());
```

（保持 WeakSet 幂等接线不变——同一 gizmo 实例只接一次。）

- [ ] **步骤 4：运行验证通过**

运行：`pnpm vitest run src/runtime/babylon/render-host.test.ts`
预期：PASS。

- [ ] **步骤 5：验证 + Commit**

运行 `pnpm typecheck && pnpm lint`（净）。

```bash
git add src/runtime/babylon/render-host.ts src/runtime/babylon/render-host.test.ts
git commit -m "feat(babylon): socket markers follow rotate/scale gizmo drag (B4b follow-up)"
```

---

### 任务 6：roadmap 勾选 + 全量验证 + smoke 标定

**文件：** 修改 `docs/roadmap.md`

- [ ] **步骤 1：全量验证**

运行：`pnpm test && pnpm typecheck && pnpm lint`
预期：`Tests` 全 passed（620 + 任务1 的 1 + 任务2 的 1 + 任务3 的 1 + 任务5 的 1 = 624）；typecheck/lint 净。

- [ ] **步骤 2：勾选 B4d**

运行 `rg -n "B4d" docs/roadmap.md` 定位，把 `- [ ]` 改 `- [x]`，按 B4a/B4c 风格补「Babylon 渲染观感对齐 + 双引擎 IBL 完成」。只改 B4d 一行。

- [ ] **步骤 3：Commit**

```bash
git add docs/roadmap.md
git commit -m "docs(roadmap): B4d rendering fidelity + dual-engine IBL complete"
```

- [ ] **步骤 4：用户视觉 smoke（强制）+ 光强标定**

`pnpm tauri dev` 两引擎并排核对：① Babylon 底色提亮到与 Three 一致（不再偏暗）；② 灯光颜色/亮度生效且两引擎相近——**若 Babylon 仍偏亮/偏暗，调 `BABYLON_LIGHT_SCALE`（adapter.ts）或 `scene.environmentIntensity`，重跑 smoke**；③ 金属在两引擎都有环境反射且观感相近；④ rotate/scale 拖拽 marker 实时跟随；⑤ Three 无回归。标定后若改了常量，补一个 `fix(babylon): tune light scale (B4d smoke)` commit。

---

## 自检

**规格覆盖度：** Part 1 sRGB（任务 2）、Part 2 灯光 intensity+color（任务 1）、Part 3 IBL Three+Babylon（任务 3+4）、Part 4 marker 跟随（任务 5）、smoke 标定 + roadmap（任务 6）——规格各节均有任务。`.env` 资产获取已在计划前由控制者完成。

**占位符扫描：** 无 TODO；每步含完整代码或精确编辑指令。`BABYLON_LIGHT_SCALE` 初值 1.0 是明确的 smoke 标定种子，非占位符。

**类型一致性：** `applyLightData`/`BABYLON_LIGHT_SCALE`（任务1 定义↔使用）、`imageProcessing`/`envTexture`（任务2/3 字段↔dispose）、`scene.environmentTexture`/`environmentIntensity`（任务3↔任务6 标定）、`rebuildSocketMarkers`/`socketMarkersForTest`（B4b 已有↔任务5 复用）、`gizmoManagerForTest.gizmos.rotationGizmo`（任务5 测试↔de-risk 已验存在）——一致。

**非对称测试已显式标注：** 任务 4（Three IBL）无单测、走 smoke；其余 Babylon 块均有 NullEngine 单测。
