# Phase 3 · 3.5 发布流程 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法跟踪进度。

**目标：** 把当前 main（Phase 0–3 + v0.5 行为）作为 v0.1.0 正式发布：写 CHANGELOG `[0.1.0]` + 三处版本 bump → PR 合并 → 经用户确认推 `v0.1.0` tag → `release.yml` 三平台构建 + draft Release（用户 publish）。

**架构：** 纯文档/配置变更，无新增源码、无单测。验证靠 `lint/typecheck/test` 回归绿 + 版本一致性人工核对 + tag 后 `release.yml` 三平台构建成功。tag/Release 是不可逆对外动作。

**技术栈：** Keep-a-Changelog + SemVer；Tauri `release.yml`（`tauri-apps/tauri-action@v0`，`releaseDraft: true`）；gh CLI。

**前置：** spec `docs/superpowers/specs/2026-06-01-phase3-3.5-release-design.md`（commit 2612765）。分支 `feat/phase3-3.5-release`。`gh` 用 `/opt/homebrew/bin/gh`。

**起手：** `pnpm install`；`pnpm test`（基线 328 通过）。

---

## 文件结构

**修改（无新增）：**

- `CHANGELOG.md` — `[0.1.0]` 段落 + 顶部空 `[Unreleased]` + 修底部坏 compare URL
- `package.json` — `version` → `0.1.0`
- `src-tauri/tauri.conf.json` — `version` → `0.1.0`
- `src-tauri/Cargo.toml` — `[package] version` → `0.1.0`
- `src-tauri/Cargo.lock` — `lowcode-3d` crate version → `0.1.0`
- `docs/roadmap.md` — 回填 3.5 + Phase 3 勾选（PR 后）

---

## 任务 T1：CHANGELOG.md 写 [0.1.0]

**文件：** 修改 `CHANGELOG.md`

> 无测试（文档）。验证靠 markdown 正确 + prettier。

- [ ] **步骤 1：整体替换 `CHANGELOG.md` 为下面内容**

```markdown
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

## [0.1.0] - 2026-06-01

First public release — a desktop editor for authoring Three.js scenes, with a
clean on-disk project format and one-click code export.

### Added

- **Desktop app shell** — Tauri 2 + React 18 + Tailwind / shadcn UI, with
  react-i18next localization (English + 简体中文) and light / dark / system themes.
- **3D viewport** (Three.js) — orbit camera, ground grid, selection outline, and a
  transform gizmo with move / rotate / scale modes (G / R / S).
- **Scene graph** — hierarchy tree with group, mesh, light, camera, helper, and
  prefab-instance nodes; a properties panel for position, Euler-angle rotation, and
  scale; per-node lock policy; and full undo / redo command history.
- **Projects** — a file-per-node folder format with New / Open / Save / Save As /
  Close via a native File menu, dirty-state tracking, and folder-as-project-name.
  Type-safe Rust ↔ TypeScript bindings (tauri-specta).
- **glTF import** — `.glb` import backed by a content-addressed asset pipeline and
  prefab-instance nodes (shared templates, per-instance placement).
- **Code export** — emit a runnable Vite project or a single-file standalone HTML
  page, both wired with OrbitControls and a fallback light.
- **Behaviors** — a no-code behavior framework with an Auto-Rotate behavior, a
  behaviors panel to add / edit / remove bindings, an editor Play / Pause mode, and
  exported runtime tickers.
- **Editor shortcuts** — Delete / Backspace to delete, Cmd/Ctrl+D to duplicate, F to
  focus the camera on the selection, Space to toggle Play, Esc to clear the
  selection, and a `?` / Cmd+/ help dialog.
- **Project templates** — a New-project picker offering Starter scene, Single cube,
  and Empty project.
- **Error handling** — a global error boundary with a crash-recovery screen,
  success / failure toasts for project I/O, and a window-level net for uncaught
  errors and promise rejections.
- **Documentation** — scene-graph spec, Three.js adapter guide, README, and roadmap.

[Unreleased]: https://github.com/longyi-xw/lowcode-3d/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/longyi-xw/lowcode-3d/releases/tag/v0.1.0
```

> 首发只用 Added（无上一版可对比，Changed/Fixed 省略）。底部链接从坏的 `lowcode-3d/desktop` 改为 `longyi-xw/lowcode-3d`。

- [ ] **步骤 2：验证 markdown 干净**

```sh
pnpm exec prettier --check CHANGELOG.md
```

预期：`All matched files use Prettier code style!`（若报格式，`pnpm exec prettier --write CHANGELOG.md` 后再继续）。

- [ ] **步骤 3：Commit**

```sh
git add CHANGELOG.md
git commit -m "docs(changelog): v0.1.0 release notes

首个正式发布的 CHANGELOG [0.1.0] 段落(Added 主题,feature 级,覆盖
Phase 0-3 + 行为系统)，顶部留空 [Unreleased]，修底部坏的 compare
URL(lowcode-3d/desktop → longyi-xw/lowcode-3d)。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T2：三处版本 bump 0.0.1 → 0.1.0

**文件：** 修改 `package.json`、`src-tauri/tauri.conf.json`、`src-tauri/Cargo.toml`、`src-tauri/Cargo.lock`

- [ ] **步骤 1：`package.json`**

第 3 行 `"version": "0.0.1-scaffold",` → `"version": "0.1.0",`。

- [ ] **步骤 2：`src-tauri/tauri.conf.json`**

第 4 行 `"version": "0.0.1",` → `"version": "0.1.0",`。

- [ ] **步骤 3：`src-tauri/Cargo.toml`**

第 3 行 `version = "0.0.1"` → `version = "0.1.0"`（`[package]` 段，紧跟 `name = "lowcode-3d"`）。

- [ ] **步骤 4：`src-tauri/Cargo.lock`**

定位 `lowcode-3d` crate 条目（`name = "lowcode-3d"` 紧跟 `version = "0.0.1"`，约 line 1709-1710），把那条 `version = "0.0.1"` → `version = "0.1.0"`。匹配这两行块以唯一定位：

```toml
name = "lowcode-3d"
version = "0.0.1"
```

→

```toml
name = "lowcode-3d"
version = "0.1.0"
```

> 只改本工作区 crate 自身版本，不影响依赖图，手改 lock 安全（避免跑全量 cargo build）。

- [ ] **步骤 5：验证三处一致 + 无回归**

```sh
grep -n '"version"' package.json src-tauri/tauri.conf.json
grep -nE '^version' src-tauri/Cargo.toml
grep -n -A1 'name = "lowcode-3d"' src-tauri/Cargo.lock
pnpm lint && pnpm typecheck && pnpm test
```

预期：四处都是 `0.1.0`；lint/typecheck/test 全绿（基线 328，版本 bump 不影响）。

- [ ] **步骤 6：Commit**

```sh
git add package.json src-tauri/tauri.conf.json src-tauri/Cargo.toml src-tauri/Cargo.lock
git commit -m "chore(release): bump version to 0.1.0

package.json(去掉 -scaffold) / tauri.conf.json / Cargo.toml / Cargo.lock
四处同步到 0.1.0，对齐 v0.1.0 tag。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T3：验证 + PR + roadmap 回填 + merge

**文件：** 修改 `docs/roadmap.md`（CI 绿后）

- [ ] **步骤 1：全套验证**

```sh
pnpm lint && pnpm typecheck && pnpm test && pnpm exec prettier --check CHANGELOG.md
```

预期：全绿。（`format:check` 整体跑会报未跟踪的 CLAUDE.md/tests/ 噪声，故只 check CHANGELOG.md。）

- [ ] **步骤 2：push + 开 PR**

```sh
git push -u origin feat/phase3-3.5-release
/opt/homebrew/bin/gh pr create --base main --head feat/phase3-3.5-release \
  --title "chore(phase3-3.5): v0.1.0 release prep (changelog + version bump)" \
  --body "$(cat <<'EOF'
## What

Phase 3 · 3.5 发布流程 —— v0.1 收口。为 **v0.1.0** 首发做准备：

- `CHANGELOG.md`：`[0.1.0]` 段落（Added 主题、feature 级，覆盖 Phase 0–3 + v0.5 行为系统），顶部留空 `[Unreleased]`，修底部坏的 compare URL（`lowcode-3d/desktop` → `longyi-xw/lowcode-3d`）。
- 版本 bump `0.0.1` → `0.1.0`：`package.json` / `src-tauri/tauri.conf.json` / `src-tauri/Cargo.toml` / `Cargo.lock` 四处同步。

合并后将（经确认）打 `v0.1.0` tag 触发 \`release.yml\` 三平台构建 + draft Release。

## Why

- Spec: \`docs/superpowers/specs/2026-06-01-phase3-3.5-release-design.md\`
- Plan: \`docs/superpowers/plans/2026-06-01-phase3-3.5-release-plan.md\`
- Roadmap: v0.1 Phase 3 · 3.5（最后一个 sub-stage）

## How to test

- [x] \`pnpm lint\` / \`pnpm typecheck\` / \`pnpm test\`（本地绿，328）
- [x] 四处 version 一致 = 0.1.0
- [ ] CI 全绿
- [ ] merge 后推 v0.1.0 tag → release.yml 三平台构建 + draft Release（单独步骤，本 PR 不含）

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **步骤 3：CI 绿后回填 roadmap**

`docs/roadmap.md`：

- 3.5 行 `- [ ] 3.5 发布流程（…）` → `- [x] 3.5 发布流程（…）（[#NN](…/pull/NN)）`
- `- [ ] Phase 3 打磨发布` → `- [x] Phase 3 打磨发布`（5 个 sub-stage 全完成）
- 检查 v0.1.0 milestone 顶层 checkbox（若有 `- [ ] v0.1.0 …`）是否该一并勾（**注意**：v0.1.0 要等 Release 真正 publish 才算 100% 完成；这里可先不勾顶层 milestone，留到 publish 后，或在 PR 描述里说明）。

```sh
git add docs/roadmap.md
git commit -m "docs(roadmap): mark 3.5 release prep shipped (#NN); Phase 3 complete"
git push
```

- [ ] **步骤 4：CI 绿后 merge**

```sh
/opt/homebrew/bin/gh run watch <new-run-id> --exit-status   # 等 roadmap commit 的 CI
/opt/homebrew/bin/gh pr merge NN --merge --delete-branch
```

预期：PR merged，main 含 CHANGELOG + bump + roadmap。

---

## 任务 T4：打 v0.1.0 tag → release.yml → 交付 publish（⚠ 不可逆，先确认）

**文件：** 无（git tag + GitHub Release）

> ⚠ **这一步触发真实三平台编译（~10–20min）+ 创建对外 tag/draft Release。执行步骤 2 的 tag push 前，agent 必须先跟用户明确确认「现在推 v0.1.0 tag 开始构建」。**

- [ ] **步骤 1：同步本地 main**

```sh
git checkout main && git pull
git log --oneline -3   # 确认含 3.5 的 CHANGELOG + bump + merge commit
```

- [ ] **步骤 2：（确认后）打 tag + push**

```sh
git tag v0.1.0
git push origin v0.1.0
```

> tag 名 `v0.1.0` 匹配 `release.yml` 的 `v*.*.*`（且非 `v*-*`）→ 触发 Release 工作流。

- [ ] **步骤 3：观察三平台构建**

```sh
/opt/homebrew/bin/gh run list --workflow=release.yml --limit 1
/opt/homebrew/bin/gh run watch <release-run-id>
```

预期：macos / ubuntu / windows 三个 build job 全绿；生成 draft Release `lowcode-3d v0.1.0` 带各平台安装包 artifacts。

- [ ] **步骤 4：交付用户 publish**

```sh
/opt/homebrew/bin/gh release view v0.1.0   # 确认 draft 存在 + artifacts
```

告知用户：draft Release 已生成，请在 GitHub review → 把 CHANGELOG `[0.1.0]` 段落贴入 release notes（可加未签名构建首启警告说明）→ 正式 **publish**。agent 不自动 publish。

- [ ] **步骤 5：（publish 后，可选）勾顶层 v0.1.0 milestone**

Release publish 后，若 roadmap 有顶层 `- [ ] v0.1.0` milestone，单独小 commit 勾上 + 标 v0.1 达成。

**回滚**：若构建失败/draft 有误 → `git push --delete origin v0.1.0` + 在 GitHub 丢弃 draft，修复后重打 tag。publish 前都可回退。

---

## 收尾验收

满足 spec §10：CHANGELOG [0.1.0] 完整 + 链接正确 · 四处 version=0.1.0 一致 · lint/typecheck/test 绿 · 3.5 PR CI 绿 + merge · （确认后）v0.1.0 tag → release.yml 三平台绿 + draft Release 带 artifacts · 交付用户 publish。

v0.1 Phase 3（3.1–3.5）全部完成 → v0.1.0 首发达成（待用户 publish draft）。
