# Phase 3 · 3.3 错误处理 polish 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法跟踪进度。

**目标：** 三层错误网——全局 ErrorBoundary→CrashFallback 兜底渲染崩溃、sonner toast 做 IO 成功/失败反馈、window error/unhandledrejection 全局兜底；交互式确认保留 `ask()`。

**架构：** `<Toaster/>`(sonner) 挂 App 根；`<ErrorBoundary>` 在 main.tsx 包 `<App/>`，崩溃渲染 `<CrashFallback/>`；`global-handlers.ts` 模块副作用注册 window 监听 → `toast.error`；actions.ts 成功点加 `toast.success`、`reportError`/`reportRawError` 从 `ask()` 改持久 `toast.error`。i18n 复用 `errors` namespace。

**技术栈：** React 18 + sonner + Vitest + @testing-library/react + react-i18next + zustand（useSettingsStore.theme）。

**前置：** spec `docs/superpowers/specs/2026-06-01-phase3-3.3-errors-design.md`（commit 88a1377）。分支 `feat/phase3-3.3-errors`。Node 20 默认，`gh` 用 `/opt/homebrew/bin/gh`。

**起手：** `pnpm install`；`pnpm test`（基线 323 通过）。

---

## 文件结构

**新增：**

- `src/ui/error/CrashFallback.tsx` + `.test.tsx` — 崩溃兜底屏
- `src/ui/error/ErrorBoundary.tsx` + `.test.tsx` — class ErrorBoundary
- `src/services/error/global-handlers.ts` + `.test.ts` — window error/unhandledrejection → toast

**修改：**

- `package.json` + `pnpm-lock.yaml`（+ sonner）
- `src/App.tsx`（挂 `<Toaster/>`）
- `src/main.tsx`（包 `<ErrorBoundary>` + import global-handlers）
- `src/services/project/actions.ts`（IO toast 路由）
- `src/services/project/actions.test.ts`（mock sonner，保持绿）
- `src/i18n/locales/{en-US,zh-CN}/errors.json`（crash/toast/uncaught）
- `docs/roadmap.md`（PR 后回填）

---

## 任务 T1：sonner 依赖 + App 挂 Toaster

**文件：** `package.json`（+dep）；修改 `src/App.tsx`

- [ ] **步骤 1：装 sonner**

```sh
pnpm add sonner
```

- [ ] **步骤 2：App.tsx 挂 Toaster**

`src/App.tsx`：import 区加

```tsx
import { Toaster } from "sonner";
import { useSettingsStore } from "@/services/settings/store";
```

组件体顶部（`useWindowTitle();` 之后）加：

```tsx
const theme = useSettingsStore((s) => s.theme);
```

在 `return (<> … </>)` 内、`<NewProjectDialog />` 之后加：

```tsx
<Toaster richColors position="bottom-right" theme={theme} />
```

> `theme` 类型是 `"light" | "dark" | "system"`，与 sonner `theme` prop 一致。

- [ ] **步骤 3：回归验证（无独立单测——装依赖 + 挂组件靠类型/回归 + 视觉验证）**

```sh
pnpm typecheck && pnpm test
```

预期：typecheck 绿、test 仍 323 绿（无回归）。

- [ ] **步骤 4：Commit**

```sh
git add package.json pnpm-lock.yaml src/App.tsx
git commit -m "feat(ui): add sonner + mount Toaster at App root

theme 接 useSettingsStore（light/dark/system），位置 bottom-right + richColors。
后续 IO toast / 全局兜底都往这个 Toaster 推。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T2：errors.json 扩 crash / toast / uncaught keys

**文件：** 修改 `src/i18n/locales/en-US/errors.json` + `zh-CN/errors.json`

> 复用既有 `errors` ns（已在 `i18n/types.ts` 注册），加 key 不碰 config/types。后面 T3/T4/T5 的测试要用这些字串，所以先加。

- [ ] **步骤 1：en-US/errors.json**

把顶层 object 改为（在 `project_load` 之后追加 `crash`/`toast`/`uncaught`）：

```json
{
  "back_to_startup": "Back to startup",
  "project_load": {
    "title": "Failed to load project",
    "schema_mismatch": "schema_version_mismatch · expected {{expected}}, found {{found}}",
    "run_migration": "Run migration script…",
    "migration_not_implemented": "Migration tooling lands in v0.2",
    "try_again": "Try again",
    "report_issue": "report issue → github.com/longyi-xw/lowcode-3d/issues"
  },
  "crash": {
    "title": "Something went wrong",
    "message_hint": "The app hit an unexpected error. Reload to recover.",
    "reload": "Reload app",
    "copy": "Copy error"
  },
  "toast": {
    "saved": "Saved",
    "opened": "Opened {{name}}",
    "imported": "Imported {{name}}",
    "exported": "Exported to {{path}}"
  },
  "uncaught": "An unexpected error occurred"
}
```

- [ ] **步骤 2：zh-CN/errors.json**

对应中文（保持与 en 相同的 key 形状）。先读现有 `zh-CN/errors.json` 的 `back_to_startup`/`project_load` 原文不动，仅追加：

```json
  "crash": {
    "title": "应用出错了",
    "message_hint": "应用遇到未预期的错误，重载即可恢复。",
    "reload": "重载应用",
    "copy": "复制错误"
  },
  "toast": {
    "saved": "已保存",
    "opened": "已打开 {{name}}",
    "imported": "已导入 {{name}}",
    "exported": "已导出到 {{path}}"
  },
  "uncaught": "发生了未预期的错误"
```

- [ ] **步骤 3：验证**

```sh
pnpm typecheck && pnpm test
```

预期：绿（en-US/errors.json 是 `types.ts` 的类型源，加 key 即扩 `errors` ns 的可用 key）。

- [ ] **步骤 4：Commit**

```sh
git add src/i18n/locales/en-US/errors.json src/i18n/locales/zh-CN/errors.json
git commit -m "feat(i18n): errors.crash / errors.toast / errors.uncaught keys

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T3：ErrorBoundary + CrashFallback

**文件：** 创建 `src/ui/error/CrashFallback.tsx` + `.test.tsx`、`src/ui/error/ErrorBoundary.tsx` + `.test.tsx`；修改 `src/main.tsx`

- [ ] **步骤 1：写失败测试 `CrashFallback.test.tsx`**

```tsx
import { render, screen } from "@testing-library/react";
import { describe, expect, it } from "vitest";

import { CrashFallback } from "./CrashFallback";

describe("CrashFallback", () => {
  it("renders the error message + reload button", () => {
    render(<CrashFallback error={new Error("kaboom")} />);
    expect(screen.getByText(/kaboom/)).toBeInTheDocument();
    expect(screen.getByRole("button", { name: /重载|reload/i })).toBeInTheDocument();
  });
});
```

- [ ] **步骤 2：跑确认失败**

```sh
pnpm vitest run src/ui/error/CrashFallback.test.tsx
```

预期：FAIL（找不到组件）。

- [ ] **步骤 3：实现 `CrashFallback.tsx`**

```tsx
import { useTranslation } from "react-i18next";
import { AlertTriangle } from "lucide-react";

export function CrashFallback({ error }: { error: Error | null }) {
  const { t } = useTranslation("errors");
  const detail = error?.stack ?? error?.message ?? "";
  return (
    <section className="flex h-screen w-screen items-center justify-center overflow-auto bg-background text-foreground">
      <div className="w-full max-w-xl px-6 text-center">
        <div className="mx-auto mb-6 flex h-14 w-14 items-center justify-center rounded-full border-2 border-destructive/60 text-destructive">
          <AlertTriangle className="h-7 w-7" />
        </div>
        <h2 className="text-base font-semibold">{t("crash.title")}</h2>
        <p className="mt-2 text-sm text-muted-foreground">{t("crash.message_hint")}</p>
        {error?.message && (
          <pre className="mt-5 max-h-48 overflow-auto rounded-md border border-border bg-card p-3 text-left font-mono text-[11px] leading-relaxed text-muted-foreground">
            {error.message}
          </pre>
        )}
        <div className="mt-6 flex items-center justify-center gap-3">
          <button
            type="button"
            onClick={() => location.reload()}
            className="rounded-md bg-primary px-3 py-1.5 text-sm text-primary-foreground hover:bg-primary/90"
          >
            {t("crash.reload")}
          </button>
          <button
            type="button"
            onClick={() => void navigator.clipboard?.writeText(detail)}
            className="rounded-md border border-border bg-background px-3 py-1.5 text-sm text-muted-foreground hover:text-foreground"
          >
            {t("crash.copy")}
          </button>
        </div>
      </div>
    </section>
  );
}
```

- [ ] **步骤 4：跑确认通过**

```sh
pnpm vitest run src/ui/error/CrashFallback.test.tsx
```

预期：PASS。

- [ ] **步骤 5：写失败测试 `ErrorBoundary.test.tsx`**

```tsx
import { render, screen } from "@testing-library/react";
import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";

import { ErrorBoundary } from "./ErrorBoundary";

function Boom(): never {
  throw new Error("kaboom");
}

describe("ErrorBoundary", () => {
  beforeEach(() => {
    vi.spyOn(console, "error").mockImplementation(() => {});
  });
  afterEach(() => {
    vi.restoreAllMocks();
  });

  it("renders children when no error", () => {
    render(
      <ErrorBoundary>
        <p>safe content</p>
      </ErrorBoundary>,
    );
    expect(screen.getByText("safe content")).toBeInTheDocument();
  });

  it("renders CrashFallback when a child throws", () => {
    render(
      <ErrorBoundary>
        <Boom />
      </ErrorBoundary>,
    );
    expect(screen.getByRole("button", { name: /重载|reload/i })).toBeInTheDocument();
    expect(screen.getByText(/kaboom/)).toBeInTheDocument();
  });
});
```

- [ ] **步骤 6：跑确认失败**

```sh
pnpm vitest run src/ui/error/ErrorBoundary.test.tsx
```

预期：FAIL（找不到组件）。

- [ ] **步骤 7：实现 `ErrorBoundary.tsx`**

```tsx
import { Component, type ErrorInfo, type ReactNode } from "react";

import { CrashFallback } from "./CrashFallback";

interface Props {
  children: ReactNode;
}
interface State {
  error: Error | null;
}

export class ErrorBoundary extends Component<Props, State> {
  state: State = { error: null };

  static getDerivedStateFromError(error: Error): State {
    return { error };
  }

  componentDidCatch(error: Error, info: ErrorInfo): void {
    console.error("[ErrorBoundary]", error, info.componentStack);
  }

  render(): ReactNode {
    if (this.state.error) {
      return <CrashFallback error={this.state.error} />;
    }
    return this.props.children;
  }
}
```

- [ ] **步骤 8：跑确认通过**

```sh
pnpm vitest run src/ui/error/ErrorBoundary.test.tsx
```

预期：PASS（2 tests）。

- [ ] **步骤 9：main.tsx 包裹**

`src/main.tsx`：import 区加 `import { ErrorBoundary } from "./ui/error/ErrorBoundary";`，render 改为：

```tsx
createRoot(container).render(
  <StrictMode>
    <ErrorBoundary>
      <App />
    </ErrorBoundary>
  </StrictMode>,
);
```

- [ ] **步骤 10：Commit**

```sh
git add src/ui/error/ src/main.tsx
git commit -m "feat(error): global ErrorBoundary + CrashFallback

render 崩溃被 ErrorBoundary 捕获 → CrashFallback(图标+标题+message+重载/复制)，
不再白屏。main.tsx 用它包 <App/>。ErrorView(项目加载错误页)原样不动。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T4：global-handlers（window error / unhandledrejection → toast）

**文件：** 创建 `src/services/error/global-handlers.ts` + `.test.ts`；修改 `src/main.tsx`

- [ ] **步骤 1：写失败测试 `global-handlers.test.ts`**

```ts
import { afterEach, describe, expect, it, vi } from "vitest";

vi.mock("sonner", () => ({ toast: { error: vi.fn(), success: vi.fn() } }));

import { toast } from "sonner";

import { installGlobalErrorHandlers } from "./global-handlers";

describe("installGlobalErrorHandlers", () => {
  afterEach(() => vi.clearAllMocks());

  it("toasts on window error event", () => {
    installGlobalErrorHandlers(); // idempotent
    window.dispatchEvent(new ErrorEvent("error", { message: "boom" }));
    expect(toast.error).toHaveBeenCalled();
  });

  it("toasts on unhandledrejection", () => {
    installGlobalErrorHandlers();
    window.dispatchEvent(new Event("unhandledrejection"));
    expect(toast.error).toHaveBeenCalled();
  });
});
```

- [ ] **步骤 2：跑确认失败**

```sh
pnpm vitest run src/services/error/global-handlers.test.ts
```

预期：FAIL（找不到模块）。

- [ ] **步骤 3：实现 `global-handlers.ts`**

```ts
import { toast } from "sonner";

import i18n from "@/i18n";

let installed = false;

/**
 * Registers window-level handlers for errors that escape React's render
 * tree (async throws, rejected promises). Non-fatal: logs + shows an error
 * toast. Render-tree crashes are handled by ErrorBoundary instead.
 * Idempotent — safe to call more than once.
 */
export function installGlobalErrorHandlers(): void {
  if (installed) return;
  installed = true;
  window.addEventListener("error", (e) => {
    console.error("[global error]", e.error ?? e.message);
    toast.error(i18n.t("errors:uncaught"), { duration: Infinity });
  });
  window.addEventListener("unhandledrejection", (e) => {
    console.error("[unhandledrejection]", e.reason);
    toast.error(i18n.t("errors:uncaught"), { duration: Infinity });
  });
}

installGlobalErrorHandlers();
```

- [ ] **步骤 4：跑确认通过**

```sh
pnpm vitest run src/services/error/global-handlers.test.ts
```

预期：PASS（2 tests）。

- [ ] **步骤 5：main.tsx import**

`src/main.tsx`：在 `import "./ui/dev/dev-tools";` 附近加 `import "./services/error/global-handlers";`（模块副作用注册一次）。

- [ ] **步骤 6：Commit**

```sh
git add src/services/error/ src/main.tsx
git commit -m "feat(error): global window error / unhandledrejection net → toast

逃出 React 树的异步报错/未处理 reject 不再静默：console.error + 持久
error toast。模块副作用注册(idempotent)，main.tsx import 一次。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T5：actions.ts IO toast 路由

**文件：** 修改 `src/services/project/actions.ts`、`src/services/project/actions.test.ts`

> 成功 toast 放各 action 函数体（**不**放共享的 `loadProjectIntoEditor`，否则 New-from-template 会误报 opened）。`reportError`/`reportRawError` 的 `ask()` 改持久 `toast.error`。交互式确认（confirmDiscard / overwrite / save-first）不动。actions 的 toast 路由因 `isTauri()` 门控难以非 Tauri 单测，主要靠视觉验证；这里只 mock sonner 保持现有测试绿。

- [ ] **步骤 1：actions.test.ts 顶部 mock sonner（保持现有测试绿 + 防 import 副作用）**

`src/services/project/actions.test.ts` 顶部（现有 `vi.mock("@tauri-apps/plugin-dialog"…)` 之后）加：

```ts
vi.mock("sonner", () => ({ toast: { success: vi.fn(), error: vi.fn() } }));
```

- [ ] **步骤 2：actions.ts import**

顶部 import 区加：

```ts
import { toast } from "sonner";

import i18n from "@/i18n";
```

- [ ] **步骤 3：openProject 成功 toast**

`openProject` 内 `loadProjectIntoEditor(renamed, selected);` 之后加：

```ts
toast.success(i18n.t("errors:toast.opened", { name: renamed.metadata.name }));
```

- [ ] **步骤 4：saveProjectInner 成功 toast**

`saveProjectInner` 内 `useProjectStore.getState().markClean();`（在 `try` 内、`setCurrentPath` 之后）那行之后加：

```ts
toast.success(i18n.t("errors:toast.saved"));
```

- [ ] **步骤 5：importGlbInner 成功 toast**

`importGlbInner` 末尾 `useProjectStore.getState().markDirty();` 之后加：

```ts
toast.success(i18n.t("errors:toast.imported", { name: nodeName }));
```

- [ ] **步骤 6：exportProjectInner 成功 → 用 toast 替换 info ask()**

把 `exportProjectInner` 末尾的 warnLine + `await ask(…info…)` 整块（`const warnLine = …` 到那个 `);` 结束）替换为：

```ts
if (result.warnings.length > 0) {
  console.warn("[export] warnings:", result.warnings);
}
toast.success(i18n.t("errors:toast.exported", { path: summary.destination_path }));
```

> 这是 success 通知（原来是 OK-only 的 info 框，非交互确认），按 Q2 改 toast。

- [ ] **步骤 7：reportRawError 改 toast.error**

`reportRawError` 整个函数体替换为：

```ts
async function reportRawError(message: string): Promise<void> {
  console.error(message);
  toast.error(message, { duration: Infinity });
}
```

> `reportError` 不变（仍调 `reportRawError`）。签名保留 `Promise<void>` 不动调用方的 `await`。

- [ ] **步骤 8：验证（全套 + typecheck + lint）**

```sh
pnpm lint && pnpm typecheck && pnpm test
```

预期：全绿（现有 actions.test 的 createProjectFromTemplate 等仍过；sonner 已 mock）。注意 `ask` 仍被 confirmDiscard / overwrite / save-first 使用，import 不要误删。

- [ ] **步骤 9：Commit**

```sh
git add src/services/project/actions.ts src/services/project/actions.test.ts
git commit -m "feat(actions): IO toasts — success toast + error toast (was native ask)

save/open/import/export 成功 → toast.success(各 action 内,非共享 loader)。
reportError/reportRawError 从阻塞 Tauri ask() 改持久 toast.error。export 的
成功 info 框也改 toast。交互式确认(confirmDiscard/overwrite/save-first)保留 ask()。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T6：验证 + 视觉验证 + PR + roadmap 回填

**文件：** 修改 `docs/roadmap.md`（PR 后）

- [ ] **步骤 1：全套验证**

```sh
pnpm lint && pnpm typecheck && pnpm test
```

预期：lint/typecheck 绿、test 绿（基线 323 + 新增 ErrorBoundary 2 + CrashFallback 1 + global-handlers 2 ≈ 328）。`format:check` 的未跟踪 CLAUDE.md/tests/ 噪声忽略。

- [ ] **步骤 2：视觉验证（人工 `pnpm tauri dev`，按 spec §11）**

- [ ] 正常使用无误报 toast
- [ ] 保存 → 「已保存」自动消失 toast；打开 → 「已打开 {name}」；导入/导出 → 对应 toast
- [ ] 制造 IO 失败 → 持久 error toast（手动关、含 message）
- [ ] confirmDiscard / 覆盖确认仍是原生 ask()
- [ ] 临时 `throw new Error("boom")` → CrashFallback 不白屏 → 重载恢复（验后回退）
- [ ] 未捕获 promise reject → error toast + console
- [ ] 中英文 + light/dark 主题 toast/崩溃屏正确

- [ ] **步骤 3：push + 开 PR**

```sh
git push -u origin feat/phase3-3.3-errors
/opt/homebrew/bin/gh pr create --base main --head feat/phase3-3.3-errors \
  --title "feat(phase3-3.3): error handling polish (ErrorBoundary + toasts + global net)" \
  --body "$(cat <<'EOF'
## What

Phase 3 · 3.3 错误处理 polish（v0.1 最后一个功能项）。三层错误网：

- **全局 ErrorBoundary**：渲染崩溃 → CrashFallback（图标+标题+message+重载/复制），不再白屏。main.tsx 包 `<App/>`。
- **IO toast**（sonner）：save/open/import/export 成功 → 自动消失 `toast.success`；错误（reportError/reportRawError）从阻塞 Tauri `ask()` → 持久 `toast.error`。交互式确认（confirmDiscard/overwrite/save-first）保留 ask()。
- **全局兜底**：window error / unhandledrejection → console.error + error toast（非致命）。

新增 sonner 依赖 + `<Toaster/>`(挂 App 根, theme 接 useSettingsStore)。i18n 复用 errors namespace（crash/toast/uncaught）。ErrorView（项目加载错误页）不动。

## Why

- Spec: \`docs/superpowers/specs/2026-06-01-phase3-3.3-errors-design.md\`
- Plan: \`docs/superpowers/plans/2026-06-01-phase3-3.3-errors-plan.md\`
- Roadmap: v0.1 Phase 3 · 3.3（决议：v0.1.0 含 3.3，之后做 3.5 发版）

## How to test

- [x] \`pnpm lint\` / \`pnpm typecheck\` / \`pnpm test\`（本地绿，新增 ~5 测试）
- [ ] CI 全绿
- [ ] 人工 \`pnpm tauri dev\` 视觉验证（plan T6 步骤 2）

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **步骤 4：CI 绿 + 视觉验证后回填 roadmap + merge**

`docs/roadmap.md` 3.3 行 `- [ ]` → `- [x] … （[#NN](…/pull/NN)）`，commit `docs(roadmap): mark 3.3 error handling shipped (#NN)` push。CI 绿 + 视觉验证通过后 `gh pr merge NN --merge --delete-branch`。然后回到 **3.5 发布流程**（v0.1.0 现在含 3.3 了）。

---

## 收尾验收

满足 spec §1：1 ✅ ErrorBoundary 兜底 · 2 ✅ IO toast 成功/失败 · 3 ✅ 全局兜底 · 4 ✅ 交互确认保留 ask() · 5 ✅ lint/typecheck/test 绿 + 视觉验证。

下一步：merge → **3.5 发布流程**（CHANGELOG + bump 0.1.0 + tag → release.yml draft；findings 见 3.5 brainstorm 记录）。
