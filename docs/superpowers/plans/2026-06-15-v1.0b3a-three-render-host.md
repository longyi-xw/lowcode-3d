# B3a 抽取 ThreeRenderHost 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 把 `ThreeViewport.tsx`（791 行）里的 render loop / 相机 / 拾取描边 / gizmo / snap 抽进 `ThreeRenderHost implements IRenderHost`，`ThreeViewport` 退化为薄壳；Three 编辑行为逐项等价（纯重构，零用户可见变化）。

**架构：** 借成熟的 Three 实现把 `IRenderHost` 的 gizmo+snap 契约「形式化」（B3b 让 Babylon 验证它）。host 保持 store/command 无关——经 `onTransformCommit` 回调 + `setSnapProvider` 注入与 app 解耦；留守的 B4 功能（socket markers / asset drop / play / focus）通过 host 的引擎专有面（`adapter` getter / `focusCamera` / `setFrameCallback`）访问。snap 纯函数共享，引擎专有特征提取下沉到 `snap-features.ts`。

**技术栈：** TypeScript / React / three.js（`WebGLRenderer` + `OrbitControls` + `TransformControls` + `EffectComposer`/`OutlinePass`）/ Zustand / Vitest。

**规格：** `docs/superpowers/specs/2026-06-15-v1.0b3a-three-render-host-design.md`

**测试约束（贯穿全程）：** jsdom 无 WebGL，`ThreeRenderHost` 的 mount/render loop 测不了 → 单测只覆盖纯逻辑单元（`editor-types` 类型移动靠 typecheck、`snap-features`、`transform-util`）；GL 类与接线靠**最终 visual smoke**（任务 8）逐项验证。每个 GL 任务的「验证」= `pnpm typecheck` 通过 + 既有 vitest 全绿。

---

## 文件结构

| 文件                                       | 职责                                                      | 操作 |
| ------------------------------------------ | --------------------------------------------------------- | ---- |
| `src/core/editor-types.ts`                 | `GizmoMode` 编辑器交互类型（中立层，core）                | 创建 |
| `src/services/ui/store.ts`                 | 从 core 引 `GizmoMode`，不再本地声明                      | 修改 |
| `src/runtime/render-host.ts`               | `IRenderHost` 加 gizmo+snap 契约 + `SnapNode`             | 修改 |
| `src/runtime/babylon/render-host.ts`       | 新契约方法的 no-op stub（Babylon 仍 view-only，B3b 填实） | 修改 |
| `src/runtime/three/snap-features.ts`       | Three 专有特征提取 + 纯 `computeSnapOffset`               | 创建 |
| `src/runtime/three/snap-features.test.ts`  | 上者单测                                                  | 创建 |
| `src/runtime/three/transform-util.ts`      | `captureTransform` / `transformsEqual`（host+壳共享）     | 创建 |
| `src/runtime/three/transform-util.test.ts` | 上者单测                                                  | 创建 |
| `src/runtime/three/render-host.ts`         | `ThreeRenderHost`（大抽取）                               | 创建 |
| `src/ui/viewport/ThreeViewport.tsx`        | 改薄壳：接线 stores ↔ host，保留 B4 功能                  | 修改 |
| `docs/roadmap.md`                          | B3 行拆为 B3a/B3b                                         | 修改 |

---

## 任务 1：GizmoMode 移到 core/editor-types.ts

**文件：**

- 创建：`src/core/editor-types.ts`
- 修改：`src/services/ui/store.ts:11`（`export type GizmoMode = ...` 行）

类型移动无运行时行为，验证 = typecheck + 既有测试。

- [ ] **步骤 1：创建 editor-types.ts**

```ts
// src/core/editor-types.ts
/**
 * Editor interaction types shared across layers (core ← runtime ← services ←
 * ui). Lives in `core` so both `runtime/render-host` and `services/ui/store`
 * can import it without a layering violation. Not serialized scene data — keep
 * it out of `scene/types.ts`.
 */
export type GizmoMode = "translate" | "rotate" | "scale";
```

- [ ] **步骤 2：改 store 引用**

在 `src/services/ui/store.ts`：删除本地 `export type GizmoMode = "translate" | "rotate" | "scale";`（:11），改为在文件顶部 import 区加：

```ts
import type { GizmoMode } from "@/core/editor-types";
```

并把 store 内对外暴露 `GizmoMode` 的消费者保持不变（`gizmoMode: GizmoMode` / `setGizmoMode`）。若有别处 `import { GizmoMode } from "@/services/ui/store"`，改为从 `@/core/editor-types` 引（用步骤 3 的 grep 找全）。

- [ ] **步骤 3：找全旧 import 点并改**

运行：`grep -rn "GizmoMode" src/ --include=*.ts --include=*.tsx`
把所有 `from "@/services/ui/store"` 引 `GizmoMode` 的改为 `from "@/core/editor-types"`。（`services/ui/store` 自身若仍想 re-export 可保留 `export type { GizmoMode }`，但不再声明。）

- [ ] **步骤 4：验证**

运行：`pnpm typecheck && pnpm test`
预期：typecheck PASS、既有测试全绿。

- [ ] **步骤 5：Commit**

```bash
git add src/core/editor-types.ts src/services/ui/store.ts
git commit -m "refactor(core): move GizmoMode to core/editor-types (neutral layer)"
```

---

## 任务 2：扩展 IRenderHost 契约 + Babylon no-op stub

**文件：**

- 修改：`src/runtime/render-host.ts`
- 修改：`src/runtime/babylon/render-host.ts`

新增契约会让 `BabylonRenderHost` 编译失败（缺方法）。Babylon 当前 view-only（`isEngineEditingCapable` 门），所以 no-op gizmo stub 就是其正确行为；B3b 再填实。

- [ ] **步骤 1：扩展 render-host.ts**

在 `src/runtime/render-host.ts` 顶部 import 增补，并在 `IRenderHost` 内（`setSelection` 之后、`dispose` 之前）加方法；文件尾部加 `SnapNode`：

```ts
import type { RuntimeTarget, SceneNode, Socket, Transform } from "@/core/scene/types";
import type { GizmoMode } from "@/core/editor-types";

// …ViewportEngine 不变…

/** Minimal per-node info the host pulls (via setSnapProvider) at drag start to
 *  cache snap targets — store-agnostic so the host never imports Zustand. */
export interface SnapNode {
  id: string;
  sockets: readonly Socket[];
  visible: boolean;
  type: SceneNode["type"];
}

export interface IRenderHost {
  // …engine / mount / start / stop / resize / setSelection 不变…

  /** Set the transform gizmo manipulation mode. */
  setGizmoMode(mode: GizmoMode): void;
  /** Attach the gizmo to a node (null = detach). `locked` → outline only,
   *  no attach (prevents drag-editing locked/editor-chrome nodes). */
  setGizmoTarget(node_id: string | null, locked: boolean): void;
  /** Register the commit sink — the host calls it once per completed drag
   *  (mouseUp with a changed transform). The viewport wires it to the command
   *  history; the host stays command-agnostic. */
  onTransformCommit(
    cb: (node_id: string, prev: Transform, next: Transform) => void,
  ): void;
  /** Inject the scene snapshot provider the host pulls at drag start to cache
   *  snap targets. Keeps the host free of store imports. */
  setSnapProvider(provider: () => SnapNode[]): void;

  dispose(): void;
}
```

- [ ] **步骤 2：给 BabylonRenderHost 加 no-op stub**

在 `src/runtime/babylon/render-host.ts` 的 `BabylonRenderHost` 类内（`setSelection` 附近）加：

```ts
  // ── Gizmo + snap (B3b will implement; Babylon is view-only until then) ──
  setGizmoMode(_mode: GizmoMode): void {
    // no-op until B3b adds the Babylon GizmoManager
  }
  setGizmoTarget(_node_id: string | null, _locked: boolean): void {
    // no-op until B3b
  }
  onTransformCommit(
    _cb: (node_id: string, prev: Transform, next: Transform) => void,
  ): void {
    // no-op until B3b
  }
  setSnapProvider(_provider: () => SnapNode[]): void {
    // no-op until B3b
  }
```

并在该文件顶部加 import：

```ts
import type { GizmoMode } from "@/core/editor-types";
import type { Transform } from "@/core/scene/types";
import type { SnapNode } from "@/runtime/render-host";
```

- [ ] **步骤 3：验证**

运行：`pnpm typecheck && pnpm test src/runtime/babylon/render-host.test.ts`
预期：typecheck PASS（`BabylonRenderHost` 现满足契约）；既有 Babylon render-host 测试全绿（no-op 不影响断言）。

- [ ] **步骤 4：Commit**

```bash
git add src/runtime/render-host.ts src/runtime/babylon/render-host.ts
git commit -m "feat(render-host): add gizmo+snap to IRenderHost contract; Babylon no-op stub"
```

---

## 任务 3：snap-features.ts（特征提取 + 纯 computeSnapOffset）— TDD

**文件：**

- 创建：`src/runtime/three/snap-features.ts`
- 测试：`src/runtime/three/snap-features.test.ts`

把 `ThreeViewport.tsx:570-666` 的 `bboxFeatures` / `toScreen` / `featureSnapPoints` / `socketPoints` 原样搬出，并新增纯函数 `computeSnapOffset`（把 `snapDraggedObject` 的 socket→node→grid 优先级链抽成可测纯函数；grid 路径把 `snapTranslation` 的绝对坐标转成 offset 以统一返回 offset）。

- [ ] **步骤 1：编写失败的测试**

```ts
// src/runtime/three/snap-features.test.ts
import { describe, expect, it } from "vitest";
import * as THREE from "three";
import { bboxFeatures, featureSnapPoints, computeSnapOffset } from "./snap-features";
import type { SnapPoint } from "@/core/snap/nodes";

describe("bboxFeatures (OBB)", () => {
  it("returns 15 world features for a unit box mesh", () => {
    const mesh = new THREE.Mesh(new THREE.BoxGeometry(2, 2, 2));
    mesh.updateMatrixWorld(true);
    const pts = bboxFeatures(mesh);
    expect(pts).toHaveLength(15);
    // center first
    expect(pts[0].x).toBeCloseTo(0);
    expect(pts[0].y).toBeCloseTo(0);
    expect(pts[0].z).toBeCloseTo(0);
  });

  it("rotates face centers with the object (OBB, not AABB)", () => {
    const mesh = new THREE.Mesh(new THREE.BoxGeometry(2, 2, 2));
    mesh.rotation.y = Math.PI / 4; // 45° about Y
    mesh.updateMatrixWorld(true);
    const pts = bboxFeatures(mesh);
    // +X face center (index 9) magnitude stays 1 from center in the XZ plane
    const fc = pts[9];
    expect(Math.hypot(fc.x, fc.z)).toBeCloseTo(1, 5);
  });

  it("returns [] for an object with no mesh geometry", () => {
    expect(bboxFeatures(new THREE.Group())).toEqual([]);
  });
});

describe("computeSnapOffset priority chain", () => {
  const cam = new THREE.PerspectiveCamera(50, 1, 0.1, 1000);
  cam.position.set(0, 0, 10);
  cam.lookAt(0, 0, 0);
  cam.updateMatrixWorld(true);

  it("returns null when no modifier paths match and not on grid → grid offset", () => {
    // grid snap always yields an offset (possibly zero); a node already on the
    // grid returns ~zero offset.
    const off = computeSnapOffset({
      currentPos: [0, 0, 0],
      draggedFeatures: [],
      draggedSockets: [],
      hasSockets: false,
      targetFeatures: [],
      targetSockets: [],
    });
    expect(off).not.toBeNull();
    expect(off![0]).toBeCloseTo(0);
  });

  it("prefers a socket-align offset over node/grid when within threshold", () => {
    const dragged: SnapPoint = { screen: [100, 100], world: [0, 0, 0] };
    // a socket target projected to the same screen point → offset toward it
    const off = computeSnapOffset({
      currentPos: [0, 0, 0],
      draggedFeatures: [dragged],
      draggedSockets: [{ screen: [100, 100], world: [0, 0, 0], tag: "a" }],
      hasSockets: true,
      targetFeatures: [],
      targetSockets: [{ screen: [101, 101], world: [1, 0, 0], tag: "a" }],
    });
    expect(off).not.toBeNull();
    expect(off![0]).toBeCloseTo(1);
  });
});
```

- [ ] **步骤 2：运行验证失败**

运行：`pnpm test src/runtime/three/snap-features.test.ts`
预期：FAIL，`Cannot find module './snap-features'`。

- [ ] **步骤 3：实现 snap-features.ts**

把 `ThreeViewport.tsx:570-666` 的四个函数原样移入（连注释），导出它们；新增 `computeSnapOffset`。完整内容：

```ts
// src/runtime/three/snap-features.ts
import * as THREE from "three";

import { snapTranslation } from "@/core/snap/grid";
import { SNAP_PIXELS, snapToNodes, type SnapPoint } from "@/core/snap/nodes";
import { snapToSockets, type SocketPoint } from "@/core/snap/sockets";
import type { Socket } from "@/core/scene/types";

// ↓↓↓ 从 ThreeViewport.tsx:578-666 原样移入（保留 OBB 中文注释） ↓↓↓
export function bboxFeatures(obj: THREE.Object3D): THREE.Vector3[] {
  /* …原 ThreeViewport bboxFeatures 函数体逐字搬入… */
}
export function toScreen(
  v: THREE.Vector3,
  camera: THREE.Camera,
  w: number,
  h: number,
): [number, number] {
  /* …原 toScreen 函数体… */
}
export function featureSnapPoints(
  obj: THREE.Object3D,
  camera: THREE.Camera,
  w: number,
  h: number,
): SnapPoint[] {
  /* …原 featureSnapPoints 函数体… */
}
export function socketPoints(
  obj: THREE.Object3D,
  sockets: readonly Socket[],
  camera: THREE.Camera,
  w: number,
  h: number,
): SocketPoint[] {
  /* …原 socketPoints 函数体… */
}
// ↑↑↑ 原样移入结束 ↑↑↑

type Vec3 = [number, number, number];

/**
 * Pure snap priority chain extracted from ThreeViewport.snapDraggedObject:
 * socket-align → node-align (only when the dragged node has no tagged socket)
 * → grid fallback. Returns the world-space offset to apply to the dragged
 * object's position, or null only when there is genuinely nothing to do
 * (never happens on the grid path — grid always returns an offset, possibly
 * zero). Behaviour-equivalent to the in-component version; the caller still
 * gates on translate-mode + modifier before calling.
 */
export function computeSnapOffset(args: {
  currentPos: Vec3;
  draggedFeatures: SnapPoint[];
  draggedSockets: SocketPoint[];
  hasSockets: boolean;
  targetFeatures: SnapPoint[];
  targetSockets: SocketPoint[];
}): Vec3 | null {
  const { currentPos, draggedFeatures, draggedSockets, hasSockets } = args;
  // Socket snap (C) — highest priority.
  const socketOffset = snapToSockets(draggedSockets, args.targetSockets, SNAP_PIXELS);
  if (socketOffset) return socketOffset;
  // Node-align snap (B) — only for nodes WITHOUT tagged sockets.
  if (!hasSockets) {
    const offset = snapToNodes(draggedFeatures, args.targetFeatures, SNAP_PIXELS);
    if (offset) return offset;
  }
  // Grid fallback (A) — absolute → offset.
  const [gx, gy, gz] = snapTranslation(currentPos);
  return [gx - currentPos[0], gy - currentPos[1], gz - currentPos[2]];
}
```

> 实现时打开 `ThreeViewport.tsx` 把 :578-666 四个函数体逐字复制进对应桩位，勿改逻辑。

- [ ] **步骤 4：运行验证通过**

运行：`pnpm test src/runtime/three/snap-features.test.ts`
预期：PASS。

- [ ] **步骤 5：Commit**

```bash
git add src/runtime/three/snap-features.ts src/runtime/three/snap-features.test.ts
git commit -m "refactor(three): extract snap feature projection + pure computeSnapOffset"
```

---

## 任务 4：transform-util.ts（captureTransform / transformsEqual）— TDD

**文件：**

- 创建：`src/runtime/three/transform-util.ts`
- 测试：`src/runtime/three/transform-util.test.ts`

把 `ThreeViewport.tsx:668-689` 的 `captureTransform` / `transformsEqual` 搬出（host 提交 + 壳 play 快照共享）。

- [ ] **步骤 1：编写失败的测试**

```ts
// src/runtime/three/transform-util.test.ts
import { describe, expect, it } from "vitest";
import * as THREE from "three";
import { captureTransform, transformsEqual } from "./transform-util";

describe("captureTransform", () => {
  it("snapshots position/quaternion/scale arrays", () => {
    const o = new THREE.Object3D();
    o.position.set(1, 2, 3);
    o.scale.set(2, 2, 2);
    const t = captureTransform(o);
    expect(t.position).toEqual([1, 2, 3]);
    expect(t.scale).toEqual([2, 2, 2]);
    expect(t.rotation).toHaveLength(4);
  });
});

describe("transformsEqual", () => {
  it("true for identical, false for any differing component", () => {
    const a = {
      position: [0, 0, 0],
      rotation: [0, 0, 0, 1],
      scale: [1, 1, 1],
    } as const;
    expect(transformsEqual(a, { ...a })).toBe(true);
    expect(transformsEqual(a, { ...a, position: [0, 1, 0] })).toBe(false);
  });
});
```

- [ ] **步骤 2：运行验证失败**

运行：`pnpm test src/runtime/three/transform-util.test.ts`
预期：FAIL，`Cannot find module './transform-util'`。

- [ ] **步骤 3：实现 transform-util.ts**

```ts
// src/runtime/three/transform-util.ts
import type * as THREE from "three";

import type { Transform } from "@/core/scene/types";

export function captureTransform(obj: THREE.Object3D): Transform {
  return {
    position: [obj.position.x, obj.position.y, obj.position.z],
    rotation: [obj.quaternion.x, obj.quaternion.y, obj.quaternion.z, obj.quaternion.w],
    scale: [obj.scale.x, obj.scale.y, obj.scale.z],
  };
}

export function transformsEqual(a: Transform, b: Transform): boolean {
  return (
    a.position[0] === b.position[0] &&
    a.position[1] === b.position[1] &&
    a.position[2] === b.position[2] &&
    a.rotation[0] === b.rotation[0] &&
    a.rotation[1] === b.rotation[1] &&
    a.rotation[2] === b.rotation[2] &&
    a.rotation[3] === b.rotation[3] &&
    a.scale[0] === b.scale[0] &&
    a.scale[1] === b.scale[1] &&
    a.scale[2] === b.scale[2]
  );
}
```

- [ ] **步骤 4：运行验证通过**

运行：`pnpm test src/runtime/three/transform-util.test.ts`
预期：PASS。

- [ ] **步骤 5：Commit**

```bash
git add src/runtime/three/transform-util.ts src/runtime/three/transform-util.test.ts
git commit -m "refactor(three): extract captureTransform/transformsEqual to transform-util"
```

---

## 任务 5：创建 ThreeRenderHost（大抽取）

**文件：**

- 创建：`src/runtime/three/render-host.ts`
- 参考源：`src/ui/viewport/ThreeViewport.tsx:71-535`（mount effect）

GL 类，jsdom 测不了 → 验证 = typecheck（本任务）+ 最终 smoke（任务 8）。**逐字搬移**为主，只引入下列已设计的 seam。

**类骨架与新 seam（完整展示；被搬移的 GL 段落按源行号引用）：**

```ts
// src/runtime/three/render-host.ts
import * as THREE from "three";
import { OrbitControls } from "three/addons/controls/OrbitControls.js";
import { TransformControls } from "three/addons/controls/TransformControls.js";
import { EffectComposer } from "three/addons/postprocessing/EffectComposer.js";
import { OutlinePass } from "three/addons/postprocessing/OutlinePass.js";
import { OutputPass } from "three/addons/postprocessing/OutputPass.js";
import { RenderPass } from "three/addons/postprocessing/RenderPass.js";

import type { GizmoMode } from "@/core/editor-types";
import type { Transform } from "@/core/scene/types";
import { ThreeAdapter } from "@/runtime/three/adapter";
import type { IRenderHost, SnapNode } from "@/runtime/render-host";

import { computeSnapOffset, featureSnapPoints, socketPoints } from "./snap-features";
import { captureTransform, transformsEqual } from "./transform-util";

type Vec3 = [number, number, number];

export class ThreeRenderHost implements IRenderHost {
  readonly engine = "three.js" as const;

  private adapterInstance: ThreeAdapter | null = null;
  private renderer: THREE.WebGLRenderer | null = null;
  private composer: EffectComposer | null = null;
  private outlinePass: OutlinePass | null = null;
  private orbit: OrbitControls | null = null;
  private gizmo: TransformControls | null = null;
  private clock: THREE.Clock | null = null;
  private rafId = 0;

  private commitCb: ((id: string, prev: Transform, next: Transform) => void) | null =
    null;
  private snapProvider: (() => SnapNode[]) | null = null;
  private frameCb: ((dt: number) => void) | null = null;

  // gizmo-drag scratch (was function-scoped in ThreeViewport)
  private dragStart: Transform | null = null;
  private cachedTargets: ReturnType<typeof featureSnapPoints> = [];
  private cachedSocketTargets: ReturnType<typeof socketPoints> = [];
  private snapModifierDown = false;
  private currentSelectionId: string | null = null;
  private onSnapPointer = (e: PointerEvent) => {
    this.snapModifierDown = e.ctrlKey || e.metaKey;
  };

  /** Engine-specific surface (not on IRenderHost) — the viewport reaches
   *  through it for the B4 features that stay in the component (socket markers,
   *  asset drop). Throws before mount() / after dispose(). */
  get adapter(): ThreeAdapter {
    if (!this.adapterInstance) {
      throw new Error("ThreeRenderHost: call mount() before adapter");
    }
    return this.adapterInstance;
  }

  mount(canvas: HTMLCanvasElement): void {
    const adapter = new ThreeAdapter();
    this.adapterInstance = adapter;

    const renderer = new THREE.WebGLRenderer({ canvas, antialias: true, alpha: true });
    renderer.setPixelRatio(window.devicePixelRatio);
    renderer.setClearColor(0x101418, 1);
    renderer.shadowMap.enabled = true;
    this.renderer = renderer;
    adapter.setBehaviorDomElement(canvas);

    // composer + OutlinePass — move ThreeViewport.tsx:102-115 verbatim
    // orbit — move :117-120
    // gizmo + getHelper + setMode — move :122-127
    // gizmo event listeners (dragging-changed / mouseDown / mouseUp /
    //   objectChange) — move :133-264, but:
    //   · read the gizmo mode from a private field set by setGizmoMode (NOT
    //     useUIStore) — store the last mode in `this.gizmoMode`.
    //   · mouseDown: pull targets via this.snapProvider() instead of reading
    //     useSceneStore; for each SnapNode (skip dragged/invisible/helper),
    //     get its Object3D via adapter.getRuntimeObject and push
    //     featureSnapPoints(obj,...) + socketPoints(obj, node.sockets,...).
    //   · objectChange → this.snapDraggedObject() (private, uses
    //     computeSnapOffset) then call this.frameCb is NOT here — markers stay
    //     in the viewport via the scene subscription; objectChange only snaps.
    //     (Marker rebuild on drag stays in the viewport — see Task 6 note.)
    //   · mouseUp: if changed, this.commitCb?.(nodeId, start, end).
    //   · dragging-changed restore: this.setSelection(this.currentSelectionId).
    // clock for render loop
    this.clock = new THREE.Clock();
  }

  start(): void {
    if (this.rafId) return;
    const tick = () => {
      this.rafId = requestAnimationFrame(tick);
      this.orbit?.update();
      const dt = this.clock?.getDelta() ?? 0;
      this.frameCb?.(dt);
      this.composer?.render();
    };
    tick();
  }

  stop(): void {
    if (this.rafId) cancelAnimationFrame(this.rafId);
    this.rafId = 0;
  }

  resize(width: number, height: number): void {
    if (width === 0 || height === 0) return;
    this.renderer?.setSize(width, height, false);
    this.composer?.setSize(width, height);
    this.outlinePass?.setSize(width, height);
    this.adapterInstance?.setViewportSize(width, height);
    const cam = this.adapterInstance?.camera;
    if (cam instanceof THREE.PerspectiveCamera) {
      cam.aspect = width / height;
      cam.updateProjectionMatrix();
    }
  }

  setSelection(node_id: string | null): void {
    this.currentSelectionId = node_id;
    if (!this.outlinePass) return;
    const obj = node_id ? this.adapterInstance?.getRuntimeObject(node_id) : null;
    this.outlinePass.selectedObjects = obj ? [obj] : [];
  }

  setGizmoMode(mode: GizmoMode): void {
    this.gizmoMode = mode;
    this.gizmo?.setMode(mode);
  }

  setGizmoTarget(node_id: string | null, locked: boolean): void {
    const obj = node_id ? this.adapterInstance?.getRuntimeObject(node_id) : null;
    if (!obj || locked) this.gizmo?.detach();
    else this.gizmo?.attach(obj);
  }

  onTransformCommit(cb: (id: string, prev: Transform, next: Transform) => void): void {
    this.commitCb = cb;
  }

  setSnapProvider(provider: () => SnapNode[]): void {
    this.snapProvider = provider;
  }

  /** Engine-specific surface — per-frame hook so the viewport can tick play
   *  behaviors without the host knowing about play state. */
  setFrameCallback(cb: ((dt: number) => void) | null): void {
    this.frameCb = cb;
  }

  /** Engine-specific surface — focus effect moves camera + controls (both
   *  owned here now). target/distance computed by the viewport via
   *  computeFocusTarget. */
  focusCamera(target: THREE.Vector3, distance: number): void {
    const cam = this.adapterInstance?.camera;
    const orbit = this.orbit;
    if (!cam || !orbit) return;
    orbit.target.copy(target);
    const dir = new THREE.Vector3();
    cam.getWorldDirection(dir).negate();
    cam.position.copy(target).add(dir.multiplyScalar(distance));
    orbit.update();
  }

  private gizmoMode: GizmoMode = "translate";

  private snapDraggedObject(): void {
    const obj = this.gizmo?.object;
    if (!obj || this.gizmoMode !== "translate" || !this.snapModifierDown) return;
    const cam = this.adapterInstance!.camera;
    const w = this.renderer!.domElement.clientWidth;
    const h = this.renderer!.domElement.clientHeight;
    const draggedNodeId = obj.userData.nodeId as string | undefined;
    const draggedNode = this.snapProvider?.().find((n) => n.id === draggedNodeId);
    const hasSockets = (draggedNode?.sockets ?? []).some((s) => s.tag);
    const offset = computeSnapOffset({
      currentPos: [obj.position.x, obj.position.y, obj.position.z],
      draggedFeatures: featureSnapPoints(obj, cam, w, h),
      draggedSockets: socketPoints(obj, draggedNode?.sockets ?? [], cam, w, h),
      hasSockets,
      targetFeatures: this.cachedTargets,
      targetSockets: this.cachedSocketTargets,
    });
    if (offset) {
      obj.position.set(
        obj.position.x + offset[0],
        obj.position.y + offset[1],
        obj.position.z + offset[2],
      );
    }
  }

  dispose(): void {
    this.stop();
    window.removeEventListener("pointermove", this.onSnapPointer, true);
    window.removeEventListener("pointerdown", this.onSnapPointer, true);
    this.gizmo?.detach();
    this.gizmo?.dispose();
    this.orbit?.dispose();
    this.composer?.dispose();
    this.renderer?.dispose();
    this.adapterInstance?.dispose();
    this.adapterInstance = null;
    this.renderer = null;
    this.composer = null;
    this.outlinePass = null;
    this.orbit = null;
    this.gizmo = null;
    this.clock = null;
    this.commitCb = null;
    this.snapProvider = null;
    this.frameCb = null;
  }
}
```

实现细节（搬移时遵守）：

- `mount` 内把 composer/OutlinePass、orbit、gizmo 及其四个事件监听器从 `ThreeViewport.tsx:102-264` 逐字搬入，按上面注释替换三处 store 读取为 `this.gizmoMode` / `this.snapProvider()` / `this.commitCb`。
- gizmo 的 `mouseDown` 缓存写 `this.cachedTargets` / `this.cachedSocketTargets`；`onSnapPointer` 监听在 `mount` 内 `window.addEventListener(..., true)` 注册（dispose 注销）。
- `setSelection` 等价原 `syncSelection` 的 outline 部分（gizmo attach 拆到 `setGizmoTarget`，由壳分别调用）。
- 相机由 `ThreeAdapter` 拥有（`adapter.camera`）；orbit/gizmo 用 `adapter.camera` 构造。
- canvas 由壳创建并传入（镜像 BabylonViewport），故 `mount` 用 `new WebGLRenderer({ canvas, ... })` 而非 `renderer.domElement`。

- [ ] **步骤 1：写 render-host.ts**（按上骨架 + 逐字搬移源段落）

- [ ] **步骤 2：验证**

运行：`pnpm typecheck`
预期：PASS。`pnpm lint src/runtime/three/render-host.ts` 无 error（注意 eslint react-hooks 不适用于非组件；普通 lint 规则需过）。

- [ ] **步骤 3：Commit**

```bash
git add src/runtime/three/render-host.ts
git commit -m "feat(three): ThreeRenderHost implements IRenderHost (gizmo+snap+outline+camera)"
```

---

## 任务 6：ThreeViewport 改薄壳

**文件：**

- 修改：`src/ui/viewport/ThreeViewport.tsx`（重写 mount effect；删除已下沉的 helper）

把 mount effect 改为：建 host → mount → 接线。保留 socket markers / asset drop / play / focus（用 host 引擎专有面）。删除文件内已搬走的 `bboxFeatures`/`toScreen`/`featureSnapPoints`/`socketPoints`/`captureTransform`/`transformsEqual`（改为从新模块 import；play 的 `captureTransform` 也改 import）。

接线要点（逐条对应规格 §6）：

- canvas：`document.createElement("canvas")` + 样式 + `container.appendChild`（镜像 BabylonViewport）；cleanup 里 `removeChild`。
- `const host = new ThreeRenderHost(); host.mount(canvas);` 之后 `const adapter = host.adapter;`。
- `adapter.assetCache.setProjectPath(useProjectStore.getState().currentPath)` + `seedScene(adapter, initial)`（`seedScene` 留在本文件，仍用 adapter）。
- `host.onTransformCommit((id, prev, next) => executeCommand(new SetNodeTransformCommand({ node_id: id, transform: next, prev_transform: prev })))`。
- `host.setSnapProvider(() => { const nodes = useSceneStore.getState().project?.scene.nodes ?? {}; return Object.values(nodes).map((n) => ({ id: n.id, sockets: n.sockets ?? [], visible: n.visible, type: n.type })); })`。
- selection 接线：`syncSelectionShell(id)` = `host.setGizmoTarget(id, id ? isEffectivelyLocked(node) : false)` + `host.setSelection(id)` + `rebuildSocketMarkers()`，其中 `node = useSceneStore.getState().project?.scene.nodes[id]`。订阅 `useUIStore` selectedNodeId 变化调用它；gizmoMode 变 → `host.setGizmoMode`；playState 变 → enter/exitPlay。
- click-pick：`onPointerDown`/`onClick` + DRAG guard 逐字保留（`ThreeViewport.tsx:368-389`），`adapter.pickAt`。
- socket markers：`ThreeViewport.tsx:266-301` 逐字保留，`adapter.scene.add(socketMarkers)`；cleanup 里 dispose geo/material（在 `host.dispose()` 之前 `adapter.scene.remove(socketMarkers)`）。
- asset drop：`ThreeViewport.tsx:398-420` 逐字保留（`adapter.raycastGroundPoint`）。
- play：`enterPlay`/`exitPlay`（:430-463）保留，但 `gizmo.detach()` 改 `host.setGizmoTarget(null,false)`、`outlinePass.selectedObjects=[]` 改 `host.setSelection(null)`；play 的逐帧 tick 改为 `host.setFrameCallback((dt) => { if (playing) adapter.tickBehaviors(dt); })`（`let playing=false`，enterPlay 置 true、exitPlay 置 false）。删除壳内 `playClock`（host 提供 dt）。
- resize：ResizeObserver 调 `host.resize(container.clientWidth, container.clientHeight)`。
- diff 订阅：`useSceneStore.subscribe` → `diffAndApply(adapter, old, next)` + `rebuildSocketMarkers()`。`diffAndApply`（:761-791）保留但删去 `gizmo`/`outlinePass` 参数，removed 分支改调 `host.setGizmoTarget(null,false)`/`host.setSelection(null)`（仅当被删的是当前选中：比较 `useUIStore.getState().selectedNodeId === n.id`）。
- **seedScene 竞态重放**：`seeded.then(() => { if (unmounted) return; syncSelectionShell(useUIStore.getState().selectedNodeId); })`（`syncSelectionShell` 已含 markers 重建）。
- focus effect（:544-565）：改为 `const host = hostRef.current; const obj = id==null?null:host.adapter.getRuntimeObject(id)??null; const {target,distance}=computeFocusTarget(obj); host.focusCamera(target, distance);`。把 `adapterRef`/`controlsRef` 换成单个 `hostRef`。
- start：`host.start()`；cleanup：注销订阅/监听 → `adapter.scene.remove(socketMarkers)` + dispose markers geo/mat → `host.dispose()` → `removeChild(canvas)` → `hostRef.current=null`。

- [ ] **步骤 1：重写 ThreeViewport.tsx**（按上要点；删除已下沉 helper 的本地定义，改 import）

- [ ] **步骤 2：验证（自动化）**

运行：`pnpm typecheck && pnpm lint && pnpm test`
预期：typecheck PASS、lint 无 error（注意 [[reference_eslint_react_hooks]]：勿在 effect body setState）、既有测试全绿。

- [ ] **步骤 3：Commit**

```bash
git add src/ui/viewport/ThreeViewport.tsx
git commit -m "refactor(viewport): ThreeViewport becomes a thin shell over ThreeRenderHost"
```

---

## 任务 7：更新 roadmap

**文件：**

- 修改：`docs/roadmap.md:127`（B3 行）

- [ ] **步骤 1：把 B3 行拆为 B3a/B3b**

将 `- [ ] **B3 · gizmo + snap 跨引擎**。` 替换为：

```markdown
- [ ] **B3 · gizmo + snap 跨引擎**（拆为 B3a/B3b）：
  - [ ] **B3a · 抽 ThreeRenderHost**：render loop/camera/pick/outline/gizmo/snap 从 ThreeViewport 抽进 `ThreeRenderHost implements IRenderHost`（行为保持重构，定义 gizmo+snap 契约）；ThreeViewport 改薄壳，socket markers/asset drop/play/focus 经引擎专有面留守（B4 抽）。
  - [ ] **B3b · Babylon gizmo + snap**：`BabylonRenderHost` 实现同契约（`GizmoManager` + Babylon 特征提取），conformance/ smoke 验跨引擎对等。
```

- [ ] **步骤 2：Commit**

```bash
git add docs/roadmap.md
git commit -m "docs(roadmap): split B3 into B3a (extract ThreeRenderHost) + B3b (Babylon gizmo)"
```

---

## 任务 8：全量验证 + visual smoke

**这是行为保持的真正验收**（vitest 绿 ≠ 功能正确，见 [[feedback_visual_verification]]）。

- [ ] **步骤 1：自动化全绿**

运行：`pnpm typecheck && pnpm lint && pnpm test`
预期：全 PASS。

- [ ] **步骤 2：visual smoke（`pnpm tauri dev`）**

逐项确认与重构前**等价**（任一不符 = 回退该处搬移）：

1. 点选节点选中、点空白清选
2. gizmo translate/rotate/scale 三模式（G/R/S 或工具栏）
3. 按住 Ctrl/Cmd 拖拽：grid 吸附 + node-align 吸附 + socket 吸附三层都生效
4. 一次连续拖拽 = 一条 undo（Cmd+Z 一步回退）
5. locked 节点：只描边、gizmo 不 attach
6. Play：进入时 gizmo/描边消失、behavior 运转；Stop：transform 复位、选中恢复
7. F focus 选中节点、F 无选中回原点
8. 资源库拖入视口落在地面命中点
9. socket markers 跟随拖拽 + 选中变色
10. 引擎切到 Babylon 再切回 Three：选中的 gizmo/描边自动恢复（B1 竞态修复仍有效）
11. 全程 console 零 error/warn

- [ ] **步骤 3：发现偏差则修复并重跑**；全过后此分支可进入收尾（PR）。

---

## 自检记录

- **规格覆盖**：§3 契约→任务 2；§4 专有面（adapter/focusCamera）→任务 5 + 新增 `setFrameCallback`（play 帧 tick 所需，规格 §6 play 留守的隐含依赖，已在任务 5/6 显式化）；§5 snap 归属→任务 3；§6 薄壳→任务 6；§7 测试→任务 3/4 单测 + 任务 8 smoke；§9 交付物→任务 1-7 全覆盖；`GizmoMode` 中立化→任务 1。
- **占位符**：snap-features/render-host 的搬移段用「源行号引用 + 逐字搬入」而非 TODO——因是机械搬移既有代码，引用精确源行比重抄 250 行更不易出错；新增/改写的胶水（seam、computeSnapOffset、薄壳接线）均给出完整代码。
- **类型一致**：`SnapNode`（render-host.ts 定义）在任务 2/5/6 一致；`computeSnapOffset` 参数对象形状在任务 3 定义、任务 5 调用一致；`setFrameCallback`/`focusCamera`/`adapter` 在任务 5 定义、任务 6 调用一致；`captureTransform`/`transformsEqual` 任务 4 定义、任务 5/6 用。
