# v0.5 Stage C · 多 behavior 补完 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法跟踪进度。

**目标：** 扩 behavior 框架支持事件驱动 + 加 hover-highlight（emissive 高亮）+ bob（正弦浮动）→ 内置 behavior ≥3，兑现 v0.5「悬停高亮」交互。

**架构：** `Behavior` 接口加 `install(object, params, ctx: BehaviorContext)` + 泛型 `THandle` + `tick?` 可选；adapter 注入 domElement/raycaster 构 ctx；事件 behavior 在 install 装 pointer 监听（dispose 清理），导出走新 `interactions` 通道；bob 走既有 `tickers`。

**技术栈：** Three.js（Raycaster/Color/emissive）+ zod + React + Vitest + react-i18next。

**前置：** spec `docs/superpowers/specs/2026-06-02-v0.5c-behaviors-design.md`（commit abceb96）。分支 `feat/v0.5c-more-behaviors`。基线 `pnpm test` 328 绿。

---

## 文件结构

**新增：** `behaviors/bob.ts`+`.test.ts`、`behaviors/hover-highlight.ts`+`.test.ts`、`behavior-params/BobForm.tsx`+`.test.tsx`、`behavior-params/HoverHighlightForm.tsx`+`.test.tsx`

**修改：** `behaviors/types.ts`、`behaviors/auto-rotate.ts`、`behaviors/index.ts`、`runtime/three/adapter.ts`+`.test.ts`、`ui/viewport/ThreeViewport.tsx`、`export/scene-codegen.ts`+`.test.ts`、`export/vite-emitter.ts`、`export/standalone-esm-emitter.ts`、`export/emitters.test.ts`、`behavior-params/registry.ts`、`i18n/locales/{en,zh}/editor.json`、`docs/roadmap.md`(PR 后)

---

## 任务 T1：框架扩展（BehaviorContext + 泛型 + tick? + adapter 接线）

**文件：** 修改 `behaviors/types.ts`、`behaviors/auto-rotate.ts`、`runtime/three/adapter.ts`、`ui/viewport/ThreeViewport.tsx`

- [ ] **步骤 1：types.ts**

整体替换 `src/runtime/three/behaviors/types.ts`：

```ts
import type * as THREE from "three";

import type { BehaviorDefinition, CodegenContext } from "@/runtime/adapter";

/** Runtime context passed to Behavior.install — lets event-driven behaviors
 *  raycast + bind pointer listeners. domElement is null in non-viewport
 *  contexts (tests); event behaviors must no-op when it's null. */
export interface BehaviorContext {
  scene: THREE.Scene;
  camera: THREE.Camera;
  domElement: HTMLElement | null;
  raycaster: THREE.Raycaster;
}

export interface BehaviorHandle {
  dispose?(): void;
}

export interface Behavior<
  TParams = unknown,
  THandle extends BehaviorHandle = BehaviorHandle,
> {
  readonly definition: BehaviorDefinition;

  install(object: THREE.Object3D, params: TParams, ctx: BehaviorContext): THandle;

  /** Optional: event-driven behaviors (hover-highlight) need no per-frame
   *  tick — they work via listeners bound in install. */
  tick?(object: THREE.Object3D, params: TParams, handle: THandle, dt: number): void;

  emit(varName: string, params: TParams, ctx: CodegenContext): string;
}
```

- [ ] **步骤 2：auto-rotate.ts install 加 ctx 参数（忽略）**

`src/runtime/three/behaviors/auto-rotate.ts` 的 install 签名加第三参：

```ts
install(_object: THREE.Object3D, _params: Params, _ctx: BehaviorContext): BehaviorHandle {
  return {};
}
```

顶部 import 加 `BehaviorContext`：`import type { Behavior, BehaviorContext, BehaviorHandle } from "./types";`

- [ ] **步骤 3：adapter.ts — domElement/raycaster/registerBehavior + ctx + tick?.()**

`src/runtime/three/adapter.ts`：

(a) 顶部加 `import * as THREE from "three";`（若已有则跳过）。

(b) 类内 `behaviorRegistry` 字段附近加：

```ts
private behaviorDomElement: HTMLElement | null = null;
private readonly behaviorRaycaster = new THREE.Raycaster();
```

(c) 在 Behaviors 区加方法：

```ts
/** Inject the canvas so event-driven behaviors can bind pointer listeners.
 *  Called by ThreeViewport at mount. */
setBehaviorDomElement(el: HTMLElement | null): void {
  this.behaviorDomElement = el;
}

/** Register an extra behavior (future custom behaviors; used by tests to
 *  inject fakes). */
registerBehavior(b: Behavior): void {
  this.behaviorRegistry.register(b);
}
```

（`Behavior` 需 import：`import type { Behavior } from "./behaviors/types";` 或既有 import。）

(d) `installBehaviors` 里 `const handle = b.install(object, parsed.data);` 改为：

```ts
const handle = b.install(object, parsed.data, {
  scene: this.scene,
  camera: this.camera,
  domElement: this.behaviorDomElement,
  raycaster: this.behaviorRaycaster,
});
```

(e) `tickBehaviors` 里 `entry.behavior.tick(object, entry.params, entry.handle, dt);` 改为 `entry.behavior.tick?.(object, entry.params, entry.handle, dt);`

- [ ] **步骤 4：ThreeViewport 注入 domElement**

`src/ui/viewport/ThreeViewport.tsx` mount effect 内、拿到 `renderer`/`canvas` 之后加一行：

```ts
adapter.setBehaviorDomElement(renderer.domElement);
```

cleanup（dispose 前）可加 `adapter.setBehaviorDomElement(null);`（可选，adapter.dispose 会丢弃）。

- [ ] **步骤 5：验证既有行为不退化**

```sh
pnpm typecheck && pnpm vitest run src/runtime/three/adapter.test.ts src/runtime/three/behaviors/
```

预期：PASS（auto-rotate 的 install 多一个被忽略的参数；tick 仍调用；adapter 既有 behavior 测试全绿）。

- [ ] **步骤 6：Commit**

```sh
git add src/runtime/three/behaviors/types.ts src/runtime/three/behaviors/auto-rotate.ts \
        src/runtime/three/adapter.ts src/ui/viewport/ThreeViewport.tsx
git commit -m "feat(behaviors): BehaviorContext + typed handle + optional tick

install 加 BehaviorContext{scene,camera,domElement,raycaster} 让事件驱动
behavior 能 raycast + 绑 pointer 监听;Behavior<TParams,THandle> 泛型 +
tick? 可选(还 project_state 债)。adapter 注入 domElement(ThreeViewport
setBehaviorDomElement) + 共享 raycaster + registerBehavior 测试 seam。
auto-rotate 忽略新 ctx 参(向后兼容)。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T2：bob behavior（tick 类，有状态 handle）

**文件：** 创建 `behaviors/bob.ts` + `.test.ts`；修改 `behaviors/index.ts`

- [ ] **步骤 1：写失败测试 `bob.test.ts`**

```ts
import * as THREE from "three";
import { describe, expect, it } from "vitest";

import { BobBehavior } from "./bob";

const ctx = {
  scene: new THREE.Scene(),
  camera: new THREE.PerspectiveCamera(),
  domElement: null,
  raycaster: new THREE.Raycaster(),
};

describe("BobBehavior", () => {
  it("oscillates position around the install-time base on the chosen axis", () => {
    const b = new BobBehavior();
    const obj = new THREE.Object3D();
    obj.position.y = 2;
    const params = { axis: "y" as const, amplitude: 1, frequency: 1 };
    const handle = b.install(obj, params, ctx);
    // t=0 → base
    b.tick!(obj, params, handle, 0);
    expect(obj.position.y).toBeCloseTo(2, 6);
    // quarter period (t = 1/(4f) = 0.25) → base + amplitude
    b.tick!(obj, params, handle, 0.25);
    expect(obj.position.y).toBeCloseTo(3, 5);
  });

  it("definition.type is bob", () => {
    expect(new BobBehavior().definition.type).toBe("bob");
  });

  it("emit pushes a ticker referencing the var", () => {
    const code = new BobBehavior().emit(
      "n_x",
      { axis: "y", amplitude: 1, frequency: 1 },
      { project: {} as never, warnings: [], currentNodeVar: "n_x" },
    );
    expect(code).toContain("tickers.push");
    expect(code).toContain("n_x.position.y");
  });
});
```

- [ ] **步骤 2：跑确认失败**

```sh
pnpm vitest run src/runtime/three/behaviors/bob.test.ts
```

预期：FAIL（找不到模块）。

- [ ] **步骤 3：实现 `bob.ts`**

```ts
import type * as THREE from "three";
import { z } from "zod";

import type { BehaviorDefinition, CodegenContext } from "@/runtime/adapter";

import type { Behavior, BehaviorContext } from "./types";

const ParamsSchema = z.object({
  axis: z.enum(["x", "y", "z"]),
  amplitude: z.number(),
  frequency: z.number(),
});
type Params = z.infer<typeof ParamsSchema>;

interface BobHandle {
  base: number;
  elapsed: number;
}

const TWO_PI = Math.PI * 2;

export class BobBehavior implements Behavior<Params, BobHandle> {
  readonly definition: BehaviorDefinition = {
    type: "bob",
    name: "Bob",
    description: "Floats the node up and down along a local axis (sine wave).",
    parameters_schema: ParamsSchema,
  };

  install(object: THREE.Object3D, params: Params, _ctx: BehaviorContext): BobHandle {
    return { base: object.position[params.axis], elapsed: 0 };
  }

  tick(object: THREE.Object3D, params: Params, handle: BobHandle, dt: number): void {
    handle.elapsed += dt;
    object.position[params.axis] =
      handle.base +
      params.amplitude * Math.sin(TWO_PI * params.frequency * handle.elapsed);
  }

  emit(varName: string, params: Params, _ctx: CodegenContext): string {
    return [
      `{`,
      `  const _base = ${varName}.position.${params.axis};`,
      `  let _t = 0;`,
      `  const _amp = ${params.amplitude};`,
      `  const _freq = ${params.frequency};`,
      `  tickers.push((dt) => {`,
      `    _t += dt;`,
      `    ${varName}.position.${params.axis} = _base + _amp * Math.sin(2 * Math.PI * _freq * _t);`,
      `  });`,
      `}`,
    ].join("\n");
  }
}
```

- [ ] **步骤 4：注册（index.ts）**

`src/runtime/three/behaviors/index.ts`：import `BobBehavior` + 在 `createThreeBehaviorRegistry` 内 `r.register(new BobBehavior());`。

- [ ] **步骤 5：跑确认通过**

```sh
pnpm vitest run src/runtime/three/behaviors/bob.test.ts src/runtime/three/behaviors/registry.test.ts
```

预期：PASS（registry.test 若断言数量需在 T6/此处同步——见 T6；bob 3 用例绿）。

- [ ] **步骤 6：Commit**

```sh
git add src/runtime/three/behaviors/bob.ts src/runtime/three/behaviors/bob.test.ts \
        src/runtime/three/behaviors/index.ts
git commit -m "feat(behaviors): bob — sine float along an axis (tick behavior)

有状态 handle{base,elapsed}(二次验证 typed handle)。install 记 base，
tick 绕 base 正弦振荡，emit push ticker。注册进 registry。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T3：hover-highlight behavior（事件驱动，emissive 交换）

**文件：** 创建 `behaviors/hover-highlight.ts` + `.test.ts`；修改 `behaviors/index.ts`

- [ ] **步骤 1：写失败测试 `hover-highlight.test.ts`**

```ts
import * as THREE from "three";
import { describe, expect, it, vi } from "vitest";

import { HoverHighlightBehavior } from "./hover-highlight";

function makeCtx(domElement: HTMLElement | null) {
  return {
    scene: new THREE.Scene(),
    camera: new THREE.PerspectiveCamera(),
    domElement,
    raycaster: new THREE.Raycaster(),
  };
}

function meshObj() {
  const m = new THREE.Mesh(
    new THREE.BoxGeometry(1, 1, 1),
    new THREE.MeshStandardMaterial(),
  );
  return m;
}

describe("HoverHighlightBehavior", () => {
  it("binds pointer listeners on install and removes them on dispose", () => {
    const el = document.createElement("div");
    const add = vi.spyOn(el, "addEventListener");
    const remove = vi.spyOn(el, "removeEventListener");
    const b = new HoverHighlightBehavior();
    const handle = b.install(
      meshObj(),
      { color: "#ffaa00", intensity: 1 },
      makeCtx(el),
    );
    expect(add).toHaveBeenCalled();
    handle.dispose?.();
    expect(remove).toHaveBeenCalledTimes(add.mock.calls.length);
  });

  it("no-ops (no throw, no listeners) when domElement is null", () => {
    const b = new HoverHighlightBehavior();
    expect(() =>
      b.install(meshObj(), { color: "#ffaa00", intensity: 1 }, makeCtx(null)),
    ).not.toThrow();
  });

  it("restores original emissive on dispose", () => {
    const el = document.createElement("div");
    const obj = meshObj();
    const mat = obj.material as THREE.MeshStandardMaterial;
    mat.emissive.set("#010203");
    const original = mat.emissive.getHex();
    const b = new HoverHighlightBehavior();
    const handle = b.install(obj, { color: "#ffaa00", intensity: 1 }, makeCtx(el));
    mat.emissive.set("#ffaa00"); // simulate a hover having changed it
    handle.dispose?.();
    expect(mat.emissive.getHex()).toBe(original);
  });

  it("emit pushes into interactions (not tickers)", () => {
    const code = new HoverHighlightBehavior().emit(
      "n_x",
      { color: "#ffaa00", intensity: 1 },
      { project: {} as never, warnings: [], currentNodeVar: "n_x" },
    );
    expect(code).toContain("interactions.push");
    expect(code).toContain("n_x");
  });
});
```

- [ ] **步骤 2：跑确认失败**

```sh
pnpm vitest run src/runtime/three/behaviors/hover-highlight.test.ts
```

预期：FAIL。

- [ ] **步骤 3：实现 `hover-highlight.ts`**

```ts
import * as THREE from "three";
import { z } from "zod";

import type { BehaviorDefinition, CodegenContext } from "@/runtime/adapter";

import type { Behavior, BehaviorContext, BehaviorHandle } from "./types";

const ParamsSchema = z.object({
  color: z.string(),
  intensity: z.number(),
});
type Params = z.infer<typeof ParamsSchema>;

/** Collect meshes under `root` whose material exposes an `emissive` Color. */
function emissiveMeshes(root: THREE.Object3D): THREE.Mesh[] {
  const out: THREE.Mesh[] = [];
  root.traverse((o) => {
    const mesh = o as THREE.Mesh;
    const mat = mesh.material as { emissive?: THREE.Color } | undefined;
    if ((mesh as THREE.Mesh).isMesh && mat && mat.emissive instanceof THREE.Color) {
      out.push(mesh);
    }
  });
  return out;
}

export class HoverHighlightBehavior implements Behavior<Params, BehaviorHandle> {
  readonly definition: BehaviorDefinition = {
    type: "hover-highlight",
    name: "Hover Highlight",
    description: "Highlights the node's material emissive while the pointer hovers it.",
    parameters_schema: ParamsSchema,
  };

  install(
    object: THREE.Object3D,
    params: Params,
    ctx: BehaviorContext,
  ): BehaviorHandle {
    const el = ctx.domElement;
    if (!el) return {}; // non-viewport context (tests/export-build) → no-op

    const meshes = emissiveMeshes(object);
    const originals = meshes.map((m) =>
      (m.material as THREE.MeshStandardMaterial).emissive.clone(),
    );
    const hi = new THREE.Color(params.color).multiplyScalar(params.intensity);

    const restore = () => {
      meshes.forEach((m, i) => {
        (m.material as THREE.MeshStandardMaterial).emissive.copy(originals[i]!);
      });
    };

    const onMove = (e: PointerEvent) => {
      const rect = el.getBoundingClientRect();
      const ndc = new THREE.Vector2(
        ((e.clientX - rect.left) / rect.width) * 2 - 1,
        -((e.clientY - rect.top) / rect.height) * 2 + 1,
      );
      ctx.raycaster.setFromCamera(ndc, ctx.camera);
      const hit = ctx.raycaster.intersectObject(object, true).length > 0;
      if (hit) {
        meshes.forEach((m) =>
          (m.material as THREE.MeshStandardMaterial).emissive.copy(hi),
        );
      } else {
        restore();
      }
    };
    const onLeave = () => restore();

    el.addEventListener("pointermove", onMove);
    el.addEventListener("pointerleave", onLeave);

    return {
      dispose() {
        el.removeEventListener("pointermove", onMove);
        el.removeEventListener("pointerleave", onLeave);
        restore();
      },
    };
  }

  emit(varName: string, params: Params, _ctx: CodegenContext): string {
    const color = JSON.stringify(params.color);
    return [
      `interactions.push((rt) => {`,
      `  const _meshes = [];`,
      `  ${varName}.traverse((o) => { if (o.isMesh && o.material && o.material.emissive) _meshes.push(o); });`,
      `  const _orig = _meshes.map((m) => m.material.emissive.clone());`,
      `  const _hi = new THREE.Color(${color}).multiplyScalar(${params.intensity});`,
      `  const _raycaster = new THREE.Raycaster();`,
      `  const _restore = () => _meshes.forEach((m, i) => m.material.emissive.copy(_orig[i]));`,
      `  rt.domElement.addEventListener("pointermove", (e) => {`,
      `    const r = rt.domElement.getBoundingClientRect();`,
      `    const ndc = new THREE.Vector2(((e.clientX - r.left) / r.width) * 2 - 1, -((e.clientY - r.top) / r.height) * 2 + 1);`,
      `    _raycaster.setFromCamera(ndc, rt.camera);`,
      `    if (_raycaster.intersectObject(${varName}, true).length > 0) _meshes.forEach((m) => m.material.emissive.copy(_hi));`,
      `    else _restore();`,
      `  });`,
      `  rt.domElement.addEventListener("pointerleave", _restore);`,
      `});`,
    ].join("\n");
  }
}
```

> emit 块的 `THREE` 来自 scene.js 模块作用域（buildScene 处已 `import * as THREE`）。`interactions` 由 prolog 声明（T4）。setup 闭包内自建 raycaster，故 main.js 的 rt 只需 `{ camera, domElement }`。

- [ ] **步骤 4：注册（index.ts）**

import `HoverHighlightBehavior` + `r.register(new HoverHighlightBehavior());`

- [ ] **步骤 5：跑确认通过**

```sh
pnpm vitest run src/runtime/three/behaviors/hover-highlight.test.ts
```

预期：PASS（4 用例）。

- [ ] **步骤 6：Commit**

```sh
git add src/runtime/three/behaviors/hover-highlight.ts src/runtime/three/behaviors/hover-highlight.test.ts \
        src/runtime/three/behaviors/index.ts
git commit -m "feat(behaviors): hover-highlight — emissive highlight on pointer hover

事件驱动：install 在 ctx.domElement 绑 pointermove/leave + raycast，命中则
交换 emissive，dispose 移监听+还原。domElement null 时 no-op(导出/测试降级)。
emit 走新 interactions 通道(T4)，闭包内自建 raycaster。注册进 registry。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T4：导出 interactions 通道

**文件：** 修改 `export/scene-codegen.ts` + `.test.ts`、`export/vite-emitter.ts`、`export/standalone-esm-emitter.ts`、`export/emitters.test.ts`

- [ ] **步骤 1：scene-codegen 写失败测试（.test.ts 追加）**

```ts
it("emits an interactions array in prolog and returns it", () => {
  const out = generateSceneModule({ project: freshProjectWithNoBehaviors() });
  expect(out.sceneModuleSource).toContain("const interactions = [];");
  expect(out.sceneModuleSource).toContain(
    "return { scene, camera, templates, tickers, interactions };",
  );
});
```

> `freshProjectWithNoBehaviors()`：复用该测试文件已有的 project 构造 helper（沿用现有用例的工厂）。

- [ ] **步骤 2：跑确认失败**

```sh
pnpm vitest run src/runtime/three/export/scene-codegen.test.ts
```

- [ ] **步骤 3：scene-codegen.ts 加 interactions**

- prolog：`push(ctx, \`const tickers = [];\`);`之后加`push(ctx, \`const interactions = [];\`);`
- BuiltScene typedef：`@property {Array<(dt: number) => void>} tickers` 之后加 `push(ctx, \` \* @property {Array<(rt: {camera: THREE.Camera, domElement: HTMLElement}) => void>} interactions\`);`
- epilog：`return { scene, camera, templates, tickers };` → `return { scene, camera, templates, tickers, interactions };`

- [ ] **步骤 4：跑确认通过 + 既有 codegen 测试不退化**

```sh
pnpm vitest run src/runtime/three/export/scene-codegen.test.ts
```

- [ ] **步骤 5：两个 emitter main.js drain interactions**

`vite-emitter.ts`：找到 `const controls = new OrbitControls(built.camera, canvas);` 那行之后（RAF tick 之前）加：

```ts
lines.push(
  `for (const setup of built.interactions) setup({ camera: built.camera, domElement: canvas });`,
);
```

`standalone-esm-emitter.ts`：同样在其 OrbitControls 行之后加等价行（变量名按该文件实际：canvas/built.camera）。

> 一次性 drain（非 RAF loop）。raycaster 在各 setup 闭包内自建，main.js 不需 import THREE。

- [ ] **步骤 6：emitters.test 断言 + 跑**

`emitters.test.ts` 加（或扩展现有 main.js 断言）：vite + standalone 的 main.js 含 `built.interactions`。

```sh
pnpm vitest run src/runtime/three/export/
```

预期：PASS。

- [ ] **步骤 7：Commit**

```sh
git add src/runtime/three/export/
git commit -m "feat(export): interactions channel for event-driven behaviors

scene-codegen prolog 声明 const interactions=[] 并返回;vite + standalone
emitter main.js 在 OrbitControls 后一次性 drain(传 {camera,domElement})。
hover-highlight 的 emit 往这里 push;tick 类仍走 tickers RAF loop。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T5：UI forms + i18n

**文件：** 创建 `behavior-params/BobForm.tsx`+`.test.tsx`、`HoverHighlightForm.tsx`+`.test.tsx`；修改 `behavior-params/registry.ts`、`i18n/locales/{en,zh}/editor.json`

- [ ] **步骤 1：i18n（先加，form 测试要用）**

`en-US/editor.json` 的 `behaviors` 对象内加（在 `auto_rotate_desc` 后）：

```json
    "intensity": "Intensity",
    "color": "Color",
    "amplitude": "Amplitude",
    "frequency": "Frequency",
    "frequency_unit": "Hz",
    "hover_highlight_name": "Hover Highlight",
    "hover_highlight_desc": "Highlights the node while the pointer hovers it.",
    "bob_name": "Bob",
    "bob_desc": "Floats the node up and down along an axis."
```

`zh-CN/editor.json` 对应中文（`强度`/`颜色`/`幅度`/`频率`/`Hz`/`悬停高亮`/`指针悬停时高亮节点。`/`浮动`/`让节点沿某轴上下浮动。`）。

- [ ] **步骤 2：BobForm + 测试**

`BobForm.tsx`（镜像 AutoRotateForm：axis radio + amplitude/frequency number，用 `t("behaviors.amplitude")` 等）。`BobForm.test.tsx`：渲染 → 改 amplitude → onChange 收到新值。（代码镜像 AutoRotateForm，axis 同样三选，数值两项。）

- [ ] **步骤 3：HoverHighlightForm + 测试**

`HoverHighlightForm.tsx`：`<input type="color">` 绑 value.color + intensity number。`.test.tsx`：渲染 + onChange。

- [ ] **步骤 4：registry.ts 注册**

```ts
import { BobForm } from "./BobForm";
import { HoverHighlightForm } from "./HoverHighlightForm";
// ...
export const BEHAVIOR_FORM_REGISTRY = {
  "auto-rotate": AutoRotateForm as unknown as ComponentType<BehaviorFormProps>,
  "hover-highlight": HoverHighlightForm as unknown as ComponentType<BehaviorFormProps>,
  bob: BobForm as unknown as ComponentType<BehaviorFormProps>,
};
```

- [ ] **步骤 5：跑确认 + 全套**

```sh
pnpm vitest run src/ui/editor/behavior-params/
pnpm lint && pnpm typecheck && pnpm test
```

预期：全绿。

- [ ] **步骤 6：Commit**

```sh
git add src/ui/editor/behavior-params/ src/i18n/locales/en-US/editor.json src/i18n/locales/zh-CN/editor.json
git commit -m "feat(ui): BobForm + HoverHighlightForm + behaviors i18n

behavior-params 注册表加两个 form(按 behavior_type 派发);editor.json
behaviors 命名空间加 bob/hover-highlight 名称·描述·参数标签(en/zh)。
BehaviorsPanel add-picker 自动列出(读 getSupportedBehaviors)。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T6：adapter.test 真测试（还 project_state 债）

**文件：** 修改 `runtime/three/adapter.test.ts`、`behaviors/registry.test.ts`

- [ ] **步骤 1：registry.test 数量同步**

`registry.test.ts` 里断言注册数量的用例改为 3（auto-rotate + bob + hover-highlight），并断言三个 type 都在。

- [ ] **步骤 2：adapter.test 加真测试**

追加（用 T1 的 `registerBehavior` seam 注入一个 throwing-tick 假 behavior + 一个真 bob，验证 resilience + 有状态 install/dispose）：

```ts
it("tickBehaviors: a throwing tick on one binding doesn't stop others", () => {
  const adapter = new ThreeAdapter(target);
  adapter.syncNode(makeMeshNode("n1"), "add");
  const ParamsSchema = (await import("zod")).z.object({});
  let goodTicks = 0;
  adapter.registerBehavior({
    definition: {
      type: "throw-tick",
      name: "x",
      description: "x",
      parameters_schema: ParamsSchema,
    },
    install: () => ({}),
    tick: () => {
      throw new Error("boom");
    },
    emit: () => "",
  });
  adapter.registerBehavior({
    definition: {
      type: "count-tick",
      name: "y",
      description: "y",
      parameters_schema: ParamsSchema,
    },
    install: () => ({}),
    tick: () => {
      goodTicks++;
    },
    emit: () => "",
  });
  adapter.installBehaviors("n1", [
    { id: "b1", behavior_type: "throw-tick", enabled: true, parameters: {} },
    { id: "b2", behavior_type: "count-tick", enabled: true, parameters: {} },
  ]);
  expect(() => adapter.tickBehaviors(1)).not.toThrow();
  expect(goodTicks).toBe(1); // count-tick still ran despite throw-tick throwing
});

it("installBehaviors threads BehaviorContext (domElement) to install", () => {
  const adapter = new ThreeAdapter(target);
  const el = document.createElement("div");
  adapter.setBehaviorDomElement(el);
  adapter.syncNode(makeMeshNode("n1"), "add");
  let seen: HTMLElement | null = "unset" as never;
  const ParamsSchema = (await import("zod")).z.object({});
  adapter.registerBehavior({
    definition: {
      type: "ctx-probe",
      name: "z",
      description: "z",
      parameters_schema: ParamsSchema,
    },
    install: (_o, _p, ctx) => {
      seen = ctx.domElement;
      return {};
    },
    emit: () => "",
  });
  adapter.installBehaviors("n1", [
    { id: "b1", behavior_type: "ctx-probe", enabled: true, parameters: {} },
  ]);
  expect(seen).toBe(el);
});
```

> 这两个 `it` 用了 `await import`，需把回调改 `async`。或在文件顶部 `import { z } from "zod"` 后直接用 `z`（推荐，去掉 await import）。实现时用顶部 import 的 `z`。

- [ ] **步骤 3：跑确认通过**

```sh
pnpm vitest run src/runtime/three/adapter.test.ts src/runtime/three/behaviors/registry.test.ts
```

预期：PASS。

- [ ] **步骤 4：Commit**

```sh
git add src/runtime/three/adapter.test.ts src/runtime/three/behaviors/registry.test.ts
git commit -m "test(behaviors): real tick-resilience + ctx-threading tests (pays down debt)

用 adapter.registerBehavior seam 注入 throwing/probe 假 behavior:验证一个
tick 抛错不影响其它 + BehaviorContext.domElement 正确透传到 install。
registry.test 数量同步到 3。还 project_state 记的 adapter.test 占位债。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 任务 T7：验证 + 视觉验证 + PR + roadmap + merge

- [ ] **步骤 1：全套验证**

```sh
pnpm lint && pnpm typecheck && pnpm test
```

预期：全绿（基线 328 + 新增 ~18：bob 3 + hover 4 + 2 forms + codegen + adapter 2 + registry）。

- [ ] **步骤 2：视觉验证（人工 `pnpm tauri dev`，spec §12 清单）**

- [ ] add-picker 列出 Auto Rotate / Hover Highlight / Bob
- [ ] hover-highlight：Play → 悬停高亮、移开还原 → Stop 还原 + 再悬停无效
- [ ] bob：Play → 浮动 → Stop 回原位
- [ ] 加/删/改参数可撤销
- [ ] Export Vite + Standalone → 运行 → hover + bob 都生效
- [ ] 中英文参数 label 正确

- [ ] **步骤 3：push + PR**

```sh
git push -u origin feat/v0.5c-more-behaviors
/opt/homebrew/bin/gh pr create --base main --head feat/v0.5c-more-behaviors \
  --title "feat(v0.5c): event-driven behavior framework + hover-highlight + bob" \
  --body "$(cat <<'EOF'
## What

v0.5 Stage C：扩 behavior 框架支持**事件驱动** + 补足内置 behavior 到 ≥3。

- **框架**：`install(object, params, ctx)` 加 `BehaviorContext{scene,camera,domElement,raycaster}`；`Behavior<TParams,THandle>` 泛型 + `tick?` 可选；adapter `setBehaviorDomElement` + 共享 raycaster + `registerBehavior` seam。
- **hover-highlight**（事件）：指针悬停 → emissive 高亮，dispose 还原 + 移监听；domElement null 降级。导出走新 `interactions` 通道。
- **bob**（tick）：沿轴正弦浮动，有状态 handle。
- **导出**：scene-codegen 加 `interactions` 通道，两个 emitter main.js drain。
- **UI**：BobForm + HoverHighlightForm + i18n；add-picker 自动列 3 个。
- **债**：补 adapter.test 的 tick-resilience + ctx-threading 真测试（还 project_state 占位债）。

## Why

- Spec: \`docs/superpowers/specs/2026-06-02-v0.5c-behaviors-design.md\`
- Plan: \`docs/superpowers/plans/2026-06-02-v0.5c-behaviors-plan.md\`
- Roadmap: v0.5 Stage C（达成 ≥3 内置 behavior，解锁 v1.0）

## How to test

- [x] \`pnpm lint\` / \`typecheck\` / \`test\`（本地绿）
- [ ] CI 绿
- [ ] 人工 \`pnpm tauri dev\` 视觉验证（plan T7 步骤 2）+ Export 运行验证

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **步骤 4：CI 绿 + 视觉验证后回填 roadmap + merge**

`docs/roadmap.md` v0.5 Stage C 行 `- [ ]` → `- [x]（[#NN](…)）`；v0.5「Partially shipped」节可更新为完成。commit `docs(roadmap): mark v0.5 Stage C shipped (#NN)` + push。CI 绿 + 视觉验证通过后 `gh pr merge NN --merge --delete-branch`。

---

## 收尾验收

满足 spec §1：框架支持事件驱动 · hover-highlight 高亮+还原 · bob 浮动 · ≥3 behavior 可加/编辑/删/撤销 · 导出内嵌且外部可运行 · lint/typecheck/test 绿 + 视觉验证。v0.5 行为系统完成 → 解锁 v1.0（多运行时适配器）。
