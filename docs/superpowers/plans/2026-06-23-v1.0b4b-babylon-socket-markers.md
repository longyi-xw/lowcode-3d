# v1.0 B4b — Babylon socket markers 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 把 socket markers 从 `ThreeViewport` 组件层提升为引擎中立的 `IRenderHost.syncSocketMarkers()`，并实现 Babylon 端渲染，让 Babylon 模式选中/编辑时显示 socket 插槽标记（对齐 Three）。

**架构：** socket markers 成为 host 自有的「选中态 overlay」。两 host 各自渲染，数据源复用 `setSnapProvider` 的 `{id, sockets}` + 当前选中。组件只在 init / 场景 diff / 选中变 三处调 `host.syncSocketMarkers()`；拖拽期由 host 内部自重建（吃掉 Three 专属的 `onGizmoChange`）。

**技术栈：** Three.js 0.184（`THREE.Group` + `SphereGeometry` + `MeshBasicMaterial`）/ Babylon `@babylonjs/core` 9.11（`TransformNode` + `MeshBuilder.CreateSphere` + `StandardMaterial`，unlit emissive）；Vitest + NullEngine。

**测试策略（非对称，依代码库现实）：** ThreeRenderHost 用真 `WebGLRenderer`，jsdom 无 WebGL → **无 headless 单测**（一向靠 smoke）。故 Three 迁移是**行为保持 1:1 搬移**，验证 = typecheck + lint + 全套绿 + 用户 Three 视觉 smoke。Babylon host 可在 NullEngine 下单测 → socket markers 在 `babylon/render-host.test.ts` 真单测。规格见 `docs/superpowers/specs/2026-06-23-v1.0b4b-babylon-socket-markers-design.md`。

---

## 文件结构

- 新建 `src/runtime/socket-markers.ts` — 引擎中立视觉常量（radius / cyan / amber）。单一职责：锁定两引擎逐像素一致。
- 新建 `src/runtime/socket-markers.test.ts` — 常量值锁定。
- 修改 `src/runtime/render-host.ts` — `IRenderHost` 加 `syncSocketMarkers()`。
- 修改 `src/runtime/three/render-host.ts` — 迁入 markers overlay + 私有 `rebuildSocketMarkers` + 公开 `syncSocketMarkers`；删 `gizmoChangeCb` 字段 + `onGizmoChange` 方法；`objectChange` 改调私有重建；mount 建 overlay、dispose 清。
- 修改 `src/ui/viewport/ThreeViewport.tsx` — 删组件层 marker 整段 + `onGizmoChange` 接线 + cleanup；三处重建调用改 `host.syncSocketMarkers()`。
- 修改 `src/runtime/babylon/render-host.ts` — 实现 markers（字段 + mount overlay/材质 + `setSelection` 存选中 + 私有 `rebuildSocketMarkers` + 公开 `syncSocketMarkers` + 拖拽帧重建 + dispose + `socketMarkersForTest` 测试 getter）。
- 修改 `src/runtime/babylon/render-host.test.ts` — NullEngine 下 markers 单测。
- 修改 `src/ui/viewport/BabylonViewport.tsx` — `applyDiff` 末尾 + 选中订阅各调 `host.syncSocketMarkers()`。
- 修改 `docs/roadmap.md` — 勾选 B4b。

---

### 任务 1：引擎中立常量模块 `socket-markers.ts`

**文件：**

- 创建：`src/runtime/socket-markers.ts`
- 测试：`src/runtime/socket-markers.test.ts`

- [ ] **步骤 1：编写失败的测试**

`src/runtime/socket-markers.test.ts`：

```ts
import { describe, expect, it } from "vitest";

import { SOCKET_MARKER } from "./socket-markers";

describe("SOCKET_MARKER", () => {
  it("locks the socket-marker visual constants (cross-engine parity)", () => {
    expect(SOCKET_MARKER.radius).toBe(0.06);
    expect(SOCKET_MARKER.color).toBe("#22d3ee");
    expect(SOCKET_MARKER.colorSelected).toBe("#f59e0b");
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`pnpm vitest run src/runtime/socket-markers.test.ts`
预期：FAIL，报错 `Cannot find module './socket-markers'`。

- [ ] **步骤 3：编写实现**

`src/runtime/socket-markers.ts`：

```ts
/**
 * Shared visual constants for the host-owned socket-marker overlay (v1.0 B4b),
 * so ThreeRenderHost and BabylonRenderHost render pixel-identical markers. Each
 * host converts the hex string to its engine colour type (THREE.Color /
 * Babylon Color3) — the values live here so they can never drift apart.
 */
export const SOCKET_MARKER = {
  /** Marker sphere radius in world units (matches the original ThreeViewport marker). */
  radius: 0.06,
  /** Sockets on non-selected nodes (cyan). */
  color: "#22d3ee",
  /** Sockets on the selected node (amber). */
  colorSelected: "#f59e0b",
} as const;
```

- [ ] **步骤 4：运行测试验证通过**

运行：`pnpm vitest run src/runtime/socket-markers.test.ts`
预期：PASS（1 passed）。

- [ ] **步骤 5：Commit**

```bash
git add src/runtime/socket-markers.ts src/runtime/socket-markers.test.ts
git commit -m "feat(runtime): socket-marker visual constants (engine-neutral, B4b)"
```

---

### 任务 2：契约 `syncSocketMarkers()` + Three 迁移（行为保持）+ Babylon 空实现桩

> 一次原子变更：markers 成为契约概念，Three 真实迁入，Babylon 先空桩满足类型。完成后 Three 视觉/行为零变化、构建绿。无 Three host headless 单测（WebGL），验证靠 typecheck/lint/全套 + 任务 4 的 smoke。

**文件：**

- 修改：`src/runtime/render-host.ts:28-54`（接口）
- 修改：`src/runtime/three/render-host.ts`（字段 / mount / objectChange / 删 onGizmoChange / dispose / 新方法）
- 修改：`src/ui/viewport/ThreeViewport.tsx`（删组件 markers、改调 host）
- 修改：`src/runtime/babylon/render-host.ts`（加空桩）

- [ ] **步骤 1：契约加 `syncSocketMarkers()`**

`src/runtime/render-host.ts`，在 `setSnapProvider(...)` 行之后、`dispose()` 之前插入：

```ts
  /** Rebuild the host-owned socket-marker overlay from the current snap
   *  provider (socket positions) + current selection (selected node's sockets
   *  render in the highlight colour). Idempotent; the viewport calls it after
   *  scene diffs and selection changes. Drag-time rebuilds are internal to the
   *  host. (v1.0 B4b — markers were component-owned through B4a.) */
  syncSocketMarkers(): void;
```

- [ ] **步骤 2：ThreeRenderHost 加 overlay 字段 + import**

`src/runtime/three/render-host.ts` 顶部 import 区加：

```ts
import { SOCKET_MARKER } from "@/runtime/socket-markers";
```

在 `private gizmoChangeCb` 字段附近（`private snapModifierDown = false;` 之前）加四个字段：

```ts
  private socketMarkers: THREE.Group | null = null;
  private socketGeo: THREE.SphereGeometry | null = null;
  private socketMat: THREE.MeshBasicMaterial | null = null;
  private socketMatSel: THREE.MeshBasicMaterial | null = null;
```

- [ ] **步骤 3：mount 里建 overlay**

`src/runtime/three/render-host.ts`，在 mount 内 `this.clock = new THREE.Clock();` 之前插入：

```ts
// Socket-marker overlay (B4b) — host-owned editor overlay alongside the
// OutlinePass selection highlight. Decoupled group of world-positioned
// markers, rebuilt on selection/scene change (syncSocketMarkers) and on
// every gizmo drag move (objectChange). Shared geo/material so group.clear()
// just detaches (no per-marker dispose); raycast is no-op'd so pickAt never
// hits a marker.
const socketGeo = new THREE.SphereGeometry(SOCKET_MARKER.radius, 8, 8);
const socketMat = new THREE.MeshBasicMaterial({
  color: new THREE.Color(SOCKET_MARKER.color),
});
const socketMatSel = new THREE.MeshBasicMaterial({
  color: new THREE.Color(SOCKET_MARKER.colorSelected),
});
const socketMarkers = new THREE.Group();
socketMarkers.name = "socketMarkers";
adapter.scene.add(socketMarkers);
this.socketGeo = socketGeo;
this.socketMat = socketMat;
this.socketMatSel = socketMatSel;
this.socketMarkers = socketMarkers;
```

- [ ] **步骤 4：加私有 `rebuildSocketMarkers` + 公开 `syncSocketMarkers`，删 `onGizmoChange`**

`src/runtime/three/render-host.ts`，删除现有 `onGizmoChange` 方法（连同其上方注释，原 `onGizmoChange(cb) { this.gizmoChangeCb = cb; }`），替换为：

```ts
  syncSocketMarkers(): void {
    this.rebuildSocketMarkers();
  }

  /** Rebuild the socket-marker overlay from the snap provider + current
   *  selection. Mirrors the pre-B4b ThreeViewport.rebuildSocketMarkers. */
  private rebuildSocketMarkers(): void {
    const group = this.socketMarkers;
    const adapter = this.adapterInstance;
    const geo = this.socketGeo;
    const mat = this.socketMat;
    const matSel = this.socketMatSel;
    if (!group || !adapter || !geo || !mat || !matSel) return;
    group.clear();
    const selId = this.currentSelectionId;
    const v = new THREE.Vector3();
    const noRaycast = () => {};
    for (const n of this.snapProvider?.() ?? []) {
      if (!n.sockets || n.sockets.length === 0) continue;
      const tobj = adapter.getRuntimeObject(n.id);
      if (!tobj) continue;
      tobj.updateWorldMatrix(true, false);
      for (const s of n.sockets) {
        const mk = new THREE.Mesh(geo, n.id === selId ? matSel : mat);
        v.set(s.position[0], s.position[1], s.position[2]).applyMatrix4(tobj.matrixWorld);
        mk.position.copy(v);
        mk.raycast = noRaycast;
        group.add(mk);
      }
    }
  }
```

然后删除 `private gizmoChangeCb` 字段声明（`private gizmoChangeCb: (() => void) | null = null;`）。

- [ ] **步骤 5：objectChange 改调私有重建**

`src/runtime/three/render-host.ts` 的 `gizmo.addEventListener("objectChange", ...)` 内，把 `this.gizmoChangeCb?.();` 改为：

```ts
this.rebuildSocketMarkers();
```

（保留上方 `this.snapDraggedObject();`；更新该行注释，去掉「stay in ThreeViewport」措辞，改为「host-owned socket markers track the dragged node」。）

- [ ] **步骤 6：dispose 清 overlay**

`src/runtime/three/render-host.ts` 的 `dispose()`，把 `this.gizmoChangeCb = null;` 替换为：

```ts
this.socketMarkers?.clear();
this.socketGeo?.dispose();
this.socketMat?.dispose();
this.socketMatSel?.dispose();
this.socketMarkers = null;
this.socketGeo = null;
this.socketMat = null;
this.socketMatSel = null;
```

- [ ] **步骤 7：ThreeViewport 删组件 markers + 改调 host**

`src/ui/viewport/ThreeViewport.tsx`：

1. 删除整段 socket-marker 代码（从 `// ── Socket markers (v0.4 C) ──` 注释起，到 `rebuildSocketMarkers` 函数闭合 `};` 止——即 `socketGeo`/`socketMat`/`socketMatSel`/`noRaycast`/`socketMarkers` group/`adapter.scene.add(socketMarkers)`/`const rebuildSocketMarkers = () => {...}`）。
2. 删除 `host.onGizmoChange(rebuildSocketMarkers);` 那一行（含其上方注释）。
3. `syncSelectionShell` 内的 `rebuildSocketMarkers();` 改为 `host.syncSocketMarkers();`。
4. 场景订阅内（`void diffAndApply(adapter, old, next, host);` 之后）的 `rebuildSocketMarkers();` 改为 `host.syncSocketMarkers();`。
5. 删除 cleanup 中的 `adapter.scene.remove(socketMarkers); socketMarkers.clear(); socketGeo.dispose(); socketMat.dispose(); socketMatSel.dispose();` 五行。
6. 更新组件顶部把 socket markers 列为「组件自有 extras」的那段头注释（改为：markers 现由 host 自有，组件只在 diff/选中后 `syncSocketMarkers()`）。

- [ ] **步骤 8：BabylonRenderHost 加空桩**

`src/runtime/babylon/render-host.ts`，在 `setSnapProvider(...)` 之后加（任务 3 替换为真实现）：

```ts
  // B4b: real overlay rendering lands in the next task; the no-op keeps the
  // IRenderHost contract satisfied so the build stays green.
  syncSocketMarkers(): void {}
```

- [ ] **步骤 9：验证（typecheck + lint + 全套）**

运行：`pnpm typecheck && pnpm lint && pnpm test`
预期：typecheck/lint 净（无 `gizmoChangeCb`/`onGizmoChange` 悬空引用）；测试全绿（无 Three host headless 单测，但任何 ThreeViewport 间接用例须仍绿）。

- [ ] **步骤 10：Commit**

```bash
git add src/runtime/render-host.ts src/runtime/three/render-host.ts src/ui/viewport/ThreeViewport.tsx src/runtime/babylon/render-host.ts
git commit -m "refactor(render-host): host-owned socket markers; migrate Three off component (B4b)"
```

---

### 任务 3：Babylon socket markers 实现 + 视口接线 + 单测

**文件：**

- 修改：`src/runtime/babylon/render-host.ts`
- 测试：`src/runtime/babylon/render-host.test.ts`
- 修改：`src/ui/viewport/BabylonViewport.tsx`

- [ ] **步骤 1：编写失败的测试**

`src/runtime/babylon/render-host.test.ts`，在文件顶部 import 区把 `Vector3` 旁补 `TransformNode`（最终为 `import { ArcRotateCamera, Mesh, NullEngine, TransformNode, Vector3 } from "@babylonjs/core";`）。再加一个 `boxNodeWithSockets` 辅助（紧随现有 `boxNode`）：

```ts
const boxNodeWithSockets = (
  id: string,
  pos: [number, number, number],
  sockets: { position: [number, number, number]; tag?: string }[],
): SceneNode =>
  ({
    ...boxNode(id),
    transform: {
      position: pos,
      rotation: [0, 0, 0, 1] as [number, number, number, number],
      scale: [1, 1, 1] as [number, number, number],
    },
    sockets,
  }) as SceneNode;
```

在 `describe("gizmo wiring (v1.0 B3b)", ...)` 之后新增 describe：

```ts
describe("socket markers (v1.0 B4b)", () => {
  function mountedWith(node: SceneNode) {
    const { host } = makeHost();
    host.mount(document.createElement("canvas"));
    host.adapter.syncNode(node, "add");
    host.setSnapProvider(() => [
      {
        id: node.id,
        sockets: node.sockets ?? [],
        visible: true,
        type: "mesh" as const,
      },
    ]);
    return host;
  }

  it("mount creates an empty socketMarkers overlay node", () => {
    const { host } = makeHost();
    host.mount(document.createElement("canvas"));
    expect(host.socketMarkersForTest).not.toBeNull();
    expect(host.socketMarkersForTest!.getChildMeshes()).toHaveLength(0);
    host.dispose();
  });

  it("syncSocketMarkers builds one marker per socket at the node's world position", () => {
    const host = mountedWith(
      boxNodeWithSockets("d", [2, 0, 0], [{ position: [1, 0, 0] }]),
    );
    host.syncSocketMarkers();
    const markers = host.socketMarkersForTest!.getChildMeshes();
    expect(markers).toHaveLength(1);
    // node at x=2, socket local x=1 → world x=3.
    expect(markers[0].position.x).toBeCloseTo(3, 5);
    expect(markers[0].isPickable).toBe(false);
    host.dispose();
  });

  it("selected node's markers use the amber material, others cyan", () => {
    const host = mountedWith(
      boxNodeWithSockets("d", [0, 0, 0], [{ position: [0, 1, 0] }]),
    );
    host.syncSocketMarkers();
    expect(host.socketMarkersForTest!.getChildMeshes()[0].material?.name).toBe(
      "socket-mat",
    );
    host.setSelection("d");
    host.syncSocketMarkers();
    expect(host.socketMarkersForTest!.getChildMeshes()[0].material?.name).toBe(
      "socket-mat-sel",
    );
    host.dispose();
  });

  it("syncSocketMarkers clears stale markers (rebuild, not append)", () => {
    const host = mountedWith(
      boxNodeWithSockets("d", [0, 0, 0], [{ position: [1, 0, 0] }]),
    );
    host.syncSocketMarkers();
    host.syncSocketMarkers(); // replay must not double the markers
    expect(host.socketMarkersForTest!.getChildMeshes()).toHaveLength(1);
    host.dispose();
  });

  it("nodes without sockets contribute no markers", () => {
    const { host } = makeHost();
    host.mount(document.createElement("canvas"));
    host.adapter.syncNode(boxNode("plain"), "add");
    host.setSnapProvider(() => [
      { id: "plain", sockets: [], visible: true, type: "mesh" as const },
    ]);
    host.syncSocketMarkers();
    expect(host.socketMarkersForTest!.getChildMeshes()).toHaveLength(0);
    host.dispose();
  });

  it("dispose tears down the overlay", () => {
    const host = mountedWith(
      boxNodeWithSockets("d", [0, 0, 0], [{ position: [1, 0, 0] }]),
    );
    host.syncSocketMarkers();
    expect(() => host.dispose()).not.toThrow();
    expect(host.socketMarkersForTest).toBeNull();
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`pnpm vitest run src/runtime/babylon/render-host.test.ts`
预期：FAIL（`socketMarkersForTest` 不存在 / 空桩不建 marker）。

- [ ] **步骤 3：BabylonRenderHost imports + 字段**

`src/runtime/babylon/render-host.ts`：

1. import 区把 `@babylonjs/core` 补 `MeshBuilder, StandardMaterial, TransformNode`，并加 `import { SOCKET_MARKER } from "@/runtime/socket-markers";`。
2. 在 `private snapModifierDown = false;` 之前加字段：

```ts
  private socketMarkers: TransformNode | null = null;
  private socketMat: StandardMaterial | null = null;
  private socketMatSel: StandardMaterial | null = null;
  private currentSelectionId: string | null = null;
```

- [ ] **步骤 4：mount 建 overlay + 材质；setSelection 存选中**

`src/runtime/babylon/render-host.ts` mount 内 `window.addEventListener("pointermove", this.onSnapPointer, true);` 之前插入：

```ts
// Socket-marker overlay (B4b) — host-owned, mirrors ThreeRenderHost. Unlit
// emissive spheres so they read regardless of scene lighting; isPickable=
// false keeps them out of pickAt. Shared materials, named for test asserts.
this.socketMarkers = new TransformNode("socketMarkers", scene);
const mkMat = new StandardMaterial("socket-mat", scene);
mkMat.disableLighting = true;
mkMat.emissiveColor = Color3.FromHexString(SOCKET_MARKER.color);
this.socketMat = mkMat;
const mkMatSel = new StandardMaterial("socket-mat-sel", scene);
mkMatSel.disableLighting = true;
mkMatSel.emissiveColor = Color3.FromHexString(SOCKET_MARKER.colorSelected);
this.socketMatSel = mkMatSel;
```

`setSelection(node_id)` 方法体首行（`const layer = this.highlight;` 之前）加：

```ts
this.currentSelectionId = node_id;
```

- [ ] **步骤 5：替换空桩为真实现 + 拖拽帧重建**

`src/runtime/babylon/render-host.ts`，把任务 2 的 `syncSocketMarkers(): void {}` 替换为：

```ts
  syncSocketMarkers(): void {
    this.rebuildSocketMarkers();
  }

  /** Rebuild the overlay from the snap provider + current selection. Disposes
   *  prior marker meshes (Babylon meshes need explicit dispose, unlike Three's
   *  shared-geo group.clear). Mirrors ThreeRenderHost.rebuildSocketMarkers. */
  private rebuildSocketMarkers(): void {
    const overlay = this.socketMarkers;
    const adapter = this.adapterInstance;
    const mat = this.socketMat;
    const matSel = this.socketMatSel;
    if (!overlay || !adapter || !mat || !matSel) return;
    for (const child of overlay.getChildMeshes()) child.dispose();
    const selId = this.currentSelectionId;
    for (const n of this.snapProvider?.() ?? []) {
      if (!n.sockets || n.sockets.length === 0) continue;
      const node = adapter.getRuntimeObject(n.id) as BabylonNode | undefined;
      if (!node) continue;
      node.computeWorldMatrix(true);
      const world = node.getWorldMatrix();
      for (const s of n.sockets) {
        const mk = MeshBuilder.CreateSphere(
          "socketMarker",
          { diameter: SOCKET_MARKER.radius * 2, segments: 8 },
          adapter.scene,
        );
        mk.parent = overlay;
        mk.isPickable = false;
        mk.material = n.id === selId ? matSel : mat;
        mk.position = Vector3.TransformCoordinates(
          new Vector3(s.position[0], s.position[1], s.position[2]),
          world,
        );
      }
    }
  }
```

在 `onGizmoDrag` 的内部 `finish` 闭包里，`node.computeWorldMatrix(true);` 之后加一行，让拖拽时 markers 跟随：

```ts
this.rebuildSocketMarkers();
```

- [ ] **步骤 6：dispose 清 overlay + 加测试 getter**

`src/runtime/babylon/render-host.ts` `dispose()`，在 `this.highlight?.dispose();` 之前加：

```ts
this.socketMat?.dispose();
this.socketMatSel?.dispose();
this.socketMarkers?.dispose(); // TransformNode.dispose cascades to child markers
this.socketMat = null;
this.socketMatSel = null;
this.socketMarkers = null;
this.currentSelectionId = null;
```

在 `gizmoManagerForTest` getter 旁加：

```ts
  /** Test-only surface: lets unit tests assert the socket-marker overlay. */
  get socketMarkersForTest(): TransformNode | null {
    return this.socketMarkers;
  }
```

- [ ] **步骤 7：运行测试验证通过**

运行：`pnpm vitest run src/runtime/babylon/render-host.test.ts`
预期：PASS（含新增 6 个 socket-marker 用例）。

- [ ] **步骤 8：BabylonViewport 接线**

`src/ui/viewport/BabylonViewport.tsx`：

1. `applyDiff` 函数体末尾（`syncGizmoTarget(useUIStore.getState().selectedNodeId);` 之后）加：

```ts
host.syncSocketMarkers();
```

2. UI 订阅里选中变分支（`host.setSelection(state.selectedNodeId); syncGizmoTarget(state.selectedNodeId);` 之后）加：

```ts
host.syncSocketMarkers();
```

- [ ] **步骤 9：验证（typecheck + lint + 全套）**

运行：`pnpm typecheck && pnpm lint && pnpm test`
预期：全绿。

- [ ] **步骤 10：Commit**

```bash
git add src/runtime/babylon/render-host.ts src/runtime/babylon/render-host.test.ts src/ui/viewport/BabylonViewport.tsx
git commit -m "feat(babylon): render socket markers (host-owned overlay, B4b)"
```

---

### 任务 4：roadmap 勾选 + 最终验证

**文件：**

- 修改：`docs/roadmap.md`

- [ ] **步骤 1：勾选 B4b**

运行 `rg -n "B4b" docs/roadmap.md` 定位条目，把对应 `- [ ]` 改为 `- [x]`（并按既有 B4a/B4c 行风格补「socket markers 跨引擎完成」措辞）。

- [ ] **步骤 2：最终全量验证**

运行：`pnpm test && pnpm typecheck && pnpm lint`
预期：`Test Files` 全 passed、`Tests` 全 passed（= 现有 613 + 任务 1 的 1 + 任务 3 的 6 ≈ 620）；typecheck/lint 净。

- [ ] **步骤 3：Commit**

```bash
git add docs/roadmap.md
git commit -m "docs(roadmap): B4b Babylon socket markers complete"
```

- [ ] **步骤 4：用户视觉 smoke（强制，host 渲染无 headless 覆盖）**

`pnpm tauri dev`，两引擎切换核对：① Babylon 选中带 socket 的节点 → 显示插槽球，选中节点 amber、其余 cyan；② 拖拽该节点 markers 跟随；③ socket 吸附仍准（B4c 修复不回归）；④ marker 不可被点选；⑤ Three 模式 markers 视觉/行为零变化（迁移无回归）。

---

## 自检

**规格覆盖度：** 契约 `syncSocketMarkers`（任务 2 步 1）、删 `onGizmoChange`（任务 2 步 4）、中立常量模块（任务 1）、Three 行为保持迁移（任务 2 步 2-7）、Babylon emissive unlit 球 + isPickable=false（任务 3 步 3-6）、三处接线（Three 任务 2 步 7 / Babylon 任务 3 步 8）、拖拽帧重建（Three 任务 2 步 5 / Babylon 任务 3 步 5）、测试（任务 1 + 任务 3）、roadmap（任务 4）——规格各节均有对应任务。非目标（可见性策略 / play 行为 / 池化）计划未触碰，符合 YAGNI。

**占位符扫描：** 无 TODO/待定；每个代码步骤含完整代码或精确编辑指令。

**类型一致性：** `syncSocketMarkers()`（契约↔两 host↔两 viewport 调用）、`socketMarkersForTest`（实现↔测试）、`SOCKET_MARKER.{radius,color,colorSelected}`（常量↔两 host↔测试）、`currentSelectionId`（Babylon 新字段↔setSelection↔rebuild）、材质名 `socket-mat`/`socket-mat-sel`（实现↔测试断言）—— 全一致。
